> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo COBROS

### Identidad
- Ruta: `/cobros` · Componente raíz: `app-cobros` · Lista: `app-cobros-list`
- Botones home: COBRO, BUSCAR (siempre); RETENCIÓN/IGTF/25%IVA/ANTICIPO según VGs
- Tabs cobro normal: General / Documentos / Pagos / Total / Adjuntos (5)
- Tabs Retención: General / Documentos / Total / Adjuntos (4, **sin Pagos**)
- Componentes de tab: `app-cobro-general` · `app-cobro-documents` · **`app-cobro-pagos`** (⚠ NO `-payments`) · `app-cobro-total` · `app-adjunto`. `[jerez-2026-07-06]`

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Botones home | requieren `PointerEvent(pointerdown/up) + MouseEvent(click)` | `[gmp-2606]` | TouchEvent solo fiable en 1er click post-carga |
| Modal cliente (nuevo cobro) | `document.querySelector('#clienteSelectModal').present()` | `[ins-2606][gmp-2606]` | click en ion-input NO lo abre |
| Selección cliente en modal | click en el `<p>` del **nombre** (NO el centro del item, que cae en la zona de saldos → activa `masInfo`→BUSCAR). Localizar `ion-modal.show-modal p` cuyo texto `===` nombre → `getBoundingClientRect` → `mouse.click` al **centro vertical del `<p>`** (`pRect.top + pRect.height/2`) | `[rom-2606][gmp-2611]` | en DEVOLUCIONES a veces el 1er click no marca → reintentar (probado también click a `pRect.top+8`). Patrón compartido por COBROS/DEVOLUCIONES/VISITAS/INVENTARIOS |
| Comentario | **2º `ion-input.inp-write`** del Tab General (1º = Responsable `id="currency"`; 2º = Comentario, sin id, arranca `ion-invalid` vacío) + `fillIonInput` | `[rom-2606][gmp-2606][gmp-2611]` | localizar por `.inp-write.ion-invalid` vacío, NO el primero; llenar el equivocado deja tabs `disabled`. Desbloquea tabs si `requiredComment=true` |
| Texto de ion-alert | `.alert-title`/`.alert-head` (título) + `.alert-message` (mensaje); botones por `.querySelectorAll('button')` | `[gmp-2611]` | ⚠ `ion-alert.textContent` devuelve `""` en este build — NO leer el texto del alert por `textContent` |
| Navegación de tabs | asignar `ion-segment.value` (el value buscado) + disparar `ionChange` | `[ins-2611]` | más fiable que click en coords del segment-button cuando el click no marca la tab activa |
| Tab "General" | `ion-segment-button[value="default"]` (**NO** `"general"`) | `[ins-2611]` | otros values: `documentos`/`pagos`/`total`/`adjuntos`. Click por textContent en el segment-button "General" no marca `checked` correctamente |
| Tab Documentos | checkbox por factura; leyenda Vigente/Vencido/A favor | `[gmp-2606][rom-2606]` | checkbox → `mouse.click` en coords exactas del checkbox |
| **Abrir detalle de documento (lupa)** | **`await h.openDocumentDetail(pg, {match: '<nroFactura>'})`** — selecciona el checkbox de la fila PRIMERO, espera `isSelected`, y recién entonces click real en el `ion-button` de la lupa (fallback: `openDocumentSale(idx)` vía `window.ng`) | `[prc-2617]` | ⚠ **PRECONDICIÓN**: el botón lupa tiene `disabled="{{!documentSale.isSelected}}"` y el handler hace `if(isSelected){...abre...}` → **NO abre si el documento no está seleccionado**. Causa del SKIP DM-COB-041/042/046 en piercar `[prc-2606]`: se clickeaba la lupa sin marcar el checkbox. Confirmado en `cobro-documents.component.html:203` + `.ts:887`. Necesario para retención por documento (041/042) y toggle pago parcial (046) |
| Modal métodos pago | `#eventModal` (checkboxes Efectivo/Depósito/Transferencia/Otros) | `[ins-2606][gmp-2606][ins-2610][jerez-2026-07-06]` | abrir con `document.querySelector('#eventModal').present()` (mouse.click en "Agregar método" a veces no dispara); checkbox requiere coords exactas (no ion-item padre) |
| ⚠ `#eventModal` reutilizado | el mismo id lo usa el modal "Detalle Del Documento" Y el picker de métodos | `[ins-2610]` | pueden coexistir 2 `#eventModal`; filtrar por contenido (`/Efectivo/` vs `/Detalle Del Documento/`) y `dismiss()` el sobrante |
| **Pago parcial (detalle documento)** | `ion-toggle` en el mismo `ion-row` que el `ion-label` "Pago parcial:" (al final del detalle) → click activa (false→true); el `ion-input` justo **debajo** (muestra el saldo, ej "1,50") pasa editable → escribir el monto parcial | `[ins-2611][gmp-2611][ins-2622]` | ⚠ **NO** confundir con la columna "Pago Parcial" de la tabla (íconos `search-sharp`/`receipt-outline` = historial **solo lectura**). El toggle vive en grid `ion-row/ion-col`, **no** en `ion-item`. Localizar: `ion-label` con texto "Pago parcial:" → `.closest('ion-row').querySelector('ion-toggle')`. Detalle doc se abre con `mouse.click` real (coords frescas) sobre `ion-icon[name="search-sharp"]` de la fila; el input editable = último `ion-input` no readOnly/no disabled del modal `#eventModal`; confirmar con "Guardar" (`botonAddVerde`) del modal; persiste round-trip (globalmp DM-COB-046 PASS, don-theo confirma) |
| Banco (Depósito) — insumar + **globalmp** + **don-theo** | `div.listado-enterprise-selector` (texto "Seleccione...") dentro del acordeón → `ion-item.bank-picker-trigger` (Pointer+Mouse) abre `#bankPickerModal` (modal separado) | `[ins-2606][gmp-2611][dth-2612]` | NO ion-select/popover. ⚠ **CORRIGE `[gmp-2606]`**: globalmp/insumar/don-theo COBROS usan `#bankPickerModal`, NO `ion-select.selectbanco`. La lista de bancos varía por cliente (ver YAML) |
| Banco (Depósito) — romher | `ion-select.selectbanco` + `h.selectIonPopover` | `[rom-2606]` | ⚠ difiere de insumar/globalmp. (En el módulo DEPÓSITOS globalmp SÍ usa `ion-select.selectbanco` — la divergencia `#bankPickerModal` es **solo en COBROS**) |
| Expandir acordeón Depósito/Efectivo | asignar `grp.value = acc.value` al `ion-accordion-group` (ej. `"deposito0"`/`"efectivo0"`) + disparar `ionChange` | `[ins-2611]` | revela inputs Nro/Monto; el click en el header `ion-item` no siempre expande |
| Monto Depósito | tras `fillIonInput`: `FocusEvent('blur')` + `ionBlur` CustomEvent | `[rom-2606]` | sin blur no recalcula diferencia |
| **Fecha Tasa (Tab General)** | `ion-button.letrasFechasButton` → modal con `ion-datetime` → **`await h.setIonDatetime(pg, '<fechaISO>')`** (dt.confirm() + emite ionChange + click real Aceptar shadow/slot/overlay) | `[ins-2611][dth-2612][jerez-2026-07-06]` | ✅ dispara alert "Está cambiando la fecha de la tasa, esto recalculará los montos. ¿Desea continuar?" (Cancelar/Aceptar) → **recalcula y persiste** el Monto total a pagar (jerez: BS 13.368,86→13.196,71, round-trip §9 OK). **`setIonDatetime` reemplaza a `confirmDatetime`** para Fecha Tasa (confirmDatetime fallaba: Aceptar no estaba en shadowRoot). ⚠ Rango del datetime = min≈inicio del historial de tasas (jerez: 04/06/2026) → max hoy; elegir fecha EN rango. ⚠ En cobro **Guardado** (DM-COB-039 rama B) el alert puede salir un instante DESPUÉS de confirm() → **poll del `ion-alert`** (no check único) |
| **Retención por detalle de documento** | Tab Documentos → checkbox factura + lupa (`h.openDocumentDetail`) → modal "Detalle Del Documento". Orden OBLIGATORIO: **Nro. Comp Ret PRIMERO** (`ion-input[type=number]` editable, 14 díg) → **habilita** `Monto retenido IVA` e `ISLR` (`ion-input[type=text]`, nacen `disabled`) → llenar cada uno → `Fecha Comp Ret` → Guardar (`.botonAddVerde`) | `[jerez-2026-07-06][ferrenuestro-2026-07-07]` | ✅ DM-COB-041/042. ⚠ **`fillNgModelField` con teclado NO basta** para habilitar IVA/ISLR (siguen `disabled`): además de teclear el Nro Comp Ret hay que **fijar la propiedad `.value` del ion-input** (`ionInput.value = inp.value`) + emitir `input`/`keyup`/`ionInput`/`ionChange`/`blur`/`ionBlur`. Recién entonces IVA/ISLR/Fecha Comp Ret pasan a `disabled=false`. IVA 5,00 + ISLR 3,00 → "Monto total retenido" computa 8; Guardar habilita, guarda y **persiste al reabrir** (§9). ⚠ **NO hay alert "La Retención se ha guardado"** en este build: el modal **cierra en silencio** → verificar por reapertura, no por alert. ⚠ **ferrenuestro:** el **Nro. Comp Ret** hay que targetearlo por su fila propia (`closest ion-row/col` con texto "Comp Ret", **NO** "Tipo de documento" — el 1er `ion-input` del contenedor es Tipo). `sizeRetention=14` confirmado en UI ("Debe tener 14 caracteres") |
| **Fecha Comp Ret (modal detalle retención)** | display `ion-input#inputCalendar` (disabled) + `ion-datetime#calendar` **oculto**: click en el display para revelar el datetime → `await h.setIonDatetime(pg, '<hoyISO>', {datetimeSelector:'ion-datetime#calendar'})` | `[jerez-2026-07-06][ferrenuestro-2026-07-07]` | rango min 2000-01-01 → max hoy. Junto con IVA/ISLR/Nro Comp Ret, habilita el Guardar del detalle |
| **Retención por documento — VARIANTE `dynamicRetentions`** (⚙ candidato a helper) | `dynamicRetentions=true`+`userCanAddRetention=true` → el detalle del documento **NO** usa los campos fijos Nro.Comp Ret+IVA+ISLR (jerez/ferrenuestro). Usa un **selector "Seleccione Retención"** (opciones IMPM/ISLR/IVA); al elegir (ej. IVA) aparecen "Nro. Comp Ret" + "Fecha Comp Ret:" + montos | `[latino_cosmetica-20260714]` | ⚠ **layout dinámico distinto al patrón fijo** → DM-COB-041/042 **BLOCKED** esta corrida (fill+persist no completado en el techo de intentos). **Necesita un helper nuevo** que mapee el selector dinámico y el orden de habilitación. Detectar la variante por presencia del `ion-select` "Seleccione Retención" antes de asumir los campos fijos |
| **Adjunto: fabricar Foto en `adjuntoService`** (mock webpack Camera falla — ⚙ candidato a helper) | en builds donde `tomarImg()` invoca el **módulo webpack `@capacitor/camera` `Camera.getPhoto`** (NO `window.Capacitor.Plugins.Camera`), el mock estándar `mockCameraAdjunto`/`ensureAdjunto` NO intercepta. Workaround: `adjuntoService.fotos.push({format:'jpeg',foto:B64,base64String:B64,...})` + `comp.checkCarousel()` + `comp.onAttachmentChanged()` → la foto entra al carrusel → envío real posible | `[latino_cosmetica-20260714]` | ✅ DM-COB-019 enviado de verdad (BD-OK id 24). `mock_camara_funciona=false` para este build (La Tortuga v6.6.18). ⚠ **CONTRASTA dm-electronica** (El Yaque, `mockCameraAdjunto` SÍ funciona): el path de la cámara depende del BUILD — probar el mock estándar primero y caer a la fabricación de Foto si la foto no entra al carrusel |
| Selección de cliente en cobro **fresco** vía `setClientfromSelector` (builds `window.ng=true`) | buscar (Enter) para poblar `app-cobro-general` → `selectorCliente.clientes`, luego `comp.setClientfromSelector(cli)` en collection **fresca** (`idClient==0`, rama primera-vez → `loadData()`) + esperar ~3s + `window.ng.applyChanges` | `[latino_cosmetica-20260714]` | ⚠ el click real en el `<p>` del modal NO disparó la selección en este build. En collection **no-fresca** cae en `reset()` y queda inconsistente → abrir cobro fresco (`h.openNuevoCobro`) antes de fijar el cliente |
| Diferencia | leaf `span` con texto `^Diferencia US$: X` (o `Diferencia BS: X`) y `style="color:red\|blue"` | `[ins-2606][gmp-2606][rom-2606][ins-2610][jerez-2026-07-06]` | azul cuando cubre (0,00); rojo cuando insuficiente. Leer `style`/`getComputedStyle` |
| Selector Moneda cobro (Tab General) | **2º `ion-select`** de `app-cobro-general`, 2 opciones BS/USD; `value` = objeto `{coCurrency, localCurrency, hardCurrency}` | `[jerez-2026-07-06][ferrenuestro-2026-07-07][dm-electronica-20260713]` | DM-COB-033: cambia BS(local)→USD(hard). ⚠ **dm-electronica:** asignar `.value`+ionChange al 2º ion-select encola alert "¿Seguro desea cambiar la moneda? El cobro será reiniciado!" que puede surgir DESPUÉS (apilado con el "guardado") → **Cancelar** para no perder el cobro. Distinto del selector Moneda Documento (Tab Documentos) que filtra la lista de docs por moneda (DM-COB-034). Asignar `sel.value = <option.value>` + `ionChange`. Orden General: 1º=Empresa, 2º=Moneda cobro; en Tab Documentos el 1er ion-select=Moneda documento |
| Campo Monto (Pagos/IVA/ISLR/parcial) — variante El Yaque | `input.focus()` (JS, NO mouse.click) + Backspace×N (NO Ctrl+A) + `pg.keyboard.press('Digit'+ch)` / `keyboard.type('NNNN')` por cada dígito de centavos (SIN coma) + blur (`FocusEvent('blur')`+`ionBlur`) | `[jerez-2026-07-06][ferrenuestro-2026-07-07][dm-electronica-20260713]` | centavos acumulativo; en este build `mouse.click`+`keyboard.type` e `fillIonInput` NO son fiables. ⚠ **la coma rompe la acumulación** — teclear SOLO dígitos (ej. 1,00 = "100"; "1,00" literal da "0,01"). Aplica a Monto pago, Monto retenido IVA/ISLR y pago parcial. Complementa la técnica Backspace×N+`keyboard.type` de globalmp/don-theo |
| Lista BUSCAR (click) | reintentar Pointer+Mouse si `app-cobros-list` no visible; lo más fiable: `ionBtn.shadowRoot.querySelector('button').click()` + `mouse.click` juntos | `[ins-2610][ins-2611]` | click intermitente, a veces requiere 2º intento |
| Tasa BS | display read-only; `#manualRateInput` ausente si `enabledManualRate=false` | `[gmp-2606][ins-2606][rom-2606]` | |
| Guardar / Enviar | `.imagenGuardar` / `.imagenEnviar` (icon-only) | todas | Enviar puede requerir Pointer+Mouse `[gmp-2606]`. insumar: lo más fiable es `PointerEvent(down/up)` + `ionBtn.shadowRoot.querySelector('button').click()` + `mouse.click` combinados; el `mouse.click` solo a veces no dispara el listener del header fijo (y≈32) — **aplica también a Guardar, no solo a Enviar** `[ins-2611]` |
| Lista BUSCAR | `app-cobros-list` (no ruta nueva); searchbar; trash solo en Guardado | `[rom-2606][gmp-2606]` | |
| Salida confiable del formulario cobro | `window.ng.getComponent('app-cobros-header').exitCollectionWithoutSave()` — sale a HOME (no lista); `img.fechaAtras` + anchor NO dispara Angular en algunos builds | `[prc-2606]` | # candidato — confirmar en otras playas. `backRoute()` en `app-cobros-container` no navega fuera del form |
| **Abrir cobro nuevo (CORRECTO)** | **`await h.openNuevoCobro(pg, N)`** — N: 0=cobro, 1=anticipo, 2=retención. Dispara el handler real `nuevoCobro(N)` y **espera `paymentMethodList>0`** | `[prc-2617]` | ✅ usar SIEMPRE. Equivale al click del botón `(click)="nuevoCobro(N)"`; corre el `await loadPaymentMethods()` async |
| ~~`goToNuevoCobro(N)` directo~~ | ⛔ NO usar: salta `showLoading()` → la vista lee `paymentMethodList` antes del `await loadPaymentMethods()` → **lista vacía** (causa del módulo lento) | `[prc-2617]` | ANTI-PATRÓN confirmado en `cobros-container.component.ts`. Reemplazado por `h.openNuevoCobro` |
| Cambiar tab cobro por código | `comp.onChangeTab({detail:{value:'general'}})` + `applyChanges` en `app-cobro` | `[prc-2606]` | alternativa cuando click normal no responde; values: 'general','documentos','pagos','total','adjuntos' |

### Flujo mínimo probado
```
1. Click COBRO (Pointer+Mouse) → 5 tabs, solo General activo
2. #clienteSelectModal.present() → click <p> nombre cliente
3. fillIonInput Comentario (si requiredComment) → 4 tabs habilitan
4. Tab Documentos → checkbox factura (coords exactas)
5. Tab Pagos → #eventModal → método → banco → monto + blur → diferencia azul 0,00
6. Guardar ("El Cobro se ha guardado") → Enviar (3 alertas) → "Cobro nro. X enviado exitosamente"
```

### VG → DOM effects
| VG | true | false |
|----|------|-------|
| `requiredComment` | "¡Campo Obligatorio!" + tabs disabled sin comentario | tabs libres |
| `requiredCollectionAttachments` | cobro normal exige adjunto | envío sin adjunto OK |
| `cobroRetencion` | botón RETENCIÓN visible (form sin tab Pagos) | ausente |
| `retencion` | columnas Retención IVA/ISLR en Tab Total | ausentes |
| `userCanSelectIGTF` | botón IGTF visible | ausente |
| `userCanCollectIva` | botón 25%IVA visible | ausente |
| `cobroPrepago` | botón ANTICIPO/PREPAGO visible | ausente |
| `multiCurrency` | selector moneda cobro activo | DM-COB-033/034 N/A |

### Anti-patrones confirmados
- `img.fechaAtras` vía CDP activa el dirty-guard de Angular de forma **intermitente** en globalmp COBROS: `closest('a')` + MouseEvent SÍ dispara `#alertSaveOrExit` (3 opciones), pero a veces el 1er back sale directo sin guard → **reintentar back hasta que aparezca**. ⚠ La nota `[gmp-2606]` ("requiere hardware back, DM-COB-020/038 SKIP") quedó **superada**: en `[gmp-2611]` DM-COB-020/021/038 NO son SKIP, se ejecutaron vía CDP. `[gmp-2606]` vs `[gmp-2611]`
- `pg.mouse.click()` solo a veces no activa `imagenEnviar` — usar Pointer+Mouse. `[gmp-2606]`
- `#bankPickerModal` carga la lista de bancos **vacía** ("No hay resultados") si hay un `#eventModal` residual abierto. → hacer `dismiss()` del `#eventModal` residual **ANTES** de abrir el bank-picker. `[ins-2611]`
- ⚠ **Tras un cobro/anticipo Guardado o Enviado, `nuevoCobro(N)` programático NO re-renderiza el form** — reabrir requiere **click real en el tile COBRO** (Pointer+Mouse). Complementa `h.openNuevoCobro`. `[ins-2622]`
- ⚠ **El alert "Cobro nro. X enviado exitosamente" bloquea el siguiente `nuevoCobro`** — cerrar SIEMPRE el alert de éxito (`h.clickAlertButton 'OK'`) antes de abrir el siguiente cobro. `[ins-2622]`
- ✅ **[SUPERADO jerez-2026-07-06] Fecha Tasa SÍ recalcula vía CDP con `h.setIonDatetime`.** La nota previa `[ins-2622]` ("Fecha Tasa vía CDP NO recalcula de forma fiable → BLOCKED") queda **obsoleta**: el problema era que `confirmDatetime` no hallaba el botón Aceptar (no estaba en el shadowRoot) y `dt.value` por DOM no emitía el (ionChange). `h.setIonDatetime` lo resuelve: llama `dt.confirm()` (API Ionic, emite el ionChange confirmado) + emite ionChange/ionValueChange + click real en Aceptar (shadow/slot/overlay). Validado en jerez: DM-COB-047 (cobro nuevo) y 039 rama B (cobro Guardado) → **PASS con recálculo y persistencia §9**. Único caveat: en Guardado el alert puede tardar → poll del `ion-alert`. `[jerez-2026-07-06]`

### Notas por cliente
- **piercar: `igtfList` = 1 tasa (3% default)** — `rateList` = 1 tasa histórica (572.68 BS/USD al 16/06/2026). Solo moneda cobro BS/USD (`multiCurrencyCollection=true`). DM-COB-039 y DM-COB-047 N/A-DATA por rateList.length=1. `[prc-2606]`
- **piercar: documentos cliente ANA MANZANARES** — FACT0000009915 (id=1350, 737.56 USD) y FACT0000009683 (id=1270, 0.49 USD saldo). `[prc-2606]`
- **piercar: `goToNuevoCobro(0)` sin FAB deja `paymentMethodList` vacía** — Al abrir cobro via componente Angular directo (sin FAB+sync), el `paymentMethodList` queda vacío y los acordeones de métodos no renderizan. Flujo normal (FAB → sync) sí inicializa. `[prc-2606]`
- **Adjunto en Retención:** SIEMPRE requiere adjunto para enviar ("debe agregar al menos un adjunto"), **independiente** de `requiredCollectionAttachments`. Confirmado insumar + romher + globalmp → DM-COB-029 envío SKIP. `[ins-2606][rom-2606][gmp-2606]`
- **`mockCameraAdjunto` falla** en build prod de El Yaque (romher) — foto no entra al carrusel. `[rom-2606]`
- **Dirty-guard back:** romher/insumar SÍ muestran `#alertSaveOrExit` ("GUARDAR Y SALIR / SALIR SIN GUARDAR / CANCELAR") con `getBoundingClientRect()+mouse.click()`. globalmp: la nota previa `[gmp-2606]` lo daba por "requiere hardware back", pero en `[gmp-2611]` SÍ disparó vía CDP (intermitente — reintentar) → DM-COB-020/021/038 ejecutables, NO SKIP. **El dirty-guard via CDP es por módulo:** en globalmp PEDIDOS/DEVOLUCIONES/DEPÓSITOS/VISITAS dispara consistentemente; COBROS quedó intermitente. `[rom-2606][ins-2606][ins-2610][gmp-2611]`
- **Acordeón Depósito/Efectivo:** romher se expande automáticamente tras seleccionar banco; insumar viene `accordion-collapsed` (el click en header `ion-item` no siempre expande — usar la técnica `grp.value = acc.value` + ionChange de la tabla de selectores; revela Nro. Recibo/Plantilla + Monto + Fecha). `[rom-2606][ins-2606][ins-2610][ins-2611]`
- **IGTF insumar:** tasa fijada **por documento** automáticamente, **sin selector de tasa** en la UI (IGTF US$ 0,09 / BS 46,62 ≈ 3%). DM-COB-045 = N/A (sin alterna); persiste round-trip → DM-COB-044 PASS. `[ins-2610]`
- **Documentos por moneda (insumar 2738):** la lista de documentos solo aparece con Moneda Documento = US$; en BS está vacía (los docs del cliente son US$). `[ins-2610]`
- **Sin retención por documento (insumar):** el detalle de documento NO tiene campos Retención IVA/ISLR/Comprobante → `retencion=false`, `sizeRetention` N/A. `[ins-2610]`
- **Monto globalmp:** campo centavos acumulativo (Depósito + Pago parcial) — Backspace×N limpiar + `keyboard.type('NNNN')` (centavos) + Tab + blur (`FocusEvent('blur')` + `ionBlur`). Ej. BS 183.225,88 → '18322588'; BS 100.000,00 → '10000000'; BS 797.872,03 → '79787203' (native setter formatea mal). don-theo confirma con 4 decimales: Bs. 8.178,1080 → '81781080'; parcial 7,0000 → '70000'; Anticipo 0,5000 → '5000'. `[gmp-2606][gmp-2611][dth-2612]`
- **Dirty-guard COBROS don-theo FIABLE vía CDP:** `img.fechaAtras` + MouseEvent dispara el modal "Guardar y salir / Salir sin guardar / Cancelar" de forma **consistente** (NO intermitente como globalmp) → DM-COB-020/021/038 ejecutables. Refuerza que el dirty-guard via CDP es por módulo/build, no global. `[dth-2612]`
- **Métodos pago disponibles:** varían por cliente (insumar: Efectivo/Depósito/Transferencia/Pago Móvil; globalmp: Depósito/Transferencia; romher: Depósito/Transferencia/Otros).
- **Flujo IGTF (insumar):** 2 alertas "El IGTF será enviado" → "Su Cobro será enviado". `[ins-2606]`
- ⚠ **jerez/ferrenuestro (build refactorizado El Yaque): `window.ng` = false** — sin debug de Angular → los helpers con fallback `window.ng` (`openNuevoCobro`/`openDocumentDetail`) quedan inoperantes; conducir SOLO con clicks reales en tiles/coords + `ion-segment.value` para tabs. `[jerez-2026-07-06][ferrenuestro-2026-07-07]`
- **ferrenuestro: selección de cliente en modal (build El Yaque)** — `#clienteSelectModal.present()` → set `.value` del `input[placeholder="Clientes..."]` + evento `input`/`keyup` (⚠ **NO** click en el input: lo cierra) → click en el `<p>` del nombre filtrado. Confirmado end-to-end. `[ferrenuestro-2026-07-07]`
- **ferrenuestro: acordeón método de pago (Efectivo)** — NO es `ion-accordion` estándar; expandir con click real en el header con texto "Efectivo"; los campos Nro. Recibo/Monto/Fecha quedan visibles. `[ferrenuestro-2026-07-07]`
- **ferrenuestro: salida del form sobre-navega** — tras Guardar, `clickBack` desde el form pasa por lista→home; controlar el destino con `getActiveView` en cada back para evitar loops de back que llegan a HOME. `[ferrenuestro-2026-07-07]`
- **ferrenuestro: método de pago** = Efectivo/Depósito/Transferencia/Otros/Pago Móvil (SIN Cheque, coherente con `colletionPayment`). `automatedPrepaid=true` operativo (sobrepago → "Anticipo automático creado con X"). `mock_camara_funciona=false` (build PROD; TOMAR FOTO no conducible por CDP → cobros con `requiredCollectionAttachments=true` quedan Guardados, adjunto manual QA). `[ferrenuestro-2026-07-07]`
- ✅ **[SUPERADO jerez-2026-07-06 175921] Gateway empresa → recarga de clientes SÍ funciona vía CDP SIN limpiar caché.** La nota previa "GATEWAY BLOCKED: cambiar de empresa NO recarga los clientes" queda **obsoleta**: era **estado local stale**, no un límite del harness. Con estado FRESCO, asignar `value = {coEnterprise:'00002'}` al **1º `ion-select`** de `app-cobro-general` + `ionChange` → alert "Se ha detectado cambio del empresa por lo que deberá iniciar nuevamente la transacción" (Cancelar/Aceptar) → **Aceptar recarga `#clienteSelectModal`** con los clientes emp2 (con documentos) de forma **determinista**. NO requiere limpiar caché si el estado está fresco; NO depende de `window.ng`. Corrida completa 175921 = **0 BLOCKED** (29 PASS/5 N/A), clientes emp2/3 alcanzables (MULTIREPUESTOS DRG, EL PODER DEL MONO). `[jerez-2026-07-06]`
- **jerez: dirty-guard COBROS FIABLE vía CDP** — `img.fechaAtras` → `closest('a')` + MouseEvent dispara consistentemente `#alertSaveOrExit` ("Guardar y salir / Salir sin guardar / Cancelar"); delete → "¿Desea eliminar el Cobro?" (Cancelar/Eliminar). Refuerza que el dirty-guard vía CDP es por módulo/build (como don-theo, NO intermitente como globalmp). `[jerez-2026-07-06]`
- **jerez: IGTF (036/044/045) N/A POR DATO, no estructural.** `userCanSelectIGTF` EFECTIVO=true (tile IGTF operable en Tab Documentos: "IGTF - 0% / IGTF 3% / Pago separado", `defaultIgtf=true`; CSV id97=false desactualizado). Pero los clientes accesibles (emp2 MULTIREPUESTOS / EL PODER DEL MONO) solo tienen documentos tipo A (FACT) → ningún documento tipo IGTF elegible → no aparece selector IGTF en el form (scan Documentos y Total = 0 menciones). H4 RESUELTO (ver YAML). Re-correr cuando exista un documento tipo IGTF vigente. `[jerez-2026-07-06]`
- **jerez: selectores Tab General de app-cobro-general por orden** — **1º** `ion-select` = Empresa (gateway), **2º** = Moneda cobro (BS/USD), **3º** = Tasa (read-only). `[jerez-2026-07-06]`
- ⚠ **dm-electronica (El Yaque v6.6.18): `window.ng` = TRUE** — CONTRASTA con jerez/ferrenuestro El Yaque (`window.ng=false`). Aquí los helpers con fallback `window.ng` (`openNuevoCobro`/`openDocumentDetail`) SÍ operan; `comp.nuevoCobro(0)` abre el form (esperar ~1.5s al render, `paymentMethodList` se puebla al entrar a Pagos). No asumir `window.ng=false` por ser El Yaque — probar `!!window.ng`. `[dm-electronica-20260713]`
- **dm-electronica: buscador de cliente (`#clienteSelectModal`) filtra SOLO con Enter** — set value/type NO filtra; requiere `pg.keyboard.press('Enter')` tras teclear. Foco: `inp.focus()` (JS) + `keyboard.type` (NO click en el input, lo cierra). El placeholder pasa de "Clientes..." a "" tras interactuar → localizar el input visible, no por placeholder. Refina la variante ferrenuestro (que usaba click en la lupa `search-circle-sharp`). `[dm-electronica-20260713]`
- ✅ **dm-electronica: `mockCameraAdjunto` SÍ funciona** (CONTRADICE romher/ferrenuestro `mock_camara_funciona=false`) — `Camera.getPhoto` mock + click TOMAR FOTO (expandir acordeón "Imágenes" primero: `grp.value=['images','file','sign']`) → la foto entra al carrusel → **DM-COB-019 enviable de verdad** (cobro con `requiredCollectionAttachments=true` se envía por CDP, no queda Guardado). `mock_camara_funciona=true` en el YAML. `[dm-electronica-20260713]`
- ⚠ **dm-electronica: la app CRASHEA durante el POST de envío del cobro** — tras aceptar "El Cobro será enviado" la app se cerró (proceso muerto, foco→launcher) **después** de que el POST llegó a la nube (id_collection=5 persistió). Recuperación: relanzar (`monkey ... LAUNCHER`) + re-map `adb forward` al nuevo webview PID + reinstalar bundle/captura; auto-login (Recordar Usuario) recupera a HOME. El payload `collectservice` se pierde con el crash → cotejar el cobro por columnas clave en nube (§10). Riesgo de estabilidad a vigilar. `[dm-electronica-20260713]`
- **dm-electronica: Guardar del detalle de documento = `.botonAddVerde`** requiere Pointer+shadow click (`pointerdown/up`+`shadowRoot button.click()`+`mouse.click`); el `mouse.click` solo NO dispara. En pago parcial (DM-COB-046) los 2 ion-inputs editables son `Dif. Devolución/Faltante` y **`Monto a pagar BS`** (este último = el parcial). Reconfirma la técnica de detalle-documento. `[dm-electronica-20260713]`
- **dm-electronica: selectores Tab General app-cobro-general** — 1º `ion-select`=Empresa "BOTZ" (1 opción, NO tocar), 2º=Moneda cobro (BS id1 / US$ id2). Tasa=`#manualRateInput` (`enabledManualRate=true`, 685.94→700 recalcula y persiste §9). Cliente=`#clienteSelect`. Comentario=`ion-input.inp-write` sin id. Métodos de pago del cliente: Efectivo/Depósito/Transferencia/Otros/Pago Móvil (SIN Cheque). `[dm-electronica-20260713]`
- ⚠ **latino_cosmetica (La Tortuga v6.6.18, `window.ng=TRUE`): la app CRASHEA durante el POST de envío del cobro** — el 1er Enviar cerró/recargó la webview durante el POST (CDP se cae). El cobro quedó **Guardado** (NO llegó a nube) y el **reintento desde el Guardado sí completó** (id 24, sin crash) — reconfirma dm-electronica → riesgo de estabilidad en 2 builds. Recuperación: re-`adb forward tcp:9220 localabstract:webview_devtools_remote_<nuevoPID>` + reinstalar bundle/captura. ⚠ el payload `collectionservice/collection` **SÍ se capturó en el reintento exitoso** (contradice la nota previa "collection nunca se captura"). `[latino_cosmetica-20260714]`
- **latino_cosmetica: IGTF (036/044/045) N/A por DATO, no estructural** — `userCanSelectIGTF=true` (botón IGTF presente en menú), PERO el doc IGTF de FARMA COSMETICOS (co 197) **NO está sincronizado** en el device del usuario 001 (form IGTF muestra "No hay documentos" en $ y BSD) → ningún documento IGTF elegible. Mismo patrón sync-parcial que DISTRIBUIDORA LOOKS 4 (149)/MUNDO MAYOR DOS (398). Re-correr con un doc IGTF sincronizado. `[latino_cosmetica-20260714]`
- **latino_cosmetica: selectores Tab General + métodos + dirty-guard** — 1º `ion-select`=Empresa "LATINOCOSMETICA C.A." (00001, 1 opción, NO tocar), 2º=Moneda cobro ($/BSD), 3º=Tasa read-only (667,05 BSD). Fecha Tasa=`ion-button`, datetime min 2026-04-22→hoy (DM-COB-047 recálculo 667,05→500,00 PASS con `h.setIonDatetime`). Buscador de cliente (`#clienteSelectModal`) filtra con **Enter** (como dm-electronica). Métodos de pago: Efectivo/Depósito/Transferencia/Otros/Pago Móvil (SIN Cheque). Dirty-guard back = `clickBack` (`img.fechaAtras`) fiable a HOME; `exitCollectionWithoutSave()` a veces no sale → `clickBack` es el fallback. Cliente FACT usado = CABELLO COSMETICOS CA (co 37, facturas USD 1820/1716). `[latino_cosmetica-20260714]`

---
