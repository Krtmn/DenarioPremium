> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo PEDIDOS

### Identidad
- Ruta: `/pedidos` (home) → `/pedido` (form) · `/pedidosLista` (lista BUSCAR)
- Componente home: `app-pedidos` (PEDIDO/BUSCAR/COPIAR) · Form: **`app-pedido`** (singular) · Lista: **`app-pedidos-lista`**
- Leer totales/tabs/comentario desde `app-pedido`, NO `app-pedidos` (evita "innerText vacío"). `[ins-2610]`
- Tabs formulario: General / Pedido / Total / Adjunto

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Modal cliente | detectar con `classList.contains('show-modal')` | `[ins-2606]` | NO usar ausencia de overlay-hidden |
| Empresa | `ion-select` idEnterprise preseleccionado | `[gmp-2606]` | |
| Categorías producto | lista de grupos por marca/familia → click expande acordeón | `[gmp-2606][ins-2606]` | |
| Producto = sub-acordeón anidado | Tab Pedido: cada categoría (ej. CAPRI 60) es `ion-list > div.product-structure-title.listaProductos > ion-item.listaItems` (texto "NN-NOMBRE ⟨conteo⟩"); click en la cabecera `ion-item.listaItems` inyecta los productos en el mismo `div.listaProductos`. **Cada producto es a su vez un sub-acordeón colapsado** → click en el `ion-item` lo expande y revela su input cantidad. **Dos niveles:** categoría → producto | `[gmp-2611][dth-2612][hidroponias-20260710]` | ⚠ **NO** es `ion-accordion`. el `ion-input[type="number"]` tiene height 0 mientras el producto está colapsado |
| Producto = `ion-accordion` (build refactorizado El Yaque) | En ferrenuestro (Isla Coche, `window.ng=false`) los productos **SÍ** son `ion-accordion.product-structure-title.accordionPedidos` dentro de `ion-accordion-group`; header = `ion-item` (Nombre+Código+Precio+Inventario), contenido = `ion-grid.contenidoProductos` con `ion-input[type=number]` (height 0 colapsado). Click en el header (`y=top+30`) expande y revela el input. Reconfirmado dm-electronica y **latino_cosmetica** (La Tortuga v6.6.18, `window.ng=TRUE`): `ion-accordion.accordionPedidos` dentro de categoría `ion-item.listaItems`; la categoría se colapsa al cambiar de tab (re-expandir) | `[ferrenuestro-2026-07-07][hidroponias-20260710][dm-electronica-20260713][latino_cosmetica-20260714]` | ⚠ **CONTRADICE** la nota `[gmp-2611]` "NO es ion-accordion" — la estructura del Tab Pedido **difiere por build**: builds viejos = `div.listaProductos` anidado; build refactorizado El Yaque = `ion-accordion` real. Detectar por presencia de `ion-accordion-group` |
| Alert "sin inventario" | `ion-alert#alertNB` "Denario / Este producto no tiene inventario. Verifique antes de enviar pedido." (botón OK) tras cargar cantidad a un producto sin stock | `[dth-2612]` | su `ion-backdrop` a pantalla completa **intercepta TODOS los clicks** posteriores (no se puede expandir otra categoría). Cerrar con `h.clickAlertButton(pg,'OK')` ANTES de continuar |
| Cantidad producto | `ion-input[type="number"]` en acordeón; visibilidad por `getBoundingClientRect().height>0` (tras expandir el producto); `fillIonInput` + blur | `[ins-2606][gmp-2611]` | NO `offsetParent`; localizar por `type="number"` + height/width>0 |
| Badge contador ítems | `app-pedido .contadorProductos` (conteo) + indicador `[color="success"]` (verde) en el ítem | `[gmp-2611]` | aparece tras cargar la cantidad |
| Comentario (Tab General) | `ion-input#txComment` | `[ins-2610][dth-2612][jerez-2026-07-06]` | orden inputs General: `#clienteSelect`(0), Nº Orden(1), Responsable(2), `#txComment`(3); bajo el fold (scroll). `fillIonInput` (reactive form, NO ngModel). Round-trip §9: sobrevive Guardar→reabrir `[jerez-2026-07-06]` |
| Match "Ref 0" en texto de lista | tras `replace(/\s+/g,' ')` el texto queda `Ref.: 0Cliente:` (el 0 pegado a "C") → `\b` NO funciona; usar `/Ref\.: 0[^0-9]/` | `[jerez-2026-07-06]` | localizar el ítem Guardado (Ref 0) en `app-pedidos-lista` sin confundir con Ref 10/20… |
| Trash ítem (Tab Total) | dentro del acordeón del ítem (`ion-item` "Código: NNN", colapsado) → `ion-button[color="danger"]` + `ion-icon[name="trash"]` | `[ins-2610][hidroponias-20260710]` | expandir acordeón primero; borrado directo sin confirmación. ⚠ **build refactorizado El Yaque (ferrenuestro):** el trash dentro del `ion-accordion` del ítem NO recalcula con `mouse.click` ni Pointer down/up (DM-PED-026 BLOCKED) — difiere de insumar donde funcionó. ⚠ **ACTUALIZACIÓN:** hidroponias La Tortuga v6.6.18 SÍ recalcula con `mouse.click` (DM-PED-026 PASS), **contradice ferrenuestro** del mismo build — investigar divergencia. ⚠ **3ª confirmación:** dm-electronica (DM-PED-026 522→316 PASS) y **latino_cosmetica** (La Tortuga, DM-PED-026 Total Items 0 PASS con `pg.mouse.click`) → el trash-recalcula-con-mouse.click queda confirmado en 3 corridas (hidroponias+dm-electronica+latino_cosmetica); la falla de ferrenuestro es específica de ese device/estado, NO del build. ✅ **CONFIRMADO en ferrenuestro mismo:** al migrar a La Tortuga (window.ng=TRUE), DM-PED-026 pasó a **PASS** ($200,45→$0,00 con `pg.mouse.click`) — la falla de julio era el estado `window.ng=false` del device, ya resuelta. `[ferrenuestro-2026-07-07][hidroponias-20260710][dm-electronica-20260713][latino_cosmetica-20260714][ferrenuestro-20260723]` |
| Searchbar lista | `ion-searchbar` "Pedidos..." | `[gmp-2606][ins-2606]` | filtra realtime (ionInput) |
| Trash en lista (`app-pedidos-lista`) | `ion-button[color="danger"]` **estrecho (w≈29)** en el lado derecho del `ion-item` | `[gmp-2611][ins-2622]` | usar coords frescas exactas del botón danger; click en zona baja del item cae fuera del botón y navega al form. Borrado pide confirmación "¿Seguro que quieres eliminar este pedido?" |
| Reabrir pedido Guardado Ref 0 vía CDP | a diferencia de COBROS (Ref 0 inestable), el pedido Guardado Ref 0 reabre su detalle con `pg.mouse.click(x, y*0.4 del item)` **sin** Pointer combinado | `[ins-2622]` | click en zona izquierda-centro del item evita el botón danger estrecho de la derecha |
| Back de `app-pedidos-lista` | `img.fechaAtras` → `closest('a')` con **`pg.mouse.click` real** (coords ≈35,54), NO `dispatchEvent` | `[gmp-2611]` | `h.clickBack` (dispatchEvent) NO navega en pedidosLista |
| Guardar / Enviar | `.imagenGuardar` / `.imagenEnviar` | `[gmp-2606]` | disabled sin ítems |
| Selector Sucursal (Tab General) | `div#inputAddress > div.listado-tps-selector.inp-write2` — custom (NO ion-select estándar); disabled (`ng-reflect-is-disabled=true`) si `listaDirecciones=[]`; se auto-selecciona con 1 opción | `[prc-2606]` | # candidato — confirmar en otras playas. Tabs Pedido/Total/Adjunto permanecen disabled hasta que cliente tiene sucursal válida |
| Selección de cliente por componente | `ng.getComponent(document.querySelector('app-pedido')).setClientfromSelector(cli)`, lista en `comp.selectorCliente.clientes` | `[latino_cosmetica-20260729]` | ⚠ El handler vive en **`app-pedido`**, NO en un hijo `app-pedido-general` (a diferencia de cobros, donde es `app-cobro-general`). En form fresco la lista ya trae los 50 clientes **sin abrir el modal**. ⚠ **CONTRADICE `_comunes.md` `[el_valle-20260728]`**: acá `setClientfromSelector` **SÍ dispara** el alert de deuda vencida |
| Aceptar alert de deuda vencida | bucle de hasta 3 intentos sobre `ion-alert:not(.overlay-hidden) button` + `offsetParent!==null`, ~2s entre intentos | `[latino_cosmetica-20260729]` | ⚠ el 1.er click puede **no tomar** y dejar el form vacío con el modal de clientes abierto. Tras el bucle, si queda `ion-modal.show-modal` → `await modal.dismiss(null,'cancel')`. Verificar `#clienteSelect` ≠ "Seleccione Cliente" antes de seguir |
| ⚠ Modal de cliente reusa `ion-item.listaItems` | el modal que auto-abre al entrar a PEDIDO lista los clientes con **la misma clase** que las categorías del Tab Pedido | `[latino_cosmetica-20260729]` | buscar "BELOTTI 74" en `ion-item.listaItems` devuelve `null`/basura si el modal sigue abierto → comprobar `modals===0` antes de localizar categorías |
| Cantidad producto — selector estable | sellar `id` propio sobre el input del acordeón: `document.querySelector('ion-accordion[value="3058"] ion-input[type=number]').id='qa-cant-3058'` y luego `__qaH.fillIonInput('#qa-cant-3058', n)` | `[latino_cosmetica-20260729]` | evita índices frágiles; el input debe tener `height>0` (producto expandido) antes de sellar |
| **Entrada al módulo: `ion-button.colorBorderBuscar`, NO `p.nombreModulos`** | en `app-pedidos` los 3 accesos son `<ion-button class="colorBorderBuscar">` con textContent `PEDIDO`/`BUSCAR`/`COPIAR`. Localizar por texto + `mouse.click` al centro del rect | `[gmp-20260730]` | ⚠ la convención de `_comunes.md` (`p.nombreModulos`→`closest('a')`) es de **`app-home`** y **falla dentro del módulo** (`.closest` de undefined). ⚠ **filtrar por `getBoundingClientRect().height>0`, NO por `offsetParent!==null`**: tras volver del form los `ion-button` quedan con `offsetParent=null` un instante y el filtro los descarta |
| ⚠ **`setClientfromSelector` NO prende en globalmp La Tortuga v1.0** | pese a `window.ng=true`, `comp.setClientfromSelector(cli)` y `sc.selectClient(cli)+sc.sendClient()` corren **sin lanzar** pero dejan `hasClient=false`, `#clienteSelect`="Seleccione Cliente" y las tabs bloqueadas | `[gmp-20260730]` | ⚠ **CONTRADICE `[latino_cosmetica-20260729]`** (donde sí operaba). **Vía que funciona:** `mouse.click` en `#clienteSelect` → esperar `ion-modal.show-modal` → localizar el `ion-item` que contiene el **código** del cliente → `scrollIntoView({block:'center'})` → `mouse.click` al **35 % del ancho y 35 % del alto**. Tras aceptar el alert queda un `ion-modal.show-modal` **residual** → `await modal.dismiss(null,'cancel')` antes de ir a Tab Pedido |
| Modal de cliente: sin searchbar, 50 items, alfabético | cada `ion-item` = `Nombre · Código: XXNN · Saldo USD: N.NNN,NN · Más detalles`; el `Saldo USD` **coincide 1:1 con `cliente.saldo1`** (etiqueta correcta, no reproduce el cruce BS/USD del listado de CLIENTES) | `[gmp-20260730][ferrenuestro-20260723]` | con clientes al inicio del alfabeto el item queda arriba y el click es fiable sin scroll |
| Sellar `id` en el input de cantidad **por descarte** | tras expandir un producto, el **único** `ion-input[type=number]` con `height>0` que **aún no tenga id sellado** es el del producto recién abierto → `el.id='qa-cant-<COD>'` + `__qaH.fillIonInput('#qa-cant-<COD>', n)` | `[gmp-20260730][latino_cosmetica-20260729]` | generaliza el patrón de latino a builds SIN `ion-accordion[value]`; soporta varios productos abiertos a la vez |
| Espera tabs post-cliente (pedidos) | tras seleccionar cliente con sucursales, esperar ≈2s antes de verificar estado de tabs | `[prc-2606][jerez-2026-07-06]` | Angular carga `listaDirecciones` asíncronamente; no marcar FAIL en snapshot inmediato. También al **reabrir** un Guardado Ref 0: tabs Pedido/Total/Adjunto salen `disabled` en el snapshot inmediato y se habilitan ~2s después `[jerez-2026-07-06]` |
| 🔴 **Modal de cliente PAGINADO — 50 de N; cargar el resto con `onIonInfinite`** | el modal lista solo los **primeros 50** clientes aunque el vendedor tenga más (alipascua: 64). Buscar el `ion-item` por código devuelve **`null` sin más señal** y se lee como "el cliente no existe". Fix: `const sc = ng.getComponent(document.querySelector('app-pedido')).selectorCliente; await sc.onIonInfinite({target:{complete(){}}})` en bucle (≤3) hasta `sc.scrollDisable===true`; luego `scrollIntoView({block:'center'})` + `mouse.click` al **35 % ancho / 35 % alto** del ítem | `[alipascua-20260804]` | el service expone `page`, `scrollDisable`, `clientes[]`, `searchClients()`. Sin searchbar en el modal (confirma ferrenuestro). El cliente objetivo quedó en el índice **63** (última página) |
| 🔴 **Panel del producto expandido = mapa de VGs de línea (5 `ion-select` en orden fijo)** | al expandir un producto aparecen `Lista de Precio` · `Unidad` · `IVA` · `% Descuento` · `Almacén`. Leer `sel.disabled` de los 5 resuelve de una pasada `userCanChangePriceList`, `userCanSelectIVA`, `userCanSelectProductDiscount`, `userCanChangeWarehouse` **sin provocar el comportamiento** | `[alipascua-20260804]` | ⚠ quedan **bajo el fold** (y≈568-892 con viewport 744) → `scrollIntoView` obligatorio antes de clickear. En alipascua: Lista de Precio `disabled`(1 opción), Unidad/IVA/Descuento habilitados, Almacén `disabled` |
| Selector de descuento por línea → popover con `ion-radio-group` | `mouse.click` en el `ion-select` "% Descuento" abre un `ion-popover` **sin `button`s directos**: las opciones son `ion-item`/`ion-radio` dentro de `ion-select-popover > ion-list > ion-radio-group`. `querySelectorAll('button')` sobre el popover devuelve `[]` y se lee como "no abrió" | `[alipascua-20260804]` | clickear el `ion-item` por texto exacto. El descuento **se reinicia al valor por defecto si se colapsa y re-expande** el acordeón del producto |
| 🔴 **`stock0=false` se aplica IMPIDIENDO la expansión, no con alerta** | un producto con `Inventario: 0` **no abre su panel** al clickearlo (ningún `ion-input[type=number]` pasa a `height>0`) y **no dispara ningún `ion-alert`** | `[alipascua-20260804]` | ⚠ **contrasta con `[dth-2612]`** (donde salía `ion-alert#alertNB "Este producto no tiene inventario"`). Antes de concluir "el ítem no responde", **mirar el Inventario del producto** — se pierden intentos diagnosticando un no-defecto |
| Trash del Tab Total **bajo el fold** | el `ion-button[color=danger]` dentro del `ion-accordion` del ítem sale en y≈756 con viewport 744 ⇒ el click no llega y no hay error | `[alipascua-20260804][gmp-20260730]` | `b.scrollIntoView({block:'center'})` + **re-leer el rect** antes de `mouse.click`. Con eso DM-PED-026 recalculó al 1er intento |
| Selector **Proveedor / Línea** (estructura de producto) | el primer `ion-select` visible del Tab Pedido conmuta la agrupación: `Proveedor` (`coTypeProductStructure=001`, default) / `Línea` (`002`) | `[alipascua-20260804]` | es el que define qué categorías se listan — resuelve `estructura_producto: TBD` en el YAML sin adivinar |
| 🔴🔴 **El Tab Pedido filtra por el par `(lista de precios activa × MONEDA del pedido)` — la moneda puede vaciar el catálogo ENTERO** | antes de diagnosticar "No hay productos disponibles" como falta de datos o defecto de estructuras, **cambiar la moneda del pedido y volver a mirar**. Consultar `price_list` **acotado a la lista activa** (`comp.listaAnterior.coList`) **y agrupado por `co_currency`** | `[el_palmar-20260805]` | 🔴 **universal candidato — aplica a CUALQUIER cliente cuya lista activa no tenga precios en la moneda por defecto.** Medido en el_palmar: lista **Z12** con **13 filas / 7 productos en USD y 0 en VES**, y el selector de moneda **llega por defecto en VES** ⇒ **0 productos en todo el árbol**; con USD `Azucar` lista **6**. ⚠ **El agregado por empresa ENGAÑA**: la empresa 1002 "tenía 1 producto en VES", pero en la lista Z12 tenía **0**. 🔴 **ACOTADO por `[difranca-20260807]`: cambiar la moneda NO vacía el árbol por sí solo — la app CONVIERTE.** Con el pedido en **BSD** (la moneda local, que es el default), el Tab Pedido listó las 7 categorías y los 50 productos de BBK **con el precio convertido** (`ACBA300U: 2.737,61 BSD` = 3,64 × 752,09). ⇒ el vaciado de el_palmar **no es una consecuencia general de elegir la moneda local**: pasa solo cuando la lista activa **no tiene precios en ninguna moneda convertible** para ese caso. **Antes de cambiar la moneda "por precaución", mirar si el árbol está realmente vacío** — en difranca habría sido un cambio innecesario |
| **Selector de MONEDA = 2.º `ion-select` del Tab General, sin `id` ni `label`** | localizarlo por sus `ion-select-option` (`VES`/`USD`), **nunca por índice fijo**. Estado en `comp.monedaSeleccionada` (`coCurrency`, `localCurrency`, `hardCurrency`) | `[el_palmar-20260805]` | cambiarlo **no** dispara alerta ni recarga el form: `#tasa` y `comp.listaAnterior` quedan intactos ⇒ sirve para un **A/B limpio** del filtro de catálogo |
| 🔴 **`ion-input#nuPurchase` (Nro. de orden) puede ser el que BLOQUEA las tabs** | con `validateNuOrder` efectivo llega `required=true` y mantiene **`comp.lockSegments=true`**: las tabs Pedido/Total/Adjunto siguen `disabled` **aunque el cliente ya esté seleccionado**. Al llenarlo, las 3 habilitan en el mismo tick | `[el_palmar-20260805]` | se lee como "DM-PED-006 FALLÓ: las tabs no habilitan". **Antes de marcar FAIL, leer `comp.lockSegments` y el `required` de los inputs del Tab General.** Hermano exacto del `idEnterprise` de CLIENTES (ver regla general en `_comunes.md`) |
| 🔴 **`ion-input#tasa` engaña con `disabled=false`: hay que leer `readonly`** | el campo de tasa del Tab General llega `disabled=false` pero **`readonly=true`** ⇒ NO es editable | `[el_palmar-20260805]` | concluir "la tasa se puede editar" mirando solo `disabled` da un veredicto de VG equivocado (`enabledManualRate`/`canChangeRate`). **Leer siempre el par `(disabled, readonly)`** |
| **Orden de los `ion-input` del Tab General (v1.0/db19)** | `#clienteSelect`(0) · **`#tasa`(1)** · `#nuPurchase`(2) · `#naResponsible`(3) · `#txComment`(4) | `[el_palmar-20260805]` | ⚠ difiere de `[ins-2610]` (que no tenía `#tasa`) ⇒ **no indexar por posición, usar el `id`** |
| **Los 7 `ion-select` del Tab General = mapa de VGs de CABECERA** | en orden: Empresa · **Moneda** · Dirección/Sucursal · Tipo de pedido · Canal · **Lista de precios** · Condición de pago. Leer `sel.disabled` + nº de opciones de los 7 | `[el_palmar-20260805]` | resuelve de UNA pasada `orderEnterpriseEnabled`, `multiCurrency`, `selectOrderType`, `userCanChangePriceList` y `userCanChangePaymentConditions` **sin provocar el comportamiento**. Complementa el mapa de 5 selects del **panel de producto** de `[alipascua-20260804]` |
| **Buscador de productos OCULTO tras `ion-icon[name="search-circle-sharp"]`** | el Tab Pedido **no** trae `ion-searchbar`: el buscador es un `input.search-input.inputsSearch` (placeholder "Búsqueda de productos") que aparece al clickear el ícono | `[el_palmar-20260805]` | `querySelectorAll('ion-searchbar')` devuelve `[]` y se lee como "no hay buscador". Volver de nivel con `ion-icon[name="arrow-back-outline"]` |
| **Lista de precios activa en `comp.listaAnterior`** | `ng.getComponent(document.querySelector('app-pedido')).listaAnterior` → `{idList, coList, naList, coEnterprise}` | `[el_palmar-20260805]` | junto con `tipoOrden`, `distChannel`, `empresaSeleccionada`, `monedaSeleccionada` da el **contexto completo de cabecera sin tocar el DOM** |
| **`order_type_product_structure` = el filtro de catálogo real de PEDIDOS** | mapea `id_order_type` → `id_product_structure` habilitadas, **por empresa**. El tipo por defecto sale de `order_types.default_value=1` | `[el_palmar-20260805]` | **tabla a inspeccionar siempre que PEDIDOS liste 0 productos.** Si sus filas apuntan solo a estructuras de **nivel 1**, el Tab Pedido muestra categorías con contador y ningún producto |
| **3.ª variante de árbol de productos: DRILL-DOWN (build Isla Coche v1.0/db19)** | click en `ion-item.listaItems` **navega DENTRO** de la categoría (el header pasa a su nombre y `listaItems` queda en 0), no expande in situ. Volver con `ion-icon[name="arrow-back-outline"]`. Dentro, los productos **sí** son `ion-accordion` | `[el_palmar-20260805][difranca-20260807]` | ⚠ un 2.º click sobre la misma categoría **no la colapsa**: revienta con `Cannot read properties of undefined`. Convive con las 2 variantes ya documentadas (`div.listaProductos` anidado / `ion-accordion-group`) ⇒ **son 3 — detectar antes de elegir estrategia**. ✅ **2.ª confirmación `[difranca-20260807]` (El Yaque, otro servidor): `ion-accordion-group=0` y `ion-accordion=0` en el nivel de categorías; el click en `ion-item.listaItems` navega DENTRO (las `listaItems` pasan a 0) y allí los productos SÍ son `ion-accordion`** ⇒ la variante **sigue al BUILD v1.0/db19, no al servidor** |
| **El panel de línea puede traer 3 selects, no 5** | la **ausencia** de "% Descuento" / "Almacén" **es** la señal de `userCanSelectProductDiscount=false` / `userCanChangeWarehouse=false` | `[el_palmar-20260805]` | leer los selects **presentes**, no asumir los 5 de `[alipascua-20260804]` |
| 🔴🔴 **`onIonInfinite` pagina el MODELO pero NO re-renderiza el DOM — falta `ng.applyChanges(sc)`** | `sc.onIonInfinite(...)` llevó `sc.clientes` de 50→100 pero `document.querySelectorAll('ion-modal.show-modal ion-item')` **siguió en 50** y el ítem objetivo devolvía `null`. Causa: `pg.evaluate` corre **fuera de NgZone** ⇒ Angular no detecta el cambio. **Fix: `window.ng.applyChanges(sc)` + esperar ~1,2 s → el DOM pasa a 100 ítems al instante** | `[difranca-20260807]` | 🔴 **COMPLETA la receta de `[alipascua-20260804]`, que estaba incompleta**: paginar sin `applyChanges` produce exactamente el falso negativo "el cliente no existe". **Agregar SIEMPRE `ng.applyChanges` tras paginar programáticamente**, en cualquier modal virtualizado |
| **Selector de EMPRESA de PEDIDOS: `ion-select` SIN `formcontrolname`, `value` = OBJETO empresa completo** | `disabled=false`, `ng-valid`, `value` = `{idEnterprise, coEnterprise, lbEnterprise, coCurrencyDefault, prioritySelection, enterpriseDefault, naEnterprise, nuRif, txAddress}` | `[difranca-20260807]` | **AMPLÍA la tabla de 4 variantes de `[el_palmar-20260805]`**: en el_palmar PEDIDOS traía `formcontrolname="idEnterprise"` con `value` numérico; en difranca **no tiene `formcontrolname`** y el `value` es el objeto ⇒ la receta `s.value=<number>` **rompería** este form. Confirma la regla: **leer `formcontrolname` + `disabled` + `value` + `ng-invalid` ANTES de decidir** |
| 🔴 **Reabrir un Guardado: las tabs y `#txComment` tardan >4 s en rehidratarse** | snapshot inmediato **y también a los 4 s**: tabs `disabled` y **`#txComment` vacío**; **a los ~9 s** todo rehidratado | `[difranca-20260807]` | se lee como "el comentario no persistió" → falso FAIL de round-trip §9. **Amplía la espera de `[jerez-2026-07-06]`: aplica también a los VALORES de los inputs, no solo al estado de las tabs.** Medir a ~9 s antes de concluir |
| **Fuente fiable del nº de líneas = `orderServ.carrito.length`** | `ng.getComponent(document.querySelector('app-pedido')).orderServ.carrito.length` | `[difranca-20260807]` | ⚠ **`.contadorProductos` NO cuenta líneas**: con 10 líneas cargadas seguía marcando `"1"` — es el badge del producto, no del pedido. Usar `orderServ.carrito` para cualquier oráculo de tamaño del pedido |
| **Componente de la lista de productos = `productos-tab-order-product-list`** | expone `page`, `scrollDisable`, `productList`, `onIonInfinite`, `onProductQuantityChange`, `refreshRemainingStock` | `[difranca-20260807]` | es **el que hay que paginar** (+`ng.applyChanges`), no `app-pedido` |
| **Agregar una línea sin abrir modales (55/55 sin un fallo)** | `scrollIntoView({block:'center'})` → 300 ms → **re-leer rect** → `mouse.click` (expande el panel) → localizar el `ion-input` por **`label === 'Cantidad:'`** → set nativo + `ionInput`/`ionChange`/`ionBlur` | `[difranca-20260807]` | receta de montaje masivo validada en un pedido de **55 líneas**; localizar por `label` es más estable que por índice o por `type=number` |
| ⚠ **El `textContent` del ítem de producto NO separa el código de «Precio»** | se lee `Código: ACPDT300Precio: 43.681,39 BSD`. **Parser correcto: `/Código:\s*([A-Za-z0-9-]+?)\s*Precio/` + igualdad exacta del grupo capturado** | `[difranca-20260807]` | 🔴 un lookahead `(?![0-9A-Za-z])` tras el código **falla en el 100 % de los casos** (la «P» de *Precio* lo rechaza) y se lee como "el producto no está en la lista" — costó un tramo entero de falsos `no-item`. Es la **contracara** de la nota de DEVOLUCIONES (donde anclar el final parecía la solución): **capturar hasta la siguiente etiqueta sirve en los dos módulos** |

### Flujo mínimo probado
```
1. Click PEDIDO → tabs disabled salvo General
2. Seleccionar cliente (mouse.click real, no dispatchEvent) → alerta deuda vencida (si aplica) → ACEPTAR
3. Tab Pedido → click categoría → click producto → fillIonInput cantidad → badge contadorProductos
4. Tab Total: totales; trash dentro de acordeón = borrado directo
5. Guardar (Nro.Ref:0) → reabrir desde lista → Enviar
```

### VG → DOM effects
| VG | true | false |
|----|------|-------|
| `multiCurrency` | Tab Total muestra Bs. + USD | Tab Total solo USD `[gmp-2606]` |
| `enterpriseEnabled` | ion-select Empresa obligatorio | sin selector |

### Anti-patrones confirmados
- `ion-item.click()` / dispatchEvent NO navega en `pedidosLista` — usar `pg.mouse.click(coords)`. `[gmp-2606]`
- 🔴🔴 **DOS HIPÓTESIS REFUTADAS sobre "No hay productos disponibles" — NO volver a promoverlas.** El reporte `smoke_el_palmar_20260805_133539/pedidos.md` las contiene en su cuerpo (con banner de corrección y fila tachada); quedan aquí registradas para que **ninguna consolidación futura las levante como regla vigente**:
  1. ❌ ~~`unit_pricelist` vacía ⇒ "No hay productos disponibles"~~ — **FALSO.** La app **no lee esa tabla** cuando `product-list.unitByPriceList === false`: PRODUCTOS listó, cotizó y convirtió los 57 productos con `unit_pricelist` en **0 filas** en device **y** nube. ⇒ **Pedir la carga de esa tabla al equipo de datos sería trabajo perdido. Leer la bandera del componente primero.**
  2. ❌ ~~"PEDIDOS no expande el subárbol de estructuras"~~ — **FALSO.** Con moneda **USD**, `Azucar` **sí** lista sus productos. (El badge de la categoría **sigue** siendo un rollup del subárbol y **no** refleja el filtro de precio/moneda — `Azucar 8` lista 6 —, pero eso es cosmético, no la causa del vacío.)
  ✅ **La causa REAL, verificada en UI, es el filtro `(lista de precios activa × moneda del pedido)`** — ver la fila 🔴🔴 de *Selectores probados*. `[el_palmar-20260805]`

### Notas por cliente
- Texto envío: globalmp dice "Su Pedido será enviado"; insumar/romher confirman secuencia de 2-3 alertas hasta "Pedido nro. X enviado exitosamente". `[gmp-2606][ins-2606][rom-2606][ins-2610]`
- **st_order piercar usa código 1 para Enviado** (en vez de 4); pedidos anteriores muestran `st_order=6`. Tabla de estados de piercar difiere — confirmar en próxima corrida. Reconfirmado en ferrenuestro (Isla Coche): pedido enviado `id_order=28458` con `st_order=1`=Enviado. `[prc-2606][ferrenuestro-2026-07-07]`
- **ferrenuestro (Isla Coche): botón "Pedido Sugerido" NO aparece en Tab Pedido** pese a `suggestedOrder=true` — misma divergencia UI-vs-config que jerez. Posiblemente depende de `suggestedOrderByDispatchAndReturn` (=false en ferrenuestro). ⚠ VERIFICAR con desarrollo antes de tocar la VG. `[jerez-2026-07-06][ferrenuestro-2026-07-07]`
- **Cliente sin sucursales (`listaDirecciones=[]`) deja tabs bloqueadas** en piercar: ej. "7 CARS". La sucursal se auto-selecciona si hay exactamente 1 entrada. Patrón consistente con INVENTARIOS y VISITAS (piercar). `[prc-2606]`
- Borrado: en **Tab Total** directo sin confirmación; desde **lista** con alert "Pedidos / ¿Seguro que quieres eliminar este pedido?" Cancelar/Aceptar. `[gmp-2606][ins-2606][ins-2610]`
- Dirty-guard atrás SÍ funciona vía CDP en insumar Y globalmp: form sucio + `img.fechaAtras`+`mouse.click` real (≈35,54) dispara modal "¡Alerta!" (Guardar y salir / Salir sin guardar / Cancelar). ⚠ Contradice la nota `[gmp-2606]` de COBROS (que requería hardware back en globalmp): **el dirty-guard vía CDP es por MÓDULO, no por cliente global** — en globalmp dispara en PEDIDOS/DEVOLUCIONES/DEPÓSITOS/VISITAS; solo COBROS quedó intermitente. En insumar dispara al **primer intento** sin Pointer combinado. `[ins-2610][gmp-2611][ins-2622]`
- ⚠ **Guardar con `.imagenGuardar` (alert "Pedido Guardado") deja el form *pristine* → atrás sale directo SIN modal dirty-guard.** El dirty-guard solo dispara cuando el form tiene ítems agregados/editados **sin** guardar (DM-PED-032). Matiza la nota de reapertura "reabrir con ítems marca dirty": eso aplica al **reabrir** un Guardado (la rehidratación ensucia), no al recién Guardar. `[gmp-2611][ins-2622][jerez-2026-07-06]` (jerez: modal "¡Alerta!" 3 opciones al 1er intento con `img.fechaAtras`+`mouse.click` real; Guardar deja pristine → atrás directo)
- **Reabrir un pedido Guardado con ítems marca el form dirty:** pulsar atrás **sin editar** SÍ dispara el modal "¡Alerta!" (la rehidratación de ítems ensucia el form). No es FAIL — comportamiento defensivo. Matiza DM-PED-032 ("reabrir sin editar → salida directa"). Confirmado también en don-theo (el back requirió `PointerEvent(pointerdown/up)+MouseEvent(click)` combinado sobre el `<a>` para disparar el dirty-guard tras reabrir). `[gmp-2611][dth-2612]`
- "Guardar y salir" (opción del dirty-guard) guarda **silenciosamente**: navega directo a `/pedidos` sin alert "Pedido Guardado"; el pedido queda Guardado (Ref 0) verificable en BUSCAR. Confirma don-theo e insumar. `[gmp-2611][dth-2612][ins-2622]`
- **Toggle de categoría se colapsa al cambiar de tab:** al volver a Tab Pedido desde otra tab, la categoría expandida se colapsa; re-expandir requiere click limpio (clicks consecutivos alternan abrir/cerrar — evitar doble click). `[dth-2612][ferrenuestro-2026-07-07]`
- Tab Total insumar muestra **solo US$ pese a `multiCurrency=true`** (sin Bs.) — comportamiento del módulo, no FAIL. `[ins-2610]`
- Defecto abierto DM-PED-032 (romher): modal "salir con ítems" no aparece si form pristine. `[rom-2606]`
- **jerez: botón "Pedido Sugerido" NO aparece en Tab Pedido** pese a `suggestedOrder=true` (la divergencia UI-vs-config `suggestedOrderByDispatchAndReturn` se manifiesta en INVENTARIOS, no aquí). Tab Total solo USD (`multiCurrencyOrder=false`). Tras Enviar, el pedido quedó **"Por Enviar"** (H1 no-persistencia El Yaque), sin transitar a "Enviado"/Ref real en ~10s — sin cotejo BD no se afirma persistencia. `[jerez-2026-07-06]`
- ✅ **[CONTRADICE jerez-2026-07-06] hidroponias Isla La Tortuga v6.6.18:** pedido Nro. 50 Enviado **SÍ persistió a nube íntegro** (id_order=50, BD-OK). **Resuelve H1 de jerez:** la no-persistencia era específica de El Yaque jerez (posible estado de BD pre-corrida), no del build v6.6.18 global. La Tortuga la v6.6.18 envíos funcionan OK. `[hidroponias-20260710]`
- ✅ **[CONFIRMA hidroponias / CONTRADICE jerez] dm-electronica El Yaque DM ELECTRONIC v6.6.18:** pedido Nro. 4 Enviado **SÍ persistió a nube íntegro** (id_order=4, st_order=1=Enviado, BD-FIELD-OK 37/37 + order_detail + order_detail_unit). El Yaque DM ELECTRONIC envía OK y con sync INMEDIATA — refuerza que la no-persistencia de jerez era local a esa BD. `window.ng=TRUE` en este servidor (contrasta jerez/ferrenuestro). `[dm-electronica-20260713]`
- **dm-electronica: botón "Pedido Sugerido" NO aparece en Tab Pedido** pese a `suggestedOrder=true` (misma divergencia UI-vs-config que jerez/ferrenuestro, ligada a `suggestedOrderByDispatchAndReturn=false`). Tab Total solo US$ (`multiCurrencyOrder=false` pese a `multiCurrency=true`, como insumar/jerez). Al seleccionar cliente con deuda vencida dispara modal "Pedidos / Este cliente tiene deuda vencida, ¿Desea continuar con el pedido?" (Cancelar/Aceptar) — no bloquea. NO FAIL. `[dm-electronica-20260713]`
- ✅ **[CONFIRMA hidroponias/dm-electronica] latino_cosmetica La Tortuga v6.6.18: pedido Nro. 34 Enviado SÍ persistió a nube íntegro** (id_order=34, st_order=1=Enviado, BD-FIELD-OK 12/12 + order_detail + order_detail_unit; hook `nativePromise` capturó 2 POST idempotentes deduplicados). `window.ng=TRUE`. Botón "Pedido Sugerido" NO aparece (`suggestedOrder`), Tab Total solo US$ (`multiCurrencyOrder=false`). Cliente ANNELI CA (13) dispara modal "Este cliente tiene deuda vencida, ¿Desea continuar?" (Aceptar continúa, no bloquea). `[latino_cosmetica-20260714]`
- **ferrenuestro (La Tortuga v6.6.18, window.ng=TRUE): pedido Nro. 29358 Enviado** (id_order sin PK en payload, st_order=1=Enviado, TORNICAGUA 504, TALADRO 080178 ×2, Total $200,45; hook capturó 2 POST `orderservice/order` idempotentes deduplicados; cotejo BD N/A por DSN Isla Coche viejo → BD-N/A payload). ⚠ **El modal de selección de cliente (`ion-modal.show-modal`) lista ~50 `ion-item` SIN `ion-searchbar`** → localizar por texto + `scrollIntoView({block:'center'})` + `mouse.click` con coords frescas (el filtrado realtime SÍ existe en la lista BUSCAR `app-pedidos-lista`, no en el modal de alta; refina la nota devoluciones jerez/ferrenuestro). **Secuencia de envío = 3 alerts:** "¿Desea Enviar el pedido?" (Cancelar/Aceptar) → "Su Pedido será enviado" (OK) → "Pedido nro. X enviado exitosamente" (OK). ⚠ **producto se consume del catálogo:** tras enviar 2 uds del TALADRO 080178, la categoría HERRAMIENTAS ELECTRICAS pasó de 17→16 y 080178 dejó de renderizarse (posible filtro por inventario=0, coherente con `stock0=false`/`hideStock0=true`); para un 2º pedido usar otro producto de la misma categoría. ⚠ VERIFICAR con desarrollo si el filtro por stock es esperado. `[ferrenuestro-20260723]`
- ✅ **latino_cosmetica servidor ISLA COCHE (app_version `1.0`, db_version 19, `window.ng=TRUE`) — pedido Nro. 123 Enviado, 14/14 PASS.** Cliente ANNELI CA (co 13 / id_client 34), 3058 BELOTTI ACOND CEBOLLA ×2 UND, Total $12,14 (Base 10,47 + IVA 1,67). **BD local confirma `id_order=123`, `st_order=1`, `st_delivery=1`, `pending_transactions=0`, `failed_transactions=0`, sin duplicados** → reconfirma **Nro.Ref UI = `id_order`**. Hook `nativePromise` capturó **1 solo POST** `orderservice/order` (sin duplicación, a diferencia de los 2 idempotentes de ferrenuestro/latino-julio). ⚠ Nube **BD-N/A** (sin GRANT). Botón "Pedido Sugerido" ausente (`suggestedOrder=false`), Tab Total solo US$ (`multiCurrencyOrder=false`), `ion-select` de Empresa presente pero **`disabled`** con el objeto empresa preasignado (`orderEnterpriseEnabled=false`). Secuencia de envío = 3 alerts: "¿Desea Enviar el pedido?" (Cancelar/**Aceptar**) → "Su Pedido será enviado" (**OK**) → "Pedido nro. X enviado exitosamente" (**OK**); Guardar = "Pedido Guardado" (**OK**). `[latino_cosmetica-20260729]`
- **Precisión numérica del Tab Total (no es defecto):** con 2 líneas la UI mostró `Total Base $ 15,16` mientras la suma de los precios **mostrados** (10,46+4,69) da 15,15. El payload aclara que el precio unitario real es `nuPriceBase=5.2328` — la app **calcula con precisión completa y redondea solo al presentar**. No levantar como descuadre; verificar contra el payload antes de reportar diferencias de 1 céntimo. `[latino_cosmetica-20260729]`
- ✅ **globalmp servidor LA TORTUGA (app_version `1.0`, db_version 19, `window.ng=TRUE`) — pedido Nro. 15167 Enviado, 14/14 PASS, 0 FAIL.** Cliente AS04 ABASTO EL SITIO DSG (id_client 742), PCE03 ×2 CJA, Total **USD 40,40** (base 40,40, IVA 0 %). BD local: `id_order=15167`, `st_order=1`, `st_delivery=1`, `pending_transactions=0`, `failed_transactions=0`, **359 filas / 359 `co_order` distintos** (sin duplicados) → reconfirma **Nro.Ref UI = `id_order`**. Hook `nativePromise` capturó **1 solo POST** `orderservice/order` con body completo (sin duplicación idempotente). Nube **BD-N/A** (sin GRANT). ⚠ **`enterpriseEnabled=true` + `orderTypeByEnterprise=true`: el `ion-select` de Empresa llega PRESELECCIONADO en 00002 y HABILITADO** (contrasta con latino_cosmetica, donde estaba `disabled`); `idOrderType=4` se resuelve solo, no hizo falta forzar nada. Secuencia de envío = **2 alerts** (no 3): "¿Desea Enviar el pedido?" (Cancelar/**Aceptar**) → "Su Pedido será enviado" (**OK**) → navega directo a `/pedidos` **sin** el "Pedido nro. X enviado exitosamente"; el envío igual fue exitoso (Ref confirmada en BD). `[gmp-20260730]`
- **⚠ `st_order=1` TAMBIÉN en pedidos GUARDADOS (no solo enviados):** en La Tortuga v1.0 un pedido recién Guardado nace con `st_order=1` e `id_order=0`. **El discriminador fiable guardado-vs-enviado es `st_delivery`** (3=guardado / 1=enviado) + `id_order` (0 vs PK del servidor). No juzgar el envío por `st_order` — refuerza el caveat de RUNTIME §10. `[gmp-20260730]`
- **Estructura del Tab Pedido = build VIEJO en globalmp (`ion-accordion-group` = 0):** confirma la variante `[gmp-2611]` (categoría `ion-item.listaItems` → click inyecta los productos como `ion-item` hermanos → click en el producto revela su `ion-input[type=number]`), **no** la variante `ion-accordion` del build refactorizado El Yaque. Detectar siempre con `querySelectorAll('ion-accordion-group').length` antes de elegir estrategia. En Tab **Total** los ítems SÍ son `ion-accordion` (3 nodos) con el trash `ion-button[color="danger"]` dentro — expandir el ítem primero; **borrado directo sin confirmación** y recálculo inmediato con `mouse.click`. `[gmp-20260730]`
- **Inventario mostrado se RESERVA en vivo:** al cargar 2 uds de PCE03 el "Inventario:" del mismo ítem pasó de **3,00 → 1,00** sin recargar. No es catálogo cambiante ni el filtro por stock de ferrenuestro. `[gmp-20260730]`
- **globalmp: botón "Pedido Sugerido" NO aparece en NINGUNA tab** pese a `suggestedOrderByDispatchAndReturn=true` en el perfil, y **no existe tab "Resumen"** (las tabs son exactamente General · Pedido · Total · Adjunto). Cuarta playa con la misma divergencia UI-vs-config (jerez, ferrenuestro, dm-electronica, globalmp) ⇒ ⚠ **verificar con desarrollo qué VG gobierna realmente ese botón**; el `vgs.suggestedOrderByDispatchAndReturn` del YAML globalmp debe corregirse. `[gmp-20260730]`
- ✅ **alipascua servidor EL YAQUE (app_version `1.0`, db_version 19, `window.ng=TRUE`) — pedido Nro. 4309 Enviado, 14/14 PASS, 0 FAIL.** Cliente RENZO FERNANDO MARTINEZ MEJIAS (`V28556138` / id_client 1744), `75992200000281` DESINFECTANTE AROMA CEREZA ×2 UNIDAD, Base US$ 3,5600 · desc. producto 5 % · desc. global 7 % · IVA 0 · **Total US$ 3,1453**. Nube **BD-OK** (`id_order=4309`, `st_order=1`, `nu_details=1`, det=1, units=1, sin duplicados) ⇒ **reconfirma Nro.Ref UI = `id_order`**. Hook `nativePromise` capturó **1 solo POST** `orderservice/order` con body completo. `ion-select` de Empresa **`disabled`** con ALIPASCUA preasignada (`enterpriseEnabled=false`). Tab Total **solo US$** pese a `multiCurrency=true`. Botón "Pedido Sugerido" **ausente** (6ª playa con la divergencia). **Secuencia de envío = 2 alerts** (como `[gmp-20260730]`): "¿Desea Enviar el pedido?" (Cancelar/**Aceptar**) → "Su Pedido será enviado" (**OK**) → navega a `/pedidos` **sin** el "Pedido nro. X enviado exitosamente". Guardar = "Pedido Guardado" (**OK**); borrado desde lista pide "¿Seguro que quieres eliminar este pedido?" (Cancelar/**Aceptar**). `[alipascua-20260804]`
- **🔎 Conversión de moneda en PEDIDOS: dirección CORRECTA (multiplica US$→BSD) — NO reproduce el bug de cobros.** Con tasa `nuValueLocal=746,6297`, todas las `*_conversion` verificadas con aritmética explícita: `nu_price_base` 1,78→1329,0009 · `nu_discount_total` 0,178→132,9001 · `nu_base_total` 3,56→2658,0017 · `nu_amount_final` 3,14526→2348,3445 · `nu_amount_global_discount` 0,23674→176,7571. ⚠ Las `*_conversion` se calculan sobre el valor **sin redondear** (3,14526, no 3,1453) → una diferencia de ~0,03 BSD contra el redondeado **no es descuadre**. `[alipascua-20260804]`
- **⚠ `requiredComment=true` NO bloquea Guardar/Enviar en El Yaque v1.0:** con cliente + 1 ítem y `#txComment` **vacío**, `.imagenGuardar`/`.imagenEnviar` ya salen habilitados. Además `#txComment` **no declara `maxlength`** pese a `longitudComentario=200`. Ninguno de los dos se marcó FAIL (un set programático de 250 chars saltea el límite del navegador y no distingue "no hay tope" de "tope solo al teclear") → **pendiente de verificación manual con teclado real**. `[alipascua-20260804]`
- **⚠ El botón PEDIDO puede no navegar al 1er click** volviendo de `/pedidosLista` a `/pedidos`, pese a `elementFromPoint` correcto sobre el `ion-button`; el 2º click sí navega. **Verificar `location.href==='http://localhost/pedido'` tras el click y reintentar UNA vez** antes de dar el flujo por fallido. `[alipascua-20260804]`
- **Inventario mostrado se RESERVA en vivo (2ª confirmación):** cargar 2 uds bajó "Inventario: 30 → 28" sin recargar; con 999 uds pasó a 0. Confirma `[gmp-20260730]`. `[alipascua-20260804]`
- **el_palmar (Isla Coche v1.0/db19, `window.ng=TRUE`, 2 empresas): `app-pedido` AUTO-ASIGNA empresa aunque CLIENTES no lo haga.** Mismo build, mismo cliente, misma corrida: en el form de **cliente potencial** el select llega vacío/`ng-invalid`; en el de **pedido** llega **preseleccionado en 1002 y habilitado**, con `enterpriseDefault:"false"` en el objeto ⇒ **no toma la `enterprise_default` de la BD local**. Acota la regla de `_comunes.md`: *la auto-asignación depende del FORMULARIO, no solo del nº de empresas.* `[el_palmar-20260805]`
- **Modal de cliente ordenado por DEUDA VENCIDA, no alfabético:** `selectorCliente.clientes` trae 50 de 144 asignados y el objetivo `1000000803` cayó en el **índice 3** ⇒ **0 rondas de `onIonInfinite`**. El orden es `oderByDueDateAndSaldo` (los morosos suben), no alfabético puro. **Paginar solo si `findIndex` falla** — no asumir que un cliente "de la V" está en la última página. `[el_palmar-20260805]`
- ✅ **Recuperar la vista virtualizada del Tab Pedido — GRADUADO de `# candidato` a CONFIRMADO:** si la categoría queda en estado inconsistente tras toggles múltiples (accordions huérfanos, `listaItems=0`), re-clic **General → Pedido** restaura las `ion-item.listaItems` colapsadas. **2.ª playa independiente**, validado **3 veces** en difranca (y usado para cambiar de familia Pasarela→BBK **conservando el carrito intacto**): es más fiable que el `arrow-back-outline`. **Cambiar de tab colapsa el árbol siempre.** `[ferrenuestro-20260723][difranca-20260807]`
- ✅ **difranca (El Yaque v1.0/db19, `window.ng=TRUE`, 3 empresas): PEDIDOS conducible de punta a punta; 8 alerts sin un reintento.**
  - 🔴 **`orderEnterpriseEnabled` EFECTIVO = `true` en difranca** — el Tab General **sí** trae selector de empresa y llega **habilitado** (no `disabled` como alipascua/latino_cosmetica). El perfil YAML lo declaraba `false`: **corregido**. ⇒ **leer el select, no el YAML**, antes de dar la VG por buena. `[difranca-20260807]`
  - **`enterpriseDefault:"true"` Y `prioritySelection:0` apuntando a la empresa CORRECTA (DDHP_A12)** ⇒ la trampa de `[el_palmar-20260805]` (donde el default local apuntaba a otra empresa) **no se activa** en difranca. Riesgo sigue siendo teórico, pero verificar igual. `[difranca-20260807]`
  - **Al reabrir un Guardado, Empresa y Moneda pasan a `disabled=true`** (también el cliente); en el alta llegan habilitados. **Comportamiento defensivo — NO es FAIL.** `[difranca-20260807]`
  - **Modal de cliente ORDENADO POR DEUDA con 2 páginas:** `selectorCliente.clientes` trae los **148** en el modelo aunque el DOM muestre 50; CAR755 cayó en el índice **52** ⇒ **1 sola ronda** de `onIonInfinite` (+ `ng.applyChanges`, ver *Selectores probados*). Cada ítem rotula `Saldo BSD:` **y** `Saldo US$:` y **ambos cuadran con la tasa** — **no reproduce** el cruce de etiquetas de `[gmp-20260730]`. `[difranca-20260807]`
  - ⚠ **El `selectorCliente` de PEDIDOS trae 296 clientes, contra 148 en DEVOLUCIONES** (misma corrida, mismo vendedor): **la cartera del selector de pedidos NO está filtrada por empresa** como la de devoluciones. **Dato a verificar en una corrida futura** — podría ser deliberado o ser el escenario de `WEB-LIMPIAR-CAMBIA-EMPRESA`. `[difranca-20260807]`
  - ✅ **`setClientfromSelector` SÍ opera en difranca** — `ng.getComponent(app-pedido).setClientfromSelector(cli)` + `applyChanges` seleccionó CAR755 **sin abrir el modal**, y dispara el alert de deuda vencida. **Confirma `[latino_cosmetica-20260729]` y contradice `[gmp-20260730]`** ⇒ probarlo primero y caer al click real en el modal solo si deja `hasClient=false`. `[difranca-20260807]`
  - 🔴 **Las etiquetas de alert NO son predecibles ni dentro de la misma corrida:** el ejercicio de carga las leyó en **MAYÚSCULAS** (`ACEPTAR`/`CANCELAR`/`OK`) y el pedido de 55 líneas, en el **mismo módulo y la misma corrida**, las leyó en **`Cancelar`/`Aceptar`/`OK`**. ⇒ **comparar SIEMPRE en minúsculas manteniendo la igualdad exacta**; una receta que compare contra `'Aceptar'` literal devuelve `null` y se lee como «el alert no tiene botones». Reparto medido: deuda vencida `[Cancelar, Aceptar]` · guardado `[OK]` · envío paso 1 `[Cancelar, Aceptar]`, pasos 2-3 `[OK]` · borrado `[Cancelar, Aceptar]` · dirty-guard `[Guardar y salir, Salir sin guardar, Cancelar]`. `[difranca-20260807]`
  - ⚠ **Hay 4 `img.fechaAtras` en el DOM y 3 tienen rect 0×0** — tomar «el primero» da coords `(0,0)` y el back no navega (se lee como cuelgue). **Filtrar por `getBoundingClientRect().width>0`**; el visible está en ≈(32,31). Reconfirma `[gmp-20260730]` (ver `_comunes.md`). `[difranca-20260807]`
  - **Tabla local de detalle: NO es `order_detail`** (`no such table` — los nombres locales van en **plural**). **`orders.nu_details` sirve como contador de líneas** sin abrir la tabla de detalle. `[difranca-20260807]`
  - **`ng.applyChanges` + namespace propio `__qaP` — 3.ª confirmación de la receta graduada:** se registró `window.__qaP` (rect · alertInfo · homeTile · modBtn · tab · arbol · cat · prods) **sin tocar `__qaH`**, consumiendo `__qaH.getPayloadData()` heredado del agente LOGIN ⇒ **1 solo POST capturado, con body, sin duplicados**. `[difranca-20260807]`
  - 📊 **RENDIMIENTO — el reporte de campo «los pedidos con más de 50 productos cuelgan la app» quedó REFUTADO.** Curva **plana** hasta **88 líneas** (sin degradación), y un pedido de **55 líneas se envió** con POST de ~6,4 s llegando **íntegro** a la nube. ⚠ Dato contraintuitivo para futuras mediciones: **`Guardar` (~4,9 s) es MÁS PESADO que `Enviar` (773 ms)** — el cuello de botella es **SQLite**, no la red. Y el «50» del reporte de campo **coincide con el tamaño de página del catálogo** (`quPageProduct=50`), **no** con un umbral del carrito ⇒ la hipótesis más probable es que el usuario viera la paginación del árbol, no un cuelgue. `[difranca-20260807]`

---
