# Copiar a qa-credentials.env y completar (no commitear).
# Usado por scripts del piloto — cuenta QA Yaque.
#
# QA_BAD_PASSWORD: para automatizacion (adb) usar solo alfanumerico.
# El guion manual puede usar Test-LOG-003; el robot necesita algo como WrongPass003.

QA_USER=
QA_PASSWORD=
QA_BAD_PASSWORD=WrongPass003
