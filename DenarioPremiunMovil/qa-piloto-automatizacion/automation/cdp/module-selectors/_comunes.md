# Module Selectors — Denario Premium Móvil
## Memoria técnica extraída de corridas reales · NO modificar manualmente

> Selectores y patrones **probados en campo** en corridas globalmp, romher, insumar, hidroponias.
> Leer como tercer archivo obligatorio junto a `RUNTIME.md` + `smoke-{modulo}.md`.
> Actualizar solo vía `prompt-consolidar-hallazgos.md` — nunca a mano.
>
> **Tags de corrida:** `[gmp-2606]` globalmp 20260605_162806 · `[gmp-2611]` globalmp 20260611_181640 · `[rom-2606]` romher 20260604 ·
> `[ins-2606]` insumar 20260603 · `[ins-2610]` insumar 20260610_180320 · `[ins-2611]` insumar 20260611_122104 (parcial: COBROS) · `[hid-2605]` hidroponias 20260529 ·
> `[dth-2612]` don-theo 20260612_103038 (1ª corrida — servidor Isla Coche, mismo backend que insumar/globalmp) ·
> `[prc-2606]` piercar 20260616_131624 (1ª corrida — repuestos automotrices, servidor independiente) ·
> `[ins-2622]` insumar 20260622_115712 (oráculo BD operativo por 1ª vez en insumar; alias `[insumar-20260622]`) ·
> `[jerez-2026-07-06]` jerez 20260706_100801 (corrida PARCIAL 8/10: sin Productos/Vendedores, sin cotejo BD — solo UI + round-trip §9; build refactorizado El Yaque, `window.ng=false`) ·
> `[ferrenuestro-2026-07-07]` ferrenuestro 20260707_175334 (1ª corrida COMPLETA 10/10 — servidor Isla Coche, catálogo ferretería; build refactorizado El Yaque `window.ng=false`; verificación BD operativa con cotejo campo-a-campo; ⚠ sync a nube DIFERIDA ~3min — devoluciones/inventarios/depósitos aparecieron tras la ventana de poll, todos persistieron; NO es no-persistencia tipo jerez) ·
> `[hidroponias-20260710]` hidroponias 20260710_084522 (2ª corrida COMPLETA 10/10 — servidor Isla La Tortuga v6.6.18; cobros/pedidos/inventarios/devoluciones/visitas/depósitos persistieron BD-OK; retención confirmada 14 díg; depositos.aplica=true confirmado) ·
> `[dm-electronica-20260713]` dm-electronica 20260713_115814 (1ª corrida COMPLETA 10/10 — servidor El Yaque DM ELECTRONIC v6.6.18, catálogo línea blanca/electrónica; build refactorizado El Yaque pero ⚠ **`window.ng=TRUE`** (contrasta jerez/ferrenuestro El Yaque `window.ng=false`); verificación BD operativa con cotejo campo-a-campo BD-FIELD-OK en clientes/pedidos/devoluciones/inventarios/visitas; sync a nube INMEDIATA (NO diferida como ferrenuestro); ⚠ app crasheó durante el POST de envío del cobro (id_collection=5 persistió, requirió relanzar+re-map adb forward); mockCameraAdjunto SÍ funciona; 0 FAIL) ·
> `[latino_cosmetica-20260714]` latino_cosmetica 20260714_130727 (1ª corrida COMPLETA 10/10 — servidor Isla La Tortuga v6.6.18, catálogo cosmética; ⚠ **`window.ng=TRUE`** (como dm-electronica; contrasta jerez/ferrenuestro El Yaque); verificación BD campo-a-campo BD-FIELD-OK en clientes/pedidos/cobros/devoluciones/inventarios/visitas/depósitos; sync a nube INMEDIATA/persistente; ⚠ app crasheó durante el POST de envío del cobro (id 24 persistió al reintentar desde Guardado; **`collection` SÍ capturado en el reintento**); ⚠ **mock cámara webpack falla** → adjunto por fabricación de Foto en `adjuntoService`; retención = variante `dynamicRetentions` (DM-COB-041/042 BLOCKED, requiere helper); 0 FAIL) ·
> `[gmp-20260730]` globalmp 20260730_094753 (servidor Isla La Tortuga, app_version **1.0** / db_version 19, **`window.ng=TRUE`**; BD nube **sin GRANT** → oráculo por **BD local vía `window.sqlitePlugin`** + payload `nativePromise`; PEDIDOS 14/14 PASS con pedido Nro. 15167 Enviado; **2 empresas** con `enterpriseEnabled` + `orderTypeByEnterprise` ambos true) ·
> `[ferrenuestro-20260723]` ferrenuestro 20260723_172350 (2ª corrida COMPLETA 10/10 — ⚠ **MIGRÓ de servidor Isla Coche → La Tortuga v6.6.18**: el device fue re-apuntado (`ws_url` → `denariolatortuga.ddns.net:8081`). El par (servidor, `window.ng`, inmediatez de sync) cambió con el build: **`window.ng` FALSE→TRUE**, sync **DIFERIDA→INMEDIATA**, botón de alertas informativas "Aceptar"→**"OK"**, depósito **2→3 alertas**. 125 PASS / 0 FAIL / 1 SKIP / 11 N/A. **window.ng=TRUE destrabó** DM-PED-026 (trash Tab Total, era BLOCKED julio) y la **retención por documento DM-COB-041/042/029** (el neto persiste al reabrir). ⚠ **cotejo BD CAÍDO**: el DSN de `qa-db.env` sigue apuntando a la BD vieja de Isla Coche → permission-denied → **BD-N/A (payload)** (verificación por hook `nativePromise` + UI). ⚠ **`openNuevoCobro` programático NO renderiza el form** en este build (aun con window.ng=true) → click real en tile COBRO/ANTICIPO/RETENCIÓN; `openDocumentDetail` SÍ opera. ⚠ wall-clock ~15.7h por 2 cuelgues de CDP (cobros 2.7h, productos 9.9h). 0 FAIL) ·
> `[el_palmar-20260805]` el_palmar 20260805_133539 (1ª corrida — servidor **Isla Coche**, app **v1.0 / db19**, `window.ng=TRUE`; **2 EMPRESAS** con `enterpriseEnabled=true`: CENTRAL EL PALMAR S.A. (id1/1002, la de la corrida) y C.A. DESTILERIA YARACUY (id2/1003, la marcada `enterprise_default`); **el `idEnterprise` llega EDITABLE + VACÍO + obligatorio** — acota la regla de `disabled=true` de latino_cosmetica/alipascua a los clientes de UNA sola empresa; `userMustActivateGPS=false`; LOGIN 6/6 PASS) ·
> `[difranca-20260807]` difranca 20260807_120232 (1ª corrida — servidor **El Yaque**, app **v1.0 / db19**, `window.ng=TRUE`, `sqlitePlugin` disponible; **3 EMPRESAS activas** con `enterpriseEnabled=true`: `DDHP_A12` *DISTRIBUIDORA DIAZ HERNANDEZ * (id 2, la de la corrida, **`enterprise_default=true` Y `priority_selection=0`** — por una vez el default coincide con la empresa correcta), `DIF_A12` DIFRANCA C.A (id 3), `DHVITAL01_A` DISTRIBUIDORA DH VITAL (id 4); la 4ª empresa **borrada en BD (`co_operation='D'`) NO baja al device**; LOGIN 6/6 PASS) ·
> `[grupo_fiel-20260817]` grupo_fiel 20260817_092435 (1ª corrida — servidor **El Yaque** `denarioelyaque.ddns.net:8081`, app **v1.0 / db19**, `window.ng=TRUE`, `sqlitePlugin` disponible; **UNA sola empresa** `00001` GRUPO FIEL, S.A. (GRUFISA) con `enterpriseEnabled=false` ⇒ el `idEnterprise` de CLIENTES vuelve a la variante **`disabled=true` auto-asignada**, cerrando el patrón "nº de empresas" con 4 corridas; vendedora `johana` / `co_user '003'` / 61 clientes; **CLIENTES 12/12 PASS, 0 FAIL**, potencial Ref 35 Enviado **BD-OK** con cotejo 15/15 campos y sync **INMEDIATA**; ⚠ el HOST **no está en `localStorage`** — solo se obtiene del hook de payload) ·
> `[kron-20260817]` kron 20260817_145314 (1ª corrida — servidor **Isla Coche** `denarioislacoche.ddns.net:8081`, **confirmado empíricamente desde el host del POST**, no asumido; app **v1.0 / db19**, `window.ng=TRUE`, `sqlitePlugin` disponible; **UNA sola empresa** `KRON_ADM` "CHOCOLATES KRON, C.A" (`lb_enterprise`, **sin punto final**; `na_enterprise` sí lo trae) con **`enterpriseEnabled=TRUE`** ⇒ **prueba que la VG NO gobierna la variante de `idEnterprise`**: mismo `disabled=true` auto-asignado que grupo_fiel con la VG en `false`; vendedora `scarlet` / `co_user 'VE0002'` / 30 clientes; **CLIENTES 12/12 PASS, 0 FAIL**, potencial Ref 78 Enviado **BD-OK** con cotejo **16/16 campos** y sync **INMEDIATA**; ⚠ el agente LOGIN dejó `__qaH` con solo 2 skills y **sin** hook de payload ⇒ hubo que instalarlo con guarda propia; 🔴 **defecto nuevo**: `Crédito Disp.` del detalle con etiquetas de moneda cruzadas + conversión invertida, en 2 clientes) ·
> `[run_vzla-20260818]` run_vzla 20260818_152824 (1.ª corrida — servidor **La Tortuga** `denariolatortuga.ddns.net:8081`, **confirmado desde el host del POST**; app **v1.0 / db19**, `window.ng=TRUE`, `sqlitePlugin` disponible, device Infinix X6728 360×744; **UNA sola empresa** `FERRE_N` "CORPORACION FERRE 19" (`lb_enterprise`; `na_enterprise` = "…, C.A.") con `enterpriseEnabled=true`; vendedor `id_user=470` / `co_user='000208'` / **1.569 clientes**; tenant **MONO-MONEDA** (`hardCurrency=""` ⇒ el saldo vive en `saldoLocal`) e **IVA 0** verificado en BD; **185/185 tablas con GRANT** en la nube; `userMustActivateGPS=TRUE` ⇒ **la guarda de GPS bloquea la NAVEGACIÓN de 3 módulos** (30,3 s pedidos · 43,1 s devoluciones · ~87 s inventarios · 6,85 s visitas) ⇒ techo de espera **≥ 120 s** graduado a RUNTIME; 🔴 **1 FAIL de severidad alta en VISITAS**: comentario > 120 chars ⇒ la visita **nunca llega al servidor sin ningún aviso** (`incidence.tx_description varchar(120)` vs input 255); 🔴 **corrección de memoria: `mouse.click` en el header del acordeón de VENDEDORES SÍ EXPANDE en este build**, contra 6 corridas previas).

---

## Stack tecnológico

| Capa | Tecnología |
|------|-----------|
| Framework | Ionic 6 + Angular (standalone, AOT production build + Ivy) |
| Runtime móvil | Capacitor (Android WebView) |
| Automatización | Playwright MCP + CDP vía `connectOverCDP('http://127.0.0.1:9220')` (`h.connectCdp`) |
| Package | `com.kiberno.denarioPremiumPro` |
| Router | Angular Router — `/login`, `/home`, `/cobros`, `/visitas`, `/visita`, `/pedidos`, `/inventarios`, `/productos`, `/vendedores`, `/depositos` |
| State | Servicios Angular con RxJS; state en instancia de servicio (no store) |

**Detección de vista activa:** siempre `h.getActiveView(pg, ['app-cobros', 'app-home', ...])` antes de interactuar. El componente activo se detecta por `offsetParent !== null`.

**Principio de interacción (universal):** los eventos Angular no se disparan con `element.click()` ni `dispatchEvent(MouseEvent)` simples en build AOT. Usar `pg.mouse.click(x, y)` con coords reales de `getBoundingClientRect()`. Para botones de navegación/envío en algunas vistas hace falta además `PointerEvent(pointerdown/up) + MouseEvent(click)` combinados (ver módulo COBROS).

**Convención de selectores recurrentes (todos los módulos):**
- Botones Guardar/Enviar de formulario: `ion-button.imagenGuardar` / `ion-button.imagenEnviar` — **icon-only, sin textContent**; localizar por clase CSS. Header fijo (`y≈32`), accesibles desde cualquier tab. Disabled hasta que el formulario es válido.
- Botón atrás: `img.fechaAtras` → `closest('a')` (helper `h.clickBack`). Excepción: PRODUCTOS no usa `.fechaAtras` (ver sección).
- Trash de borrado: `ion-button[color="danger"]` — solo aparece en ítems Estatus **Guardado** (nunca en Enviado).
- Modales: detectar apertura con `classList.contains('show-modal')`, no por ausencia de `overlay-hidden`.
- **Entrada a un módulo desde HOME (universal):** `app-home` renderiza cada tile como `<a class="ion-text-center">` que envuelve `<p class="nombreModulos">{Nombre}</p>` dentro de un `ion-col`. Localizar el `<p>` cuyo `textContent.trim()` == nombre del módulo → `closest('a')` → `pg.mouse.click` en el centro de su `getBoundingClientRect()`. ⚠ **NO** clicar el `ion-col` contenedor (absorbe el click sin navegar). Confirmado para Clientes/Productos/Vendedores. `[ferrenuestro-20260723][dth-2612]`

---

## Gaps pendientes de mapear (sin selector probado aún)

Estos elementos no tienen selector confirmado porque ninguna corrida tuvo los datos/condiciones para ejecutarlos. Completar con búsqueda dirigida en el XML (Fase 5) o cuando una corrida los alcance.

| # | Módulo | Elemento sin mapear | Por qué no se capturó | Cómo desbloquear |
|---|--------|---------------------|------------------------|------------------|
| G1 | COBROS | Detalle de retención en documento (campo comprobante `sizeRetention`/`formatRetention`, fecha, monto IVA, monto ISLR) — DM-COB-041/042 | Ningún cliente tuvo documento vencido (rojo) elegible en sesión | Buscar `cobro-document-detail` en XML; o corrida con factura vencida en cartera |
| G2 | COBROS | Flujo interno de ANTICIPO/PREPAGO y COBRO 25% IVA (form, tabs) — DM-COB-028/037 | Botones visibles pero "No hay clientes disponibles" | Buscar componentes prepago/IGTF en XML; o cliente con elegibles |
| G3 | DEPÓSITOS | ✅ **CERRADO** `[ins-2610]` — Guardar→Enviar con cobro vinculado ejecutado end-to-end en insumar (Ref real Enviado); el bug de render no bloqueó | — | — |

---

## Apéndice — Notas CDP transversales

- **Socket CDP:** `webview_devtools_remote_<PID>` (NO `_1`). El PID cambia al reiniciar la app. Re-mapear: `adb forward tcp:9220 localabstract:webview_devtools_remote_<PID>`. `[gmp-2606][ins-2606]`
- **Conexiones stale:** múltiples corridas acumulan TCP ESTABLISHED en :9220 → `adb forward --remove` + re-forward. `[rom-2606]`
- **🔴 El forward de CDP puede estar CAÍDO al arrancar un módulo AUNQUE el PID siga vivo — verificarlo ANTES del primer `connectCdp`.** `connectOverCDP` dio `ECONNREFUSED :9220` con la app corriendo y el socket `webview_devtools_remote_29842` presente en `/proc/net/unix`: el proceso estaba bien, el **forward** no. **Receta de 3 comandos que lo resolvió a la 1.ª** (funciona en PS 5.1; `setup-cdp.ps1` muere por un warning de adb):
  ```sh
  adb shell cat /proc/net/unix | grep -o "webview_devtools_remote_[0-9]*"
  adb forward --remove tcp:9220; adb forward tcp:9220 localabstract:webview_devtools_remote_<PID>
  curl -s http://127.0.0.1:9220/json/version
  ```
  El pre-vuelo ahorra un `CDP-DOWN:` y un **BLOCKED falso** al módulo entero. `[run_vzla-20260818]`
- **🔴 `pg.waitForFunction` IGNORA el `timeout` y corta a 30 s sobre el `pg` de CDP.** `{timeout:70000}` devolvió `page.waitForFunction: Timeout 30000ms exceeded` a los **30,2 s**. ⇒ **para cualquier espera > 30 s (guarda de GPS, sync, rehidratación de forms) sondear a mano en bucle con `page.waitForTimeout`**, nunca confiar en el `timeout` de `waitForFunction`: el corte se lee como "la navegación falló" cuando en realidad seguía en curso. ✅ **GRADUADO a `RUNTIME §11`.** `[run_vzla-20260818]`
- **🔴 Cotejar `character_maximum_length` de la NUBE contra el `maxlength` del input ANTES de teclear al tope.** Consulta barata: `SELECT column_name, character_maximum_length FROM information_schema.columns WHERE table_name='<tabla>'`. **Las tres fuentes pueden discrepar entre sí** — VG `longitudComentario`=250 · constante de la APK / `maxlength` del input=255 · **columna de la nube=120** (`incidence.tx_description`) — y el registro que excede la columna **se pierde en silencio** (ver el FAIL de VISITAS). Aplicar a todo campo de texto largo antes de un caso de límite. `[run_vzla-20260818]`
- **Reconexión mid-run:** el PID puede cambiar a mitad de corrida (app reinicia en HOME); la data ya enviada no se pierde. Tras re-mapear `adb forward` al nuevo PID, `connectOverCDP(:9220)` reconecta limpio y el estado (HOME) se conserva. ⚠ **Verificar `pg.url()` ≠ `chrome-error://` antes de operar; reintentar 1-2× si el target es inválido.** `[rom-2606][ins-2622]`
- **MCP→WebView (el `page` del MCP NO es el WebView):** el MCP playwright (`@playwright/mcp@0.0.75`) arranca SIN `--cdp-endpoint` → su `page` es un Chromium propio (about:blank, Chrome 149), NO el WebView. Para alcanzar el WebView desde `browser_run_code_unsafe`: `page.context().browser()._browserType.connectOverCDP('http://127.0.0.1:9220')` (patrón `h.connectCdp`) — funciona dentro del sandbox donde `require`/`process`/`module` están bloqueados. **Reconectar al inicio de CADA `browser_run_code_unsafe`** (contexto nuevo por llamada); `window.__qaH` persiste entre `evaluate` de la misma página. `[ins-2622]`
- **🔴🔴 CÁMARA — el mock hay que ponerlo en el BRIDGE, no en `Plugins.Camera`:** `window.Capacitor.Plugins.Camera` es un **Proxy**: `cam.getPhoto = fn` **NO se pega** (tras asignar sigue leyendo `getPhoto() { [capacitor code] }`) ⇒ el viejo `mockCameraAdjunto` devolvía **"OK" siendo un FALSO OK**, el click abría la **cámara NATIVA** —actividad de Android fuera del WebView, que CDP no alcanza— y la app quedaba colgada. **No hay salida automática** (`adb shell input` está prohibido): sólo reiniciar a mano. Incidente real: se perdió un cobro y el PID pasó de 20475→28660. **Punto que SÍ funciona: `Capacitor.nativePromise(plugin, metodo, opciones)`** — interceptar `plugin==='Camera'` y resolver local. Verificado en device: `getPhoto` 7 ms, `checkPermissions` 7 ms (esa también colgaba: el diálogo de permisos es nativo) y **los demás plugins siguen operando** (`Device.getInfo` 34 ms). Usar `h.installCameraMock(pg)` **antes de tocar cualquier botón de foto**; `h.cameraMockActivo(pg)` para re-verificar (el mock se pierde al recargar/reiniciar la app) y `h.clickCamaraSeguro(pg, coords)`, que **se niega a clickear** si el mock no está. `[el_valle-20260728]`
- **🔴 `ion-loading` se apila ENCIMA del `ion-alert` y su backdrop se come el click del botón:** al abrir el formulario de un módulo puede quedar un `ion-loading` visible (`z-index: 40015`) **por encima** del `ion-alert` (`z-index: 20016`). `alertButtonCoords('Aceptar')` devuelve coords correctas, el botón existe y es visible, pero `document.elementFromPoint(x,y)` cae en `ION-BACKDROP.sc-ion-loading-md ... backdrop-no-tappable` ⇒ el click **no llega nunca al alert** y el alert reaparece intacto en cada intento (se observó 3 veces seguidas antes de diagnosticarlo). **Fix: `await l.dismiss()` de todos los `ion-loading` visibles ANTES de clickear cualquier botón de alert.** Diagnóstico: comparar el botón esperado contra `elementFromPoint` (mismo método que el modal residual, pero el interceptor es el loading). Reproducido con el alert de geolocalización de INVENTARIOS. `[latino_cosmetica-20260729]`
  - ⚠ **Variante TRANSITORIA que se auto-resuelve — esperar, no remover:** tras pulsar BUSCAR en DEPÓSITOS, `elementFromPoint` devolvió `ION-BACKDROP.sc-ion-loading-md` en **los 3 puntos probados** del ítem, aunque `ion-loading:not(.overlay-hidden)` contaba **0**. Segundos después el loading desapareció **solo**, y los 13 `ion-backdrop` restantes eran todos 0×0 dentro de `ion-alert` (legítimos). **Diagnosticar con `elementFromPoint` y REINTENTAR tras esperar**, en vez de remover backdrops (que además está prohibido dentro de `ion-alert`/`ion-modal`). `[el_palmar-20260805]`
- **🔴 `ion-modal.show-modal` RESIDUAL intercepta TODOS los clicks por coordenadas:** un modal (típicamente el selector de cliente) queda en el DOM tras cerrarse y `elementFromPoint` cae fuera del target → Guardar, trash y navegación de lista fallan **sin error visible**. Diagnóstico: comparar el elemento esperado contra `document.elementFromPoint(x,y)`. Fix: `await modal.dismiss(null,'cancel')` antes de seguir. Tumbó varios flujos en inventarios hasta detectarlo. `[el_valle-20260728]`
- **✅ `expirationBatch` gobierna la VALIDACIÓN, no la VISIBILIDAD — cierra la "divergencia UI-vs-config" arrastrada desde piercar:** los campos Lote y Fecha de vencimiento **se renderizan siempre**; lo que cambia con la VG es si son **obligatorios**. Con `expirationBatch=true` (el_valle) la app rechaza con *"Complete cantidad, unidad, fecha y lote para continuar."* y el producto no queda inventariado; con `false` (dm-electronica, latino_cosmetica) los mismos campos aparecen pero son opcionales. **No era una divergencia**: se estaba mirando la visibilidad en vez de la validación. Al evaluar esta VG, probar con el campo VACÍO, no mirar si el input existe. `[el_valle-20260728]`
  - ⬆️ **GRADUADO a `RUNTIME.md §3` `[kron-20260817]` — 3.ª confirmación.** La regla completa es: **el alcance de `expirationBatch` es POR MÓDULO** (en el_palmar, grupo_fiel y kron bloquea en INVENTARIOS y **no** en DEVOLUCIONES de la misma corrida) y **el campo bloqueante es el LOTE**, nunca la fecha (nace en HOY). Ver RUNTIME §3 para la regla operativa; acá quedan los detalles por cliente.
- **Selección de cliente: usar `setClientfromSelector`, NO click real en el modal.** El click sobre `#clienteSelectModal ion-item` selecciona un **cliente vecino** (desalineación de coords en lista virtualizada). ⚠ **CORRECCIÓN `[latino_cosmetica-20260729]`: `setClientfromSelector` SÍ dispara el alert** (deuda vencida) también en form fresco — la nota previa "no dispara alert en form fresco" NO es general. Tras llamarlo, esperar ≥2,5 s y **aceptar el alert en bucle (hasta 3 intentos)**: el 1.er click puede no tomar y dejar el form vacío con el modal de clientes abierto; si queda `ion-modal.show-modal`, cerrarlo con `await modal.dismiss(null,'cancel')` y **verificar que el campo cliente quedó seteado** antes de continuar. El componente dueño del handler varía por módulo (`app-cobro-general` en cobros, **`app-pedido`** en pedidos). El selector de productos tiene un `arrow-back-outline` interno para volver al nivel de familias. `[el_valle-20260728]`
- **`eval` SÍ funciona en el sandbox de `browser_run_code_unsafe`** (aunque `require`/`fs` no): permite guardar el prelude de helpers en `window.__qaSrc` y bootstrapearlo con `eval`, en vez de reinlinar el bundle en cada llamada → ~10× menos tokens por `browser_run_code_unsafe`. `[el_valle-20260728]`
- **🔴 `sqlite3` NO existe en el device (build La Tortuga v1.0):** `automation/db/local-query.js` falla SIEMPRE (`adb run-as` + `sqlite3` no disponible) ⇒ `local-query.js` queda inoperante. **No gastes reintentos en él.** `[el_valle-20260728]`
  - ✅ **2.ª y 3.ª confirmación en Isla Coche v1.0/db19** (`run-as: exec failed for sqlite3: No such file or directory`): el fallo **no es de La Tortuga, es del build**. Dar `local-query.js` por inoperante de entrada e ir directo a `window.sqlitePlugin`. `[el_palmar-20260805]`
  - ⚠ **Los nombres de tabla LOCALES van en PLURAL y difieren de la nube (singular):** `products` · `product_units` · `price_lists` · `stocks` · `units` · `product_structures` · `visits` · `incidences` · `deposits` · `deposit_collects` · `collections` · `returns`. **`unit_pricelist` es la excepción** (igual en ambas). Usar el nombre de nube contra la BD local **aborta la transacción `sqlitePlugin` en silencio**. `[el_palmar-20260805]`
  - ✅ **PERO la BD LOCAL SÍ es legible desde CDP** — `window.sqlitePlugin` está expuesto en el WebView, así que el oráculo local del §10 (`pending_transactions`, `failed_transactions`, `st_delivery`, duplicados) **NO es `BD-N/A`: es consultable**. Verificado leyendo `enterprises` y confirmando que ambas tablas existen. `[latino_cosmetica-20260729]`
    ```js
    // dentro de browser_run_code_unsafe / pg.evaluate
    const db = window.sqlitePlugin.openDatabase({ name: 'denarioPremium', location: 'default' });
    const filas = await new Promise((res, rej) => db.transaction(tx =>
      tx.executeSql('SELECT * FROM pending_transactions', [],
        (t, r) => res(Array.from({ length: r.rows.length }, (_, i) => r.rows.item(i))),
        (t, e) => rej(e))));
    ```
    ⚠ `CapacitorSQLite` y `window.openDatabase` **NO** existen — `window.sqlitePlugin` es la única vía.
  - **Descubrir el esquema ANTES de consultar** — un nombre de columna errado aborta la transacción y el callback de error devuelve solo `no such column: X` (o, si se ignora, silencio). Primero:
    `SELECT name, sql FROM sqlite_master WHERE type='table' AND name LIKE '<modulo>%'`.
    Ej. real: `collections` usa **`nu_amount_total`**, no `nu_amount`; la conversión es `nu_amount_total_conversion`. `[gmp-20260730]`
  - 🔑 **`sqlite_sequence` distingue "se borró" de "nunca llegó":** `SELECT seq FROM sqlite_sequence WHERE name='<tabla>'` da el **techo histórico de inserts** de la tabla. Comparado con `count(*)` y con los `local_id` supervivientes (los huecos = filas borradas) **reconstruye la historia de la tabla sin logs**. Fue la evidencia decisiva para establecer el criterio del filtro de la lista de COBROS. `[el_palmar-20260805]`
  - **Los BOOLEANOS vuelven como STRING** desde `executeSql`: `in_payment_partial` llega `"true"`/`"false"` (texto), mientras el mismo campo en el modelo Angular (`window.ng.getComponent(...)`) es un `boolean` real. Un `=== true` sobre la fila cruda **siempre** da false → comparar contra `'true'` en SQL y contra `true` en el modelo. Mismo caso en `has_discount`, `hasIGTF`. `[gmp-20260730]`
    Vale sobre todo cuando la BD **nube** está sin GRANT: recupera `BD-QUEUED`/`BD-MISMATCH` local, aunque **no** prueba llegada a la nube (eso lo prueba la capa web).
- **🔴 El bundle `__qaH` es IDEMPOTENTE ⇒ cada agente HEREDA las skills del agente ANTERIOR (y el hook de payload se APILA):** `helpers-inline.js` abre con `if (window.__qaH) return 'OK: __qaH ya instalado'`. Del 2º agente de la corrida en adelante **tu versión NO se instala**: corrés con la del primero. Impacto real (clientes, latino_cosmetica-20260729): el `installPayloadCapture` heredado del agente LOGIN registra `{plugin, method, url}` **sin `options.data`** ⇒ `_payloads.jsonl` queda **sin body**. Y forzar `window.__qaCaptureInstalled=false` + reinstalar **NO reemplaza** el hook: **apila un 2º wrapper sobre `Capacitor.nativePromise`** ⇒ **cada POST aparece DUPLICADO** en `getCapturedPayloads()`. Diagnóstico rápido: si las entradas traen `plugin`/`method` y no `data`, estás usando el hook de otro agente.
  - **Fix ya instalado en el WebView de esta corrida:** `window.__qaH.getPayloadData()` → `[{url, data}]` (array `window.__qaPayloadsData`, wrapper único protegido por `window.__qaDataHook`). **Usar ése para el body**; `getCapturedPayloads()` sirve solo para confirmar QUE hubo POST (y viene duplicado).
  - Regla general: **no asumas que las skills de `window.__qaH` son las tuyas.** Si necesitás una variante propia, registrala con otro nombre (`window.__qaH.miSkill = ...`) en vez de reinstalar el bundle. `[latino_cosmetica-20260729]`
  - ✅ **RECETA VALIDADA — bundle propio con NAMESPACE distinto + hook de payload con guarda (0 duplicados, `data` completo):** en vez de pelear con `__qaH`, el agente registra **`window.__qaC`** (o `__qaP`, `__qaV`… un namespace por módulo) con sus propias skills, y hookea `Capacitor.nativePromise` **una sola vez** protegido por una bandera propia:
    ```js
    if (!window.__qaDataHook && typeof Cap.nativePromise === 'function') {
      window.__qaPayloadsData = [];
      const orig = Cap.nativePromise.bind(Cap);
      Cap.nativePromise = function (plugin, method, options) {
        try { if (options && options.url && /service\//i.test(String(options.url)) && /post/i.test(String(method)))
          window.__qaPayloadsData.push({ url: options.url, data: options.data }); } catch(e){}
        return orig(plugin, method, options);
      };
      window.__qaDataHook = true;   // ⚠ la guarda es lo que evita el apilado de wrappers
    }
    ```
    Resultado en clientes/alipascua: el POST `potentialclientservice/potentialclient` se capturó **1 sola vez y CON `data`** (el `__qaH` heredado del agente LOGIN traía 5 skills y **ninguna** captura). **No** tocar `window.__qaCaptureInstalled=false` + reinstalar el bundle: eso es justo lo que apila el 2.º wrapper y duplica cada POST. `[alipascua-20260804]`
    - ✅ **RECETA GRADUADA — confirmada en 4 módulos de otra corrida** (`__qaD` devoluciones, `__qaI` inventarios, `__qaV` visitas, `__qaW` web): cada agente registra su namespace propio con sus skills (`alertInfo`, `alertBtn`, `rectOf`, `homeTile`, `views`, `killLoadings`, `tabs`…) **y consume `__qaH.getPayloadData()` heredado del agente LOGIN sin reinstalarlo**. Resultado en los 4: payload capturado **1 sola vez y CON body**, y **0 reintentos** de alert. Es la forma estándar de trabajar de los agentes 2-10. `[alipascua-20260804][el_palmar-20260805]`
    - 🔴 **PERO el hook heredado puede NO EXISTIR — verificarlo al arrancar, nunca asumirlo.** En kron el agente LOGIN dejó `window.__qaH` con **solo 2 skills** (`fillIonInput`, `activeAlertInfo`) y **`window.__qaDataHook === false`** ⇒ la receta "consumir `__qaH.getPayloadData()`" **no aplicaba** y `getPayloadData` ni siquiera existía. Un agente que la dé por hecha se queda **sin payload** y pierde el único medio de descubrir el HOST (que no está en `localStorage`). **Diagnóstico de 1 línea en el arranque:** `Object.keys(window.__qaH||{})` + `!!window.__qaDataHook`; si el hook falta, instalarlo con **guarda propia** (el snippet de `[alipascua-20260804]` de arriba). Resultado en kron: 1 POST capturado **1 sola vez con `data`**, 0 duplicados. ⇒ **La regla estable no es "heredar" ni "reinstalar", es: comprobar `__qaDataHook` y actuar en consecuencia.** `[kron-20260817]`
      - ✅ **2.ª corrida seguida en que el hook heredado NO existe ⇒ la regla queda CERRADA: comprobar SIEMPRE, nunca heredar ni reinstalar a ciegas.** En run_vzla el agente LOGIN dejó `window.__qaH` **vacío** (`Object.keys → []`) y `__qaDataHook=false`; instalado con guarda propia dio **6 POST capturados, `potentialclient` 1 sola vez con `data`, 0 duplicados**. ⚠ Y el estado varía **dentro de la misma corrida**: en COBROS (módulo 10) `window.__qaH` llegó **inexistente** (ni siquiera figura en `Object.keys(window)`) pero `__qaDataHook=true` con **616 payloads** ⇒ hook heredado **consumido sin reinstalar** (641 al cierre, 0 duplicados). **Las dos banderas se leen por separado.** `[run_vzla-20260818]`
      - 🔴 **El hook cuenta los REINTENTOS: N POST del mismo `co_<x>` = transacción ATASCADA, no "sync lenta".** 7 POST a `visitservice/visit` para 3 visitas ⇒ 5 eran la misma. **Diagnóstico de 1 línea y sin BD:** agrupar `window.__qaPayloadsData` por `data.<entidad>.co<Entidad>` y buscar duplicados. Distingue *sync lenta* (1 POST pendiente) de *rechazo en bucle* (N POST idénticos) — y es lo que `failed_transactions` **no** dice (ver `RUNTIME §10`). `[run_vzla-20260818]`
      - **Namespace por módulo de 3 letras — ya tomados:** `__qaCLI` · `__qaPED` · `__qaDEV` · `__qaINV` · `__qaVIS` · `__qaPRO` · `__qaCOB`. Con 8 skills propias (`views`, `rectOf`, `alertInfo`, `alertBtn`, `homeTile`, `killLoadings`, `fill`, `back`) instaladas sin tocar `__qaH`: **cero colisiones en toda la corrida**. `[run_vzla-20260818]`
- **⚠ La variante de `idEnterprise` sigue a la VERSIÓN DE BUILD, no a la playa — probar `sel.disabled` PRIMERO:** en app **v1.0 / db19** el `ion-select[formcontrolname="idEnterprise"]` del form de cliente potencial llega **`disabled=true` + `.select-disabled`** con la empresa ya auto-asignada ⇒ queda **fuera de la validación** y Guardar/Enviar habilitan con solo los `ion-input` obligatorios (el envío igual viaja con el `idEnterprise`/`coEnterprise` correctos). En **v6.6.18** en cambio hay que asignar `sel.value = <number>` + `ionChange`. Ya visto en Isla Coche (`[latino_cosmetica-20260729]`) y ahora reconfirmado en **El Yaque** con la misma v1.0/db19 ⇒ **2 servidores distintos, mismo build, mismo comportamiento**. Regla: leer `sel.disabled` antes de intentar setear el value; si es `true`, no hay nada que hacer. `[latino_cosmetica-20260729][alipascua-20260804]`
  - 🔴 **ACOTACIÓN IMPORTANTE `[el_palmar-20260805]`: eso vale solo con UNA empresa. Con DOS o más, el mismo build v1.0/db19 entrega el select EDITABLE, VACÍO y OBLIGATORIO.** En el_palmar (Isla Coche, `enterpriseEnabled=true`, 2 empresas) el `ion-select[formcontrolname="idEnterprise"]` del form de cliente potencial llega **`disabled=false`, `value=null` y con clase `ng-invalid`** ⇒ **entra en la validación**: mientras no se elija empresa, Guardar/Enviar **no habilitan** (se lee como "el botón no responde" → falso FAIL/BLOCKED). Receta validada: `s.value = <number>` (¡number, no string!) + `s.dispatchEvent(new CustomEvent('ionChange',{bubbles:true,detail:{value:<number>}}))`; tras eso `ng-invalid` desaparece y el shadowRoot rotula el nombre. **No hace falta abrir el popover.** Regla general: **leer `sel.disabled` Y `sel.value`** — `disabled=true` ⇒ nada que hacer; `disabled=false` ⇒ hay que setearlo sí o sí.
    - ✅ **MEDIDO end-to-end en el módulo CLIENTES `[el_palmar-20260805]`:** con los **8 `ion-input` obligatorios ya llenos** y `idEnterprise=null`, `imagenGuardar`/`imagenEnviar` seguían **`disabled=true`**; tras `s.value=1` (number) + `ionChange`, `ng-invalid` cayó, el shadowRoot rotuló `CENTRAL EL PALMAR, S.A.` y **ambos botones habilitaron en el mismo tick**. Es la confirmación cuantitativa de que el select entra en la validación ⇒ **"el botón no habilita" con un select vacío NO es FAIL ni BLOCKED: es el campo faltante.** Medir siempre el par (botones antes / botones después) en vez de concluir por el botón solo.
    - ✅ **La empresa elegida SOBREVIVE el round-trip Guardar→reabrir y llega intacta a la nube** `[el_palmar-20260805]`: al reabrir el registro Guardado, `idEnterprise` vuelve como `1` (number) con su label; el payload viaja `coEnterprise:"1002"/idEnterprise:1` y la fila de nube trae `co_enterprise='1002'`/`id_enterprise=1`. **La "trampa" del `enterprise_default` apuntando a OTRA empresa no se materializó en ningún punto** (ni al crear, ni al reabrir, ni al enviar) — el default local no pisa la elección del usuario. Verificar igual, pero el riesgo es teórico hasta que alguna corrida lo reproduzca.
  - 🔴 **El `enterprise_default` de la BD local puede NO ser la empresa de la corrida:** en el_palmar la tabla local `enterprises` marca `enterprise_default="true"` en **C.A. DESTILERIA YARACUY (id 2 / co 1003)**, mientras la corrida opera **CENTRAL EL PALMAR, S.A. (id 1 / co 1002)**. Si un form auto-asigna empresa, la autoasignada sería la equivocada. **Verificar la empresa efectiva en cada registro creado**, no darla por buena. `[el_palmar-20260805]`
  - **No hay selector de empresa en `app-login` ni en `app-home`** (0 `ion-select` en ambas vistas, sin cabecera de empresa) aun con `enterpriseEnabled=true` y 2 empresas: el selector vive **únicamente dentro de los formularios de módulo**. `[el_palmar-20260805]`
    - ✅ **3.ª confirmación con TRES empresas y en otro servidor (El Yaque):** 0 `ion-select` en `app-login` y `app-home` (de hecho `document.querySelectorAll('ion-select').length === 0` estando en HOME), y el `ion-select[formcontrolname="idEnterprise"]` de CLIENTE POTENCIAL llegó otra vez **`disabled=false`, `value=null`, `ng-invalid`, shadowRoot "Seleccione..."**, con `imagenGuardar`/`imagenEnviar` ambos `disabled=true`. ⇒ **la variante "editable + vacío + obligatorio" en CLIENTES vale para 2 Y para 3 empresas, en Isla Coche Y en El Yaque** — es del formulario, no del servidor ni del nº de empresas. `[difranca-20260807]`
  - **⚠ El `ion-select` de empresa rotula `lb_enterprise`, que viene TRUNCADO a 19 caracteres desde el servidor** — no `na_enterprise`. En difranca la UI lista literalmente `*DISTRIBUIDORA DIAZ` / `DIFRANCA C.A` / `DISTRIBUIDORA DH VI`, mientras el nombre completo (`*DISTRIBUIDORA DIAZ HERNANDEZ *`, `DISTRIBUIDORA DH VITAL, C.A.`) **no se muestra en ningún lado**. Al cotejar "el nombre de la empresa" UI↔BD, comparar contra **`lb_enterprise`**; un mismatch contra `na_enterprise` es falso positivo. `[difranca-20260807]`
  - ✅ **Las empresas BORRADAS en BD (`co_operation='D'`) no llegan al dispositivo:** en difranca la nube tiene 4 empresas y la tabla local `enterprises` trae **solo las 3 activas** ⇒ el filtro se aplica en la sincronización, no en la UI. **Verificable barato con `sqlitePlugin` sin abrir ningún formulario** (`SELECT * FROM enterprises`) — hacerlo antes de gastar navegación en comprobar un selector. `[difranca-20260807]`
  - **⚠ `co_currency_default` puede ser INCONSISTENTE entre empresas del mismo cliente:** en difranca id 2 y 4 traen `US$` pero id 3 (DIFRANCA C.A) trae `USD`. Si un módulo compara el código de moneda por string, las dos grafías divergen. Leer `co_currency_default` de la empresa efectiva antes de juzgar cualquier veredicto de moneda. `[difranca-20260807]`
  - 🔴🔴 **EL SELECTOR DE EMPRESA VARÍA POR MÓDULO DENTRO DEL MISMO BUILD — leerlo, NUNCA asumirlo.** Medido en los 6 módulos de la misma corrida (el_palmar, v1.0/db19 Isla Coche, `enterpriseEnabled=true`, 2 empresas), con **4 variantes distintas**:

    | Módulo | `formcontrolname` | Estado al abrir | Tipo de `value` | ¿Entra en la validación? | Qué hacer |
    |---|---|---|---|---|---|
    | **CLIENTES** | `idEnterprise` | `disabled=false`, `value=null`, **`ng-invalid`** | `number` | **SÍ — obligatorio** | `s.value = 1` (**number**) + `ionChange` |
    | **PEDIDOS** | `idEnterprise` | preseleccionado (1002) y **habilitado** | `number` | no | nada |
    | **DEVOLUCIONES · INVENTARIOS · DEPÓSITOS · VISITAS** | **ninguno** | preseleccionado, `ng-valid` | **el OBJETO empresa completo** (`{idEnterprise, coEnterprise, lbEnterprise, …}`) | no | nada — **la receta `s.value=<number>` NO aplica ni hace falta** |

    - 🔴 **3.ª CONFIRMACIÓN de la regla, y la tabla se AMPLÍA `[difranca-20260807]`** (El Yaque, v1.0/db19, **3 empresas**): **CLIENTES** volvió a llegar `disabled=false` / `value=null` / `ng-invalid` (obligatorio); **DEVOLUCIONES y VISITAS** volvieron a llegar sin `formcontrolname`, preselectos y con el **objeto** completo como `value`; y **PEDIDOS cambió de casilla**: en el_palmar traía `formcontrolname="idEnterprise"` con `value` numérico, y en difranca llega **SIN `formcontrolname`, habilitado y con el OBJETO empresa completo** (`{idEnterprise, coEnterprise, lbEnterprise, coCurrencyDefault, prioritySelection, enterpriseDefault, naEnterprise, nuRif, txAddress}`) ⇒ aplicarle la receta `s.value=<number>` **rompería el form**. **Corolario definitivo: la variante NO es estable ni por módulo entre clientes — hay que LEERLA en cada form, siempre.**
    - 🔴🔴 **4.ª CONFIRMACIÓN y ⬆️ GRADUACIÓN a `RUNTIME.md §3` `[kron-20260817]` — con la VG en el valor CONTRARIO, la tabla no se mueve.** kron (Isla Coche, v1.0/db19, **1 empresa**, `enterpriseEnabled=**TRUE**`) reprodujo **4 variantes en el mismo tenant y el mismo día**: **CLIENTES** `disabled=true` auto-asignado (`value=null`, `ng-invalid=false`, shadowRoot "Seleccione…") · **INVENTARIOS** `disabled=**false**` con el objeto empresa completo y `ng-valid` · **DEPÓSITOS** sin `formcontrolname`, `disabled=false`, objeto · **VISITAS** `disabled=true`, sin `formcontrolname`, objeto de 8 claves, fuera de la validación · **PEDIDOS** `disabled=true` **pero YA RESUELTO** (objeto completo, shadowRoot rotulando `CHOCOLATES KRON, C.A`). ⇒ **`enterpriseEnabled` es irrelevante para la variante** (kron `true` = grupo_fiel `false`, mismo select) y **el nº de empresas solo predice si entra en la validación**. **DEROGADA toda lectura que ate la variante a la VG.**
    ⇒ **La variante no la fija el build ni el nº de empresas ni `enterpriseEnabled`: la fija el FORMULARIO.** Un guión que aplique la receta de CLIENTES en DEVOLUCIONES pisa un objeto con un number y rompe el form; uno que asuma "ya viene resuelto" en CLIENTES deja Guardar/Enviar deshabilitados para siempre. **Receta única y segura: leer `sel.getAttribute('formcontrolname')`, `sel.disabled`, `sel.value` y `sel.classList.contains('ng-invalid')` ANTES de decidir**, y actuar solo si `value` es `null`. En DEPÓSITOS se comprobó además que la app elige por **`prioritySelection:0`**, no por `enterpriseDefault` — por eso NO tomó la empresa marcada `enterprise_default`. `[el_palmar-20260805]`
- **⚠ `enterpriseEnabled` NO está en `localStorage.globalConfiguration`:** ese objeto trae ~180 claves pero **ninguna** matchea `/enterprise|empresa|gps|igtf/i` — la VG vive en la tabla `global_configuration` de la BD. No gastar intentos buscándola en localStorage. `[el_palmar-20260805]`
  - 🔴 **GENERALIZADO — `localStorage.globalConfiguration` NO sirve para leer NINGUNA VG de módulo.** Las ~180 claves tampoco matchean `/upload|file|signature|expiration|batch|invoice|return/i` (devoluciones) ni `/prod|price|stock|hide|unit|min|mul|curren|rate|page/i` (productos): **0 coincidencias en ambas búsquedas.** Dejar de buscar ahí. `[el_palmar-20260805]`
  - 🔴 **Y la tabla `global_configuration` de la BD LOCAL tampoco es oráculo en este build:** su esquema es `(id_config, clave, valor, descripcion)` — ojo, **`clave`/`valor`, NO `na_variable`/`va_variable`** — pero sus ~318 filas devuelven **los 4 campos NULL**. **Las dos fuentes fiables son (a) el COMPONENTE Angular** (`ng.getComponent(document.querySelector('product-list'))` expone `showStock`, `productStock0`, `unitByPriceList`, `currencyModuleEnabled`, `defaultCurrency`; `app-pedido` expone `listaAnterior`, `monedaSeleccionada`, `lockSegments`; `app-visita` expone `textCommentMaxLength`) **y (b) la BD de NUBE**. `[el_palmar-20260805]`
- **✅ Receta de arranque del 1er agente de la corrida (LOGIN) que deja el WebView listo para los 9 siguientes:** instalar el bundle `__qaH` con dos endurecimientos ya validados — (a) `alertButtonCoords` por **igualdad exacta** + filtro `width>0` (el canónico usa `includes`, peligroso en alerts de 3 botones), y (b) `installPayloadCapture` que hookea `Capacitor.nativePromise` **guardado por `window.__qaDataHook`** y empuja `{url, data}` a `window.__qaPayloadsData`, exponiendo `__qaH.getPayloadData()`. Como el bundle es idempotente, los 9 agentes siguientes **heredan la captura CON body y sin duplicados** — cierra el gap de `[latino_cosmetica-20260729]`. Corolario para los agentes 2-10: **no reinstalar el bundle ni tocar `__qaCaptureInstalled`**; usar `__qaH.getPayloadData()`. `[el_palmar-20260805]`
- **🔴 REGLA GENERAL — "el botón/tab que no responde" es casi siempre un CAMPO OBLIGATORIO VACÍO, no un fallo de automatización.** Generaliza el hallazgo de `idEnterprise` a cualquier control bloqueado. **2ª confirmación, con otro campo y otro módulo `[el_palmar-20260805]`:** en PEDIDOS, con el cliente **ya seleccionado**, las tabs Pedido/Total/Adjunto seguían `disabled` porque `ion-input#nuPurchase` (Nro. de orden de compra) llegaba **`required=true` y vacío**, manteniendo `comp.lockSegments=true`. Al llenarlo, `lockSegments` cayó a `false` y **las 3 tabs habilitaron en el mismo tick**. Se leía como "DM-PED-006 FALLÓ: las tabs no habilitan". **Receta antes de marcar FAIL/BLOCKED por un control que no habilita:** (1) listar los `ion-input`/`ion-select` del form con su `required`/`value`/`disabled`; (2) leer la bandera de bloqueo del componente (`lockSegments`, `ng-invalid`…); (3) **medir el par antes/después** de completar el campo sospechoso. Un control deshabilitado es un **síntoma**, nunca la evidencia de un defecto. `[el_palmar-20260805]`
- **🔴 En `ion-input`, `disabled=false` NO significa editable — hay que leer `readonly` aparte:** el campo de tasa del Tab General de PEDIDOS llega **`disabled=false` pero `readonly=true`** con la tasa del día. Mirar solo `disabled` lleva a concluir "la tasa se puede editar a mano" y a dar por buena la VG equivocada (`enabledManualRate`/`canChangeRate`). **Leer siempre el par `(disabled, readonly)`** antes de emitir veredicto sobre cualquier VG de editabilidad. `[el_palmar-20260805]`
- **🔴 `img.fechaAtras` tiene COPIAS OCULTAS con rect 0×0 — el back "no navega" sin error:** `document.querySelector('img.fechaAtras')` puede devolver una instancia con `getBoundingClientRect()` = `{0,0,0,0}` (vista anterior aún montada) ⇒ `pg.mouse.click(0,0)` cae sobre el `ION-GRID` de fondo y **no pasa nada, sin excepción ni log**. Se reprodujo 3 veces seguidas al intentar volver de `pedidosLista` a HOME. **Fix: filtrar siempre por tamaño**, igual que se filtra `offsetParent!==null` en alerts: `Array.from(document.querySelectorAll('img.fechaAtras')).filter(e=>e.getBoundingClientRect().width>0)[0]`. Con el visible el back opera con `mouse.click` (coords ≈32,47) en `app-pedido`, `app-pedidos-lista` y `app-pedidos`. `[gmp-20260730]`
- **🔴 Coords FUERA DEL VIEWPORT en listas largas → el click cae al vacío SIN error (hermano del `fechaAtras` rect 0×0):** el viewport del device es **360×744**. `getBoundingClientRect()` de un `ion-item` que está **bajo el fold** devuelve un rect perfectamente válido pero con `y` fuera de pantalla (real: `y=795` en la lista de productos de INVENTARIOS) ⇒ `pg.mouse.click(180, 795)` **no hace absolutamente nada, sin excepción ni log**, y se lee como "el modal no abre / el ítem no responde". **Regla: antes de clickear por coords, `el.scrollIntoView({block:'center'})` → esperar ~900 ms → RE-LEER el rect → validar `0 < y < innerHeight` y `0 < x < innerWidth`.** Generaliza el filtro que ya se aplicaba por `width>0`: **un rect que existe no implica un punto clickeable**. `[gmp-20260730]`
  - 🔴 **CORRECCIÓN al validador de viewport — validar el CENTRO, no las esquinas:** un `ion-item` de lista ocupa el ancho completo ⇒ `getBoundingClientRect().x === 0`. Un chequeo `r.x > 0 && r.x < innerWidth` lo descarta como "fuera del viewport" siendo perfectamente clickeable — **falso negativo real en DEVOLUCIONES, costó un intento**. Validar `cx = r.x + r.width/2` y `cy = r.y + r.height/2`. `[el_palmar-20260805]`
  - 🔴 **RE-LEER el rect después de TODO cambio que agregue o quite un campo del form, no solo tras scroll:** en DEPÓSITOS, al elegir el banco aparece el `ion-input` "Banco:" (cuenta autollenada) y **Fecha Doc baja de `y=477` a `y=348`**. Clickear con las coords leídas *antes* del banco cae al vacío **sin error** y se lee como "el modal de fecha no abre". Mismo quirk en el `ion-modal#eventModal` de VISITAS: el botón `Agregar` se desplaza `y=539→400→539` cuando aparece el `ion-select` de Motivo. **Nunca cachear coords entre acciones.** `[el_palmar-20260805]`
  - 🔴 **`pg.mouse.click` SIN `delay` puede NO registrar en los botones del header fijo:** el 1.er click en `ion-button.imagenEnviar` de DEPÓSITOS **no disparó nada** — `elementFromPoint` daba el botón correcto, `disabled=false`, y el oráculo `app-message` seguía con el mensaje viejo de otro módulo ⇒ el evento nunca llegó. Con **`pg.mouse.click(x, y, {delay:120})`** disparó a la primera. **Usar `delay:120-150` en Guardar/Enviar y en ítems de lista.** `[el_palmar-20260805]`
  - 🔴 **Un botón DENTRO de un `ion-modal` puede tener rect válido y aun así pertenecer a OTRO elemento:** el `Agregar` del `ion-modal#eventModal` de VISITAS tiene `getBoundingClientRect()` correcto, pero `document.elementFromPoint(x,y)` en ese punto devuelve **el botón "AÑADIR ACTIVIDAD" del form de fondo** ⇒ `mouse.click` re-dispara el form subyacente en vez de agregar. **Fix: `b.shadowRoot.querySelector('button').click()`** (funcionó al 1.er intento). Hermano nuevo de "rect válido ≠ punto clickeable": acá el punto **sí** es clickeable, pero **no es tuyo**. Diagnosticar siempre con `elementFromPoint` antes de reintentar. `[el_palmar-20260805]`
- **🔴 `ion-datetime`: el valor se confirma SOLO al pulsar Aceptar — leerlo antes es un falso negativo:** tras clickear el día en el calendario, `dt.value` **sigue devolviendo la fecha anterior**; recién cambia cuando se pulsa **Aceptar** en el `shadowRoot` (`h.confirmDatetime`). Verificar el cambio inmediatamente después del click y concluir "el día no tomó" lleva a reintentos innecesarios. **Leer `dt.value` DESPUÉS de Aceptar.** `[gmp-20260730]`
- **🔴 El calendario de `ion-datetime` es un CARRUSEL de 3 meses: 2 están fuera de pantalla:** `dt.shadowRoot.querySelectorAll('.calendar-day')` devuelve **~102 nodos** (mes anterior + actual + siguiente), todos con rect válido. Los del mes anterior dan **`x` negativo** (real: `x=-226`) y los del siguiente caen a la derecha del viewport ⇒ elegir "el primer día que cumpla la condición" clickea al vacío. **Filtrar SIEMPRE los candidatos por `x>0 && x<innerWidth && y>0 && y<innerHeight` antes de clickear**, y confirmar el mes con `data-month`/`data-year` (los `.calendar-day` los exponen como atributos). `[gmp-20260730]`
  - ⚠ **`data-month` viene SIN cero a la izquierda** (`"8"`, no `"08"`) — comparar contra el número, no contra el string con padding. `[el_palmar-20260805]`
- **🔴 Aceptar/Cancelar del `ion-datetime` son `ion-button#confirm-button` / `#cancel-button` en el shadowRoot — NO `button` nativos:** `dt.shadowRoot.querySelectorAll('button')` devuelve **solo el header de mes y los ~102 `.calendar-day`**: ningún Aceptar. Se concluye "el datetime no tiene botón de confirmar" y se pierde un intento. Los reales son **`ion-button` con `id`**: `sr.querySelector('#confirm-button')` (texto "Aceptar") y `#cancel-button`. **`dt.showDefaultButtons===true` es la señal de que existen.** Receta que cerró limpio en DEPÓSITOS: `dt.value='YYYY-MM-DD'` + `ionChange` → coords de `#confirm-button` → `pg.mouse.click`. `[el_palmar-20260805]`
- **⚠ El `ion-datetime` de un modal EXISTE SIEMPRE en el DOM con `offsetParent=null`** (vive dentro de un `ion-modal` cerrado) ⇒ leerlo sin abrir el modal da un falso "no hay campo fecha". Abrirlo con click real en `ion-button.letrasFechasButton` / `ion-datetime-button`. ⚠ El `ion-datetime-button` tiene **`textContent` vacío**: la etiqueta legible (`"5 ago 2026"`) vive en su `shadowRoot > button#date-button`; en DEVOLUCIONES el `.letrasFechasButton` **sí** rotula la fecha elegida y sirve de oráculo visual. Confirmado en devoluciones (`ion-datetime#fechaVence0` dentro de `ion-modal.fechasModal`) e inventarios. `[el_palmar-20260805]`
- **⚡ `page.__qa` PERSISTE entre llamadas de `browser_run_code_unsafe` (ahorro grande de tokens):** el objeto `page` que el harness MCP inyecta es **la misma instancia JS** en todas las llamadas del agente. Inlinar `connectCdp`/`withTimeout`/`_espera`/`makeWatchdog`/`waitSyncOverlay` **una sola vez** y guardarlos (`page.__qa = {...}`) permite que cada llamada siguiente abra con dos líneas: `const q = page.__qa; const pg = await q.connectCdp(page);` — ~40 líneas menos por `browser_run_code_unsafe`. Es **complementario** al truco de `window.__qaSrc`+`eval`: aquél cachea del lado *página* (skills DOM), éste del lado *Playwright/Node* (conexión y watchdog, que no pueden vivir en `window`). Validado durante todo el módulo PEDIDOS sin una sola recarga. `[gmp-20260730]`
  - 🔴 **CORRECCIÓN `[difranca-20260807]`: `page.__qa` NO siempre persiste — envolver SIEMPRE en fallback.** El truco funcionó 5 llamadas seguidas y a la 6.ª reventó con `TypeError: Cannot read properties of undefined (reading 'connectCdp')`: el harness MCP entregó **otra instancia de `page`** (ocurrió sin recarga de la app ni cambio de PID). Costó una llamada perdida y obligó a reinlinar el prelude entero. **Receta segura:** abrir cada `browser_run_code_unsafe` con `const q = page.__qa;` **y verificar `if (!q) { …reinlinar prelude y reasignar page.__qa… }`**, o directamente inlinar el prelude corto (connectCdp + makeWatchdog, ~15 líneas) en cada llamada. El ahorro de tokens sigue valiendo, pero **no como suposición**.
- **🔴 `setTimeout` NO EXISTE en el sandbox de `browser_run_code_unsafe`:** inlinar cualquier helper que lo use (`withTimeout`, `makeWatchdog` de `RUNTIME §11`) revienta con `ReferenceError: setTimeout is not defined`. **Usar `page.waitForTimeout(ms)`** como temporizador: `h.makeWatchdog({ moduleMs, page })` y `h.withTimeout(p, ms, label, page)`. Los helpers ya lo aceptan; en contexto Node (scripts/self-tests) el parámetro se omite. `[el_valle-20260728]`
- **🔴 Anti-patrón: NUNCA matchear botones de `ion-alert` por regex/`includes` — solo por igualdad EXACTA.** En el dirty-guard de DEPÓSITOS los botones son **[Guardar y salir · Salir sin guardar · Cancelar]**: un `/SALIR/i` (o `includes('salir')`) **matchea "Guardar y salir"** ⇒ se dispara un **guardado no deseado** en vez de descartar el form. Lo mismo aplica a "Aceptar" vs "No aceptar" y a cualquier alerta de 3 botones. Comparar siempre `btn.textContent.trim().toLowerCase() === '<texto exacto>'` y, si se prueban varias etiquetas, recorrerlas en **orden de preferencia explícito**. `h.clickAlertButton` usa `includes` por diseño (alertas de 1-2 botones): **no usarlo tal cual en alertas de 3 botones** — resolver las coords con comparación exacta y `pg.mouse.click`. `[latino_cosmetica-20260729]`
- **🔴 Botones de `ion-alert` etiquetados "OK" en lugar de "Aceptar" (El Yaque v1.0):** en el build EL YAQUE v1.0 (alipascua-20260804), los botones de alerts informativos usan **"OK"** (no "Aceptar"), confirmado en login (DM-LOG-002 aparece correctamente pero con etiqueta "OK"). Patrón ya observado en La Tortuga v6.6.18 y ferrenuestro. Los helpers `clickAlertButton` que buscan por `includes('aceptar')` fallan — usar comparación exacta `=== 'OK'` o probar ambas etiquetas en bucle con orden de preferencia. Afecta transversalmente a todos los módulos en este servidor/build. `[ferrenuestro-20260723][alipascua-20260804]`
- **`ion-alert.textContent` VACÍO en este build:** leer el mensaje por **`.alert-message`**, no por `textContent` del `ion-alert`. Además **los alerts se ACUMULAN** en el DOM (se observaron 7 nodos): filtrar SIEMPRE por `:not(.overlay-hidden)` **+** `offsetParent !== null` para quedarse con el activo. Botón de alerts informativos = **"OK"** (no "Aceptar") — consistente con La Tortuga. `[el_valle-20260728]`
- **🔴 `await alert.dismiss()` en BUCLE sobre TODOS los `ion-alert` CUELGA el `evaluate` (TIMEOUT del watchdog):** la receta de limpieza `document.querySelectorAll('ion-alert').forEach(a => a.dismiss())` es segura **solo si no se espera el resultado**. En cuanto se hace `for (const a of ...) { await a.dismiss(); }`, la promesa **nunca resuelve** para los alerts **ocultos/acumulados** de `<app-message>` — son `<ion-alert [isOpen]>` estáticos que jamás llegaron a presentarse, y su `dismiss()` se queda colgado ⇒ `TIMEOUT:<label>` y un cuelgue contra el techo del watchdog (§11). **Fix: hacer `dismiss()` SOLO sobre los alerts VISIBLES** (`:not(.overlay-hidden)` **+** `offsetParent !== null`), o cerrarlos por click en su botón. Complementa la prohibición de `.remove()`: **ni `.remove()` ni `await dismiss()` masivo**. `[alipascua-20260804]`
- **🔴 Las tabs (`ion-segment-button`) se rotulan en MAYÚSCULAS por CSS — matchearlas por texto exacto en mayúsculas FALLA:** el `textContent` real es `General`/`Inventario`/`Resumen`/`Adjuntos`, y el `INVENTARIO` que se lee en pantalla (y en `innerText`) es `text-transform: uppercase`. Un `.find(s => s.textContent.trim() === 'INVENTARIO')` devuelve `undefined` y se lee como "la tab no existe". **Comparar siempre con `.toLowerCase()`.** Aplica a todo módulo con tabs. `[alipascua-20260804]`
- **⚠ `maxlength` de los campos Comentario NO viene de la VG `longitudComentario`:** en el form de inventario el límite lo fija la constante de producto `TEXT_COMMENT_MAX_LENGTH` (`src/app/utils/text-comment-field.constants.ts`) vía `applyTextCommentMaxLength` — **120** en la APK El Yaque v1.0, **255** en el fuente actual. Medido: 200 caracteres tecleados → 120 aceptados. Las VGs `requiredComment`/`longitudComentario` gobiernan **cobros** (`requiredCommentOrder` → pedidos), no inventarios. **No reportarlo como incumplimiento de VG**; el campo tampoco es obligatorio ahí (Guardar/Enviar habilitan con el comentario vacío). `[alipascua-20260804]`
- **ⓘ `pending_transactions` tiene solo 3 columnas** (`co_transaction`, `id_transaction`, `type`) — un `SELECT` con nombres inventados aborta toda la transacción `sqlitePlugin`. Descubrir con `SELECT name FROM pragma_table_info('<tabla>')`. `[alipascua-20260804]`
- **⚠ Antes de culpar a los `ion-backdrop`, medí: pueden no ser huérfanos.** Se observaron **14 `ion-backdrop`** acumulados y el filtro `!b.closest('ion-alert') && !b.closest('ion-modal')` removió **0** — todos pertenecían a overlays legítimos. El síntoma real ("el modal de cliente no abre") era **timing**: el modal aún estaba cerrado al leer los ítems, y un 2.º click en `ion-input#clienteSelect` lo abrió normalmente. **Diagnosticar siempre con `document.elementFromPoint(x,y)` sobre el target** antes de asumir intercepción. `[alipascua-20260804]`
- **⚠ Rect `0×0` en ítems de un modal CERRADO (hermano del `fechaAtras` oculto):** con `ion-modal.show-modal=false` los `ion-item` del modal **siguen en el DOM** y `getBoundingClientRect()` devuelve `{0,0,0,0}` ⇒ `pg.mouse.click(0,0)` cae al vacío sin excepción. **Validar `width>0` ADEMÁS de `0<y<innerHeight`** antes de clickear cualquier ítem de modal. `[alipascua-20260804]`
  - ✅ **FIX DETERMINISTA (sube el diagnóstico a receta) — esperar `show-modal` con `waitForFunction` ANTES de leer el rect, en vez de "clickear de nuevo y ver":** el ítem correcto se encuentra por `textContent` y aun así devuelve `{cx:0, cy:0, w:0}` **porque el click en `ion-input#clienteSelect` no llegó a abrir el modal** — se lee como "el cliente no existe" o "el click no selecciona". Cierra el quirk de timing de `[alipascua-20260804]` ("el síntoma real era timing; un 2.º click lo abrió"): en vez de reintentar a ciegas, **bloquear hasta que el modal esté realmente abierto y poblado**:
    ```js
    await pg.mouse.click(x, y, {delay:150});                 // ion-input#clienteSelect
    await pg.waitForFunction(() => { const m = document.querySelector('#clienteSelectModal');
      return m && m.classList.contains('show-modal') && m.querySelectorAll('ion-item').length > 0; }, {timeout:15000});
    // recién ahora: paginar → scrollIntoView → esperar ~900 ms → RE-LEER el rect → validar centro en viewport → click
    ```
    Resolvió a la 1.ª tras un intento perdido por esta causa exacta. **Regla: `width>0` es el filtro; `waitForFunction` sobre `show-modal` es la prevención.** `[grupo_fiel-20260817]`
- **⚠ Las etiquetas de botón de `ion-alert` son MIXTAS dentro del MISMO build:** en El Yaque v1.0 los alerts **informativos** usan **"OK"**, pero el de **confirmación de envío** es `[Cancelar/"Aceptar"]` y el de **borrado** es `[Cancelar/"Eliminar"]`. No basta con "este build usa OK": recorrer las etiquetas por **igualdad exacta** en un orden de preferencia explícito (`aceptar → ok → eliminar`), nunca por `includes`/regex (ver anti-patrón de 3 botones). `[alipascua-20260804]`
  - 🔴 **El reparto de etiquetas NO es estable entre clientes del mismo build — no memorizar "guardado usa Aceptar":** en el_palmar (v1.0/db19 Isla Coche) **guardado y borrado usan `OK`**, justo al revés de lo anotado en alipascua; el único `Aceptar` del módulo CLIENTES es el **1.er paso del confirm de envío** (`[Cancelar, Aceptar]`), y los pasos 2-3 vuelven a `OK`. ⇒ **la etiqueta es un dato a LEER, no a predecir.** Receta que resolvió los 5 alerts del módulo sin un reintento: tomar el alert visible (`:not(.overlay-hidden)` + `offsetParent!==null`, quedarse con el **último**), leer `.alert-title`/`.alert-message`, y recorrer `['Aceptar','OK','Eliminar']` por **igualdad exacta case-insensitive** filtrando `width>0`; devolver coords y clickear con `pg.mouse.click`. Como subproducto queda registrada la etiqueta real de cada alerta, que es lo que alimenta el perfil. `[el_palmar-20260805]`
    - 🔴 **Y tampoco es estable entre MÓDULOS del mismo cliente** — reparto real medido en los 6 módulos de el_palmar: CLIENTES borrado `[OK]` · DEVOLUCIONES borrado `[Cancelar, **Eliminar**]` · VISITAS borrado `[CANCELAR, **Aceptar**]` · INVENTARIOS guardar `[Cancelar, **Aceptar**]` pero CLIENTES guardar `[**OK**]` · DEPÓSITOS guardado `[**Aceptar**]`. Hasta los **títulos** son inconsistentes dentro de un mismo módulo (`Denario Inventario` / `Denario Inventarios` / `Denario Premium`). ⇒ **la regla operativa definitiva es: LISTAR los botones del alert activo y clickear por igualdad exacta**; nunca predecir la etiqueta ni memorizarla por build, por cliente ni por módulo. Recorrer `['Aceptar','OK','Eliminar']` filtrando `width>0` resolvió los **~25 alerts de la corrida sin un solo reintento**. `[el_palmar-20260805]`
- **⚠ `JSON.stringify` de un componente/servicio Angular revienta con "Converting circular structure to JSON" al cruzar el puente Playwright:** los servicios inyectados traen `Subscription._parentage`. El `pg.evaluate` serializa el valor de retorno automáticamente ⇒ devolver `comp.returnLogic` (o cualquier objeto con servicios) falla. **Fix: serializar DENTRO del `evaluate` y devolver un string ya recortado** (`return JSON.stringify({...campos planos})`). `[alipascua-20260804]`
  - ⚠ **Caso concreto que tumba el `evaluate`: `comp.returnLogic` de DEVOLUCIONES.** `returnValid` / `returnValidToSave` / `returnValidToSend` **no son booleanos: son `Subject` de RxJS** — el nombre engaña. Serializarlos da `Converting circular structure to JSON`. **Oráculo de validez alternativo: los `disabled` de `.imagenGuardar`/`.imagenEnviar`**, o serializar solo `constructor.name`. `[el_palmar-20260805]`
- **⚠ `ng.getComponent(<componente de tab>)` revienta al salir de ese tab:** `Error: Expecting instance of DOM Element` cuando el componente se desmontó (ej. `devolucion-general` al navegar a la lista de familias/productos). **Anclar el `getComponent` al componente RAÍZ del módulo** (`app-devoluciones`, `app-cobros`…), que sí persiste, y llegar al modelo por sus propiedades (`comp.returnLogic`). `[alipascua-20260804]`
- **`browser_run_code_unsafe` requiere firma `async (page) => { ... }`:** el harness MCP envuelve el código como función y la invoca con `page`. Sentencias top-level con `const`/`return` o IIFE dan `SyntaxError` / `__fn__ is not a function`. Escribir siempre el cuerpo como arrow async que recibe `page`. `[dth-2612]`
- **Captura de payload por hook `CapacitorHttp`/`nativePromise` — cobertura por build:** en el build refactorizado El Yaque de ferrenuestro el hook SÍ capturó los POST de **order** (`orderservice/order`, cabecera+detalle+unidad), **return** (`returnservice/return`, cabecera+detalles), **visit** (`visitservice/visit`) y **potentialclient** — contradice parcialmente `reference_qa_payload_capture_gap` (que reportaba order/collection NO capturables). ⚠ **collection** SÍ siguió sin capturarse (0 POST `collectservice`, coherente con 0 cobros enviados por adjunto obligatorio) y **client_stock/deposit** no se interceptaron (registro despachado con sync diferida). Útil como cotejo BD cuando `sqlite3` local no está disponible en el device. Revisar si el gap era específico de otro build. `[ferrenuestro-2026-07-07]`
- **[dm-electronica amplía cobertura]** en el build El Yaque v6.6.18 de dm-electronica el hook `nativePromise` capturó **order**, **return**, **visit**, **potentialclient** Y **client_stock** (`clientstockservice/clientstock`, cabecera+detail+detail_unit) — client_stock SÍ se interceptó aquí (a diferencia de ferrenuestro, donde la sync diferida lo perdió; en dm-electronica la sync es inmediata). ⚠ **collection** siguió sin capturarse pero por una causa distinta: la app **crasheó durante el POST del cobro** (`collectservice`) — el POST llegó a la nube (id_collection=5 persistió) pero el hook pre-crash se perdió al recargar la app. **deposit** N/A (sin depósito creado). Confirma que la cobertura del hook depende del BUILD (ampliar `reference_qa_payload_capture_gap`). `[dm-electronica-20260713]`
- **⚠ `window.ng` disponible/indisponible NO es constante en el build refactorizado El Yaque:** jerez y ferrenuestro (El Yaque) tienen `window.ng=false` (sin debug Angular → helpers con fallback `window.ng` inoperantes), PERO **dm-electronica (El Yaque v6.6.18) tiene `window.ng=TRUE`** — `openNuevoCobro`/`openDocumentDetail`/`comp.nuevoCobro(0)` con fallback `window.ng` SÍ operan. ✅ **[2ª confirmación] latino_cosmetica (Isla La Tortuga v6.6.18) también tiene `window.ng=TRUE`** — conducción por componentes Angular fiable (fue NECESARIA en DEVOLUCIONES: los modales de cliente/factura no aceptan clicks). No asumir `window.ng=false` por servidor/build; **probar `!!window.ng` al inicio de la corrida** y elegir la ruta de conducción (fallback Angular vs solo clicks reales) según el resultado. `[dm-electronica-20260713][latino_cosmetica-20260714]`
- **⚠ El par (servidor, `window.ng`, inmediatez de sync) sigue al BUILD/SERVIDOR cargado, NO al slug de cliente — prueba fuerte por MIGRACIÓN:** **el MISMO cliente ferrenuestro cambió de comportamiento al re-apuntar el device de Isla Coche → La Tortuga v6.6.18.** En Isla Coche (`[ferrenuestro-2026-07-07]`): `window.ng=false`, sync DIFERIDA ~3min, alertas informativas "Aceptar", depósito 2 alertas. En La Tortuga (`[ferrenuestro-20260723]`, `ws_url=denariolatortuga.ddns.net:8081`): **`window.ng=TRUE`, sync INMEDIATA, alertas "OK", depósito 3 alertas.** ⇒ estas propiedades NO son atributos estables del cliente; **probar `!!window.ng` SIEMPRE al inicio** y no fiarse del `window.ng`/`ws_url` cacheado en el YAML si el device pudo re-apuntarse. Corolario práctico: `window.ng=TRUE` **destrabó** conducción que estaba BLOCKED en Isla Coche — trash del pedido (DM-PED-026) y retención por documento (DM-COB-041/042/029) pasaron a PASS. `[ferrenuestro-2026-07-07][ferrenuestro-20260723]`
- **⚠ Crash de app durante POST del cobro + recuperación (2 builds confirmados):** la app se cierra (proceso muerto, foco→launcher) tras aceptar "El Cobro será enviado", **después** de que el POST llegó a la nube (dato persistió). Confirmado en **dm-electronica** (El Yaque, id_collection=5) y **latino_cosmetica** (La Tortuga, el cobro quedó Guardado; el **reintento desde el Guardado completó sin crash**, id 24). Recuperación: relanzar (`monkey -p com.kiberno.denarioPremiumPro ... LAUNCHER`) + re-map `adb forward tcp:9220 localabstract:webview_devtools_remote_<nuevoPID>` + reinstalar bundle/captura; el auto-login (Recordar Usuario) recupera a HOME solo. ⚠ **Estrategia:** si el 1er Enviar crashea, reintentar el envío **desde el cobro Guardado** (no recrear). Riesgo de estabilidad a vigilar — reproducido en 2 servidores v6.6.18 distintos. `[dm-electronica-20260713][latino_cosmetica-20260714]`
- **Captura de `collection` — SÍ es posible tras el crash (latino_cosmetica):** contra la nota previa "collection nunca se captura", en latino_cosmetica el hook `nativePromise` capturó el POST `collectionservice/collection` en el **reintento exitoso** desde el Guardado (el POST pre-crash del 1er intento sí se perdió, como en dm-electronica). La cobertura del hook para collection depende de que el envío no crashee a mitad — reintentar desde Guardado da una 2ª oportunidad de captura. `[latino_cosmetica-20260714]`
- **🔴🔴 NUNCA remover nodos `ion-alert` del DOM — se destruye el sistema de alerts de TODA la app, sin recuperación:** en este build los alerts NO los crea un `AlertController` imperativo, sino que son **`<ion-alert [isOpen]>` estáticos declarados en el template de `<app-message>`** (hijo directo de `ion-app`, junto a `ion-router-outlet` y `app-calculator`). Un "cleanup" preventivo tipo `document.querySelectorAll('ion-alert.overlay-hidden').forEach(a => a.remove())` —receta que arrastraba la nota de alerts residuales de DEVOLUCIONES— **los borra para siempre**: a partir de ahí la app sigue disparando sus avisos (el componente fija `alertMessageOpen=true` + `mensaje`), pero **no se renderiza nada**, `messageService.showingMessage` queda `true` bloqueado y **ningún alert vuelve a verse en ningún módulo**. **No se recupera** con `ng.applyChanges` ni toggleando `alertMessageOpen` (Angular ya no tiene el nodo que reinsertar). Se observó como "Guardar no muestra alert" y casi se reporta un FAIL falso. **Regla: para descartar overlays usar SIEMPRE `await a.dismiss()`; `.remove()` está PROHIBIDO sobre `ion-alert`, `ion-modal` y `app-message`.** Para `ion-backdrop` huérfanos, remover sólo los que NO estén dentro de un `ion-alert`/`ion-modal`. `[gmp-20260730]`
  - **Oráculo de alert independiente del render:** `window.ng.getComponent(document.querySelector('app-message'))` expone `header`, `mensaje`, `alertMessageOpen`, `isAlertOpen`, `alertMessageOpen2`, `alertCustomBtnOpen` y `messageService.showingMessage`. Permite **confirmar qué aviso disparó la app** aunque el DOM del alert falte, y distinguir "la app no avisó" (defecto) de "el alert no se renderizó" (infra). `[gmp-20260730]`
    - ✅ **3.er uso, el más valioso: distinguir "el click no llegó" de "la app no avisó".** En DEPÓSITOS `app-message` conservaba `mensaje:"¡EL Inventario se borro con exito!"` (del módulo **anterior**) con `alertMessageOpen=false` tras pulsar Enviar ⇒ prueba de que **el click nunca se registró**, no de que la app fallara. Evitó un FAIL falso en DM-DEP-017. **Leerlo ANTES de reintentar y antes de escribir cualquier FAIL de "no pasó nada".** `[el_palmar-20260805]`
- **🔴 `pg.reload()` NO recupera la app en este build — deja el `ion-router-outlet` VACÍO:** tras `pg.reload()` el WebView bootea (vuelven `app-message` y sus `ion-alert`), pero la app **nunca rutea**: `ion-router-outlet` con **0 hijos**, `location.href='http://localhost/'`, pantalla en blanco, y no se destraba esperando (probado >30 s). El arranque real depende de la secuencia nativa de Capacitor, que un reload de página no reproduce. **Recuperación válida (única probada):** `adb shell am force-stop com.kiberno.denarioPremiumPro` → `adb shell monkey -p ... LAUNCHER 1` → leer el PID nuevo (`adb shell cat /proc/net/unix | grep -o "webview_devtools_remote_[0-9]*"`) → `adb forward --remove tcp:9220` + `adb forward tcp:9220 localabstract:webview_devtools_remote_<nuevoPID>` → `connectCdp`. El auto-login (Recordar Usuario) pasa solo por `app-synchronization` y llega a HOME en ~30 s; **lo guardado en BD local sobrevive**. Tras el relanzamiento `window.__qaH` se pierde: ahí sí corresponde reinstalar el bundle + `installPayloadCapture` (es un WebView limpio, no apila wrappers). `[gmp-20260730]`
- **⚠ Cuelgues de CDP alargan el wall-clock de la corrida — candidato a watchdog/fail-fast (nota para PROPUESTA):** en `[ferrenuestro-20260723]` el wall-clock total fue **~15.7h** dominado por **2 cuelgues de CDP** (módulo COBROS ~2.7h, módulo PRODUCTOS ~9.9h) — no por volumen de casos (cobros 34 casos / productos 10). Un cuelgue de la conexión CDP (`browser_run_code_unsafe` sin respuesta) no se detecta ni corta solo → el módulo queda colgado hasta el techo de la sesión. **Candidato: watchdog/fail-fast de CDP** (timeout por `browser_run_code_unsafe` + reintento acotado de `connectOverCDP`, y abortar el módulo tras N cuelgues en vez de esperar). Registrar en `PROPUESTAS-CAMBIOS.md`. `[ferrenuestro-20260723]`
- **🔴 `document.querySelectorAll('ion-input')` ARRASTRA LOS CAMPOS DE `app-login` — filtrar SIEMPRE por visibilidad:** `app-login` queda montado tras el login, así que un `querySelectorAll('ion-input')` global en CUALQUIER formulario devuelve además los controles `formcontrolname="login"` y `formcontrolname="password"` **con sus valores reales**. Impacto doble: (a) un conteo de "cuántos inputs tiene el form" da 2 de más y se lee como campo fantasma; (b) 🔴 **las credenciales aparecen en el retorno del `evaluate`** — nunca volcarlas al reporte, al ledger ni al chat. **Fix: filtrar `offsetParent!==null` (+ `width>0`) o anclar el `querySelectorAll` al componente del form** (`document.querySelector('app-client-new-potential-client').querySelectorAll('ion-input')`). `[difranca-20260807]`
- **⚠ `userCanSaveGPS=false` NO impide que un formulario envíe coordenada — la VG tiene alcance por MÓDULO:** en difranca la VG está en `false` (documentada como "las visitas NO guardan coordenadas") y aun así el payload de **cliente potencial** viaja con `coordenada:"11.0490573,-63.8649905"` (`coordenadaClient:null`), sin alert ni exigencia de GPS. Mismo comportamiento que en el_palmar, donde la VG estaba en el otro sentido. ⇒ **No leer una coordenada presente en CLIENTES como incumplimiento de la VG, ni extrapolar el veredicto de un módulo a otro.** Verificar el alcance en el módulo que se está evaluando. `[el_palmar-20260805][difranca-20260807]`
- **🔴 `client.nu_balance` de la NUBE no es el saldo que muestra la app — no usarlo como oráculo:** para CAR755/DDHP_A12 la nube trae `nu_balance=7.410.782,79` mientras la app muestra `62.523,87`. La app **no lee ese campo**: suma los `nuBalance` de los documentos que tiene sincronizados (sync parcial por vendedor). Cotejar contra **Σ documentos**, nunca contra `client.nu_balance`, o se levanta un FAIL falso de "el saldo no cuadra". `[difranca-20260807]`
- **🔴🔴 SEGURIDAD — `browser_run_code_unsafe` DEVUELVE el código cargado en su respuesta: pasar credenciales por `filename` NO las oculta.** El MCP incluye el cuerpo del archivo en el transcript, y además el proceso de Playwright **no expone `require` ni `import` dinámico** (`fs` inaccesible), así que tampoco hay forma de leerlas en runtime sin materializarlas. ⇒ **Política explícita: las credenciales se tipean con `browser_type` / `pg.keyboard.type` sobre los inputs (localizados por el árbol de accesibilidad), NUNCA embebidas en el código ni en un archivo cargado.** Se suma al riesgo ya documentado de que `querySelectorAll('ion-input')` arrastre los campos de `app-login` **con sus valores reales**: nunca volcar el retorno crudo de ese `evaluate` al reporte, al ledger ni al chat. `[difranca-20260807]`
- **🔴 `pg.evaluate` corre FUERA de NgZone ⇒ paginar el modelo NO re-renderiza el DOM. Cerrar siempre con `ng.applyChanges(comp)`.** Cualquier llamada programática a un método del componente que cambie el modelo (`onIonInfinite`, `setClientfromSelector`, `selectInvoice`…) actualiza el array pero deja el DOM viejo: `sc.clientes` pasó de 50→100 y `querySelectorAll('ion-modal.show-modal ion-item')` **siguió en 50**, con el ítem objetivo devolviendo `null` — se lee como **"el registro no existe"**. **Fix: `window.ng.applyChanges(comp)` + esperar ~1,2 s.** Completa las recetas de paginación de `[alipascua-20260804]` (modal de clientes de PEDIDOS) y `[el_palmar-20260805]`, que estaban incompletas. ⚠ Además, **cortar el bucle de paginación por `sc.scrollDisable===true`, nunca por "el `length` dejó de crecer"**, y darle **~800-900 ms por vuelta** (con 350 ms corta antes de tiempo y devuelve 50 de 148). `[difranca-20260807]`
- **⚠ La INMEDIATEZ DEL SYNC es por MÓDULO, no solo por servidor/build.** `[ferrenuestro-2026-07-07][ferrenuestro-20260723]` establecieron que el par (servidor, `window.ng`, inmediatez) sigue al build/servidor; **`[difranca-20260807]` lo acota: dentro de la MISMA corrida, mismo device y mismo servidor, PEDIDOS sincronizó al instante y DEVOLUCIONES tardó 5-12 min** (y en la misma playa El Yaque, dm-electronica y alipascua tuvieron devoluciones inmediatas). ⇒ **no extrapolar la latencia de un módulo a otro ni de un cliente a otro de la misma playa**; ampliar el poll o hacer un **2.º baseline-diff al cierre** por módulo, y **nunca marcar BD-SAVED como no-persistencia** sin ese 2.º pase. `[difranca-20260807]`
- **⚠ `st_*` no siempre es traducible por catálogo — y `transaction_statuses` puede no tener filas para un tipo.** La fuente confiable para traducir un `st_*` es **`transaction_statuses` con `co_status='env'`** (`[el_palmar-20260805]`), **pero no está garantizada**: en difranca la tabla cubre `cob`/`dev`/`ped`/`inv` y **no tiene ninguna fila `vis`**. Es además una tabla de **historial apenas poblada** (en difranca: 2 filas para 795 devoluciones y 1.383 para 15.517 pedidos) ⇒ **no sirve como oráculo de "cuántos hay en tal estado"**. Cuando falte, caer al campo propio del módulo (`visit.st_visit=2` + `is_visited`, `order.st_order`, …) y **al rotulado por constantes del componente**, que es lo que la app usa realmente. `[difranca-20260807]`
- **⚠ Localizar un registro por su código en un `ion-item`: capturar hasta la etiqueta siguiente, NUNCA usar lookahead.** El `textContent` puede correr pegado (`Código: ACPDT300Precio: 43.681,39 BSD`), así que `Código: <cod>(?![0-9A-Za-z])` **falla el 100 % de las veces** en PEDIDOS; y sin anclaje, `Código: ACBA300U` matchea también `ACBA300UX12` en DEVOLUCIONES. **Receta única que sirve en los dos: `/Código:\s*([A-Za-z0-9-]+?)\s*Precio/` (o hasta la etiqueta que siga) + igualdad exacta del grupo capturado.** `[difranca-20260807]`
- **🔑 `sqlite_sequence` como oráculo de IDEMPOTENCIA (uso nuevo):** además de distinguir "se borró" de "nunca llegó", comparar `seq` **antes/después de un 2.º Guardar** distingue **UPDATE** (seq estable) de **delete+reinsert** (seq crece). Cerró el veredicto de `VIS-GUARDAR-NO-IDEMPOTENTE` sin ambigüedad. Aplicable a cualquier módulo con Guardar re-ejecutable. `[difranca-20260807]`
  - ✅ **Oráculo barato de saldos, sin recorrer la tabla paginada:** `ng.getComponent(document.querySelector('app-client-detail'))` expone **`allDocuments`** (array COMPLETO, no paginado — 149 docs mientras la tabla mostraba "1-30 de 149"), más `saldoFuerte` y `saldoLocal`. Verificar `Σ allDocuments.nuBalance === saldoFuerte` y `saldoLocal === saldoFuerte × tasa`. Es lo que descarta en una sola llamada el defecto de etiquetas de saldo cruzadas de `[gmp-20260730]`. `[difranca-20260807]`
- **🔴 Un `ion-select` `disabled` con `value=null` NO significa "sin asignar" — y su shadowRoot MIENTE: rotula "Seleccione...".** En el form **nuevo** de cliente potencial de grupo_fiel (1 sola empresa) el `ion-select[formcontrolname="idEnterprise"]` llega `disabled=true`, **`value=null`** y con el shadowRoot rotulando literalmente **"Seleccione..."**. Los tres síntomas juntos se leen como "la empresa no está asignada" y tientan a forzar `s.value=1` — **que es exactamente lo que NO hay que hacer**. Prueba de que ya estaba resuelto internamente: (a) con los 8 `ion-input` llenos y el select así, `imagenGuardar`/`imagenEnviar` pasaron de `disabled=true` a `false` **en el mismo tick** ⇒ el control está fuera de la validación; (b) al **reabrir** el registro Guardado el MISMO select devuelve **`value=1`** y rotula **"GRUPO FIEL, S.A. (GR"**; (c) el payload viajó con `idEnterprise:1` / `coEnterprise:"00001"` y la fila de nube los trae idénticos. ⇒ **Con `disabled=true`: no tocar nada, y NO juzgar por `value` ni por el rótulo del shadowRoot.** Los únicos oráculos válidos son el **par botones antes/después** y el **payload**. Matiza `[latino_cosmetica-20260729]`, que había visto el `disabled` pero no había medido `value` ni el rótulo. `[grupo_fiel-20260817]`
  - ✅ **En CLIENTES, el discriminador de la variante de `idEnterprise` es el Nº DE EMPRESAS — 4 corridas coherentes:** `1 empresa ⇒ disabled=true`, auto-asignada, fuera de la validación (`[latino_cosmetica-20260729]`, `[grupo_fiel-20260817]`); `2+ empresas ⇒ disabled=false, value=null, ng-invalid`, **obligatoria** (`[el_palmar-20260805]` con 2, `[difranca-20260807]` con 3). El corolario de difranca ("la variante la fija el FORMULARIO") sigue valiendo **entre módulos**; **dentro de CLIENTES** el predictor es el nº de empresas. Igual la regla operativa no cambia: **leer `sel.disabled` SIEMPRE antes de decidir.** `[grupo_fiel-20260817]`
- **🔴 Un `ion-select` puede abrir un `ion-alert` con radios en vez de un `ion-popover` — `querySelectorAll('ion-popover')` devuelve `[]` y se lee como "el selector no abre".** En PEDIDOS/grupo_fiel el selector **Descuento Global** (Tab Total) abrió un **`ion-alert`** cuyos `button` son `[SIN DESCUENTO, DESC 7 TEST, Cancelar, Aceptar]`: las **opciones y los botones de acción conviven en la misma lista**, y hacen falta **2 clicks** (opción → `Aceptar`). El mismo build usa `ion-popover` con `ion-item` para el selector de **Tipo de pedido** ⇒ **la variante es por control, no por build: probar popover y, si da `[]`, leer el alert activo.** ⚠ Es un alert de 4 botones: matchear por `includes` es doblemente peligroso (`/desc/i` matchea "SIN DESCUENTO"; ver el anti-patrón de 3 botones más arriba) — **igualdad exacta case-insensitive, siempre**. `[grupo_fiel-20260817]`
  - 🔴 **2.ª confirmación en COBROS, y con dos agravantes nuevos.** El `ion-select` **Moneda Documento** (Tab Documentos) abrió el mismo `ion-alert` con radios, botones **`[Moneda, BS, USD, Cancel, OK]`**. Los dos agravantes:
    1. **La asignación programática NO surte efecto y falla EN SILENCIO.** `s.value='USD'` + `ionChange` dejó el shadowRoot en `""` y la lista **sin filtrar** — se lee como *"el filtro de moneda está roto"* y casi produce un **FAIL falso**. La causa: el `value` real **no es un string, es un OBJETO** (`{idCurrencyEnterprise, idCurrency, coCurrency, localCurrency, hardCurrency, coEnterprise, idEnterprise}`), así que ningún string puede matchear. Con click real + alert, el filtro funcionó perfecto (BS→USD ⇒ la lista recargó a 0 documentos).
    2. 🔴 **Los botones de acción vienen en INGLÉS (`Cancel` / `OK`) mezclados con opciones en español.** Un recorrido de etiquetas `['Aceptar','OK','Eliminar']` salva este caso por el `OK`, pero **`['Aceptar','Cancelar']` no encuentra nada** y se lee como "el alert no tiene botón de confirmar". Suma `Cancel`/`OK` al repertorio de etiquetas a probar.
    ⇒ **Receta general para todo `ion-select` de este build: (a) leer el TIPO de `value` antes de intentar asignarlo — si es objeto, la vía programática está descartada; (b) click real y, si `querySelectorAll('ion-popover')` da `[]`, leer el `ion-alert` activo; (c) 2 clicks (opción → acción) por igualdad exacta.** `[grupo_fiel-20260817]`
  - 🔴 **3.ª confirmación en DEVOLUCIONES, y la prueba más limpia: LOS DOS OVERLAYS CONVIVEN EN EL MISMO FORMULARIO.** En `app-devolucion`, **Tipo de devolución** (cabecera, 3 opciones, `value` **number**) abre un **`ion-popover`** con `ion-item`+`ion-radio` y se resuelve con **1 click**; el **Motivo** del acordeón de producto (24 opciones, `value` number) abre un **`ion-alert` de 26 botones** = 24 opciones + **`Cancel`/`OK` en INGLÉS**, y necesita **2 clicks** (opción → acción). Mismo componente, mismo build, misma corrida ⇒ **la variante no la fija el build ni el formulario: la fija el CONTROL.** Queda descartado predecirla por cualquier vía: **probar `ion-popover` y, si da `[]`, leer el `ion-alert` activo** es la única receta segura. En el alert largo hay que **`scrollIntoView({block:'center'})` la opción y RE-LEER el rect** antes de clickear (24 botones no entran en el viewport de 744 px). `[grupo_fiel-20260817]`
- **🔴 HIGIENE DE NAMESPACE — una sola inicial YA NO ALCANZA: `__qaC` estaba tomado y devolvió las skills de otro agente.** La receta graduada "un namespace por módulo" (`__qaD`, `__qaI`, `__qaV`…) chocó en COBROS: `window.__qaC` ya existía —instalado por un agente anterior de la misma corrida— y, como el bundle abre con `if (window.__qaC) return`, la instalación se saltó **en silencio** y el agente corrió con skills ajenas. Síntoma: `TypeError: C.alertInfo is not a function` en la primera llamada, con una call perdida. **Fix: usar un sufijo de 3 letras del módulo (`__qaCOB`, `__qaDEV`, `__qaINV`…) en vez de una inicial, o verificar una firma propia (`if (window.__qaX && window.__qaX.miSkill)`) antes de confiar en el namespace.** Las iniciales son un espacio de nombres demasiado chico para 10 agentes. `[grupo_fiel-20260817]`
- **🔑 `sqlite_sequence` como PRUEBA NEGATIVA — la única forma de responder con certeza "¿quedó algo guardado?".** Tercer uso del oráculo (los dos previos: distinguir borrado-de-nunca-llegó `[el_palmar-20260805]` e idempotencia UPDATE-vs-reinsert `[difranca-20260807]`). En una corrida de **solo lectura**, leer `SELECT seq FROM sqlite_sequence WHERE name='<tabla>'` **al abrir y al cerrar** el módulo demuestra que no hubo **ningún** insert: en grupo_fiel/COBROS se midió `18 → 18` incluso después de llenar un formulario completo (cliente + documento + pago) y descartarlo con "Salir sin guardar". Es **mucho más fuerte que "la lista sigue igual"**, que solo prueba render y no descarta una fila local sin reflejo en UI. Usarlo siempre que haya que certificar que un módulo no persistió nada (auditoría, corridas de solo lectura, o después de un descarte accidental). ✅ **2.ª corrida:** en kron/COBROS `collections` 10→10, `collection_details` 7→7 y `collection_payments` 8→8 tras abrir 4 detalles y ejercer el buscador. `[grupo_fiel-20260817][kron-20260817]`
- **⚠ Antes de mapear las VGs de un formulario, comprobá que el form esté COMPLETO: los controles pueden aparecer recién al elegir el registro padre.** En el Tab General de PEDIDOS, con `hasClient=false` hay **1 solo `ion-select`** (Empresa) y **1 solo `ion-input`** (`#clienteSelect`); Moneda, Dirección, Tipo de pedido, Lista de precios, Condición de pago, `#tasa`, `#nuPurchase`, `#naResponsible` y `#txComment` **se materializan al seleccionar cliente**. Leer el mapa de VGs antes de eso devuelve un form casi vacío y se lee como "este cliente no tiene esos controles" ⇒ veredictos de VG equivocados sobre `multiCurrency`, `selectOrderType`, `userCanChangePriceList`… **Mapear SIEMPRE después de completar el campo que gobierna el resto.** Hermano del `lockSegments`/`idEnterprise`: el form incompleto es un síntoma, no una VG. `[grupo_fiel-20260817]`
- **🔴 ANTES de invocar un método de componente, LEER SU FIRMA con `String(comp.metodo)` — el nombre engaña y el error apunta al lugar equivocado.** En INVENTARIOS, `getOriginalIndex` **parece** recibir un índice (así se usa en casi todos los templates) pero su firma real es `getOriginalIndex(item){ return this.inventariosLogicService.itemListClientStocks.findIndex(s => s.coClientStock === item.coClientStock) }`: **recibe el ITEM**. Pasarle un número devuelve **`-1`**, y `beforeOpenStock(-1)` revienta con `Cannot read properties of undefined (reading 'stDelivery')` — un mensaje que apunta a `stDelivery` y **tienta a concluir que la app tiene un bug de estado**. Casi se reporta como defecto; no lo era. `String(c.metodo).slice(0,300)` cuesta una línea y resuelve la ambigüedad de una. Hermano de `returnValid` (que parece booleano y es un `Subject`, `[el_palmar-20260805]`): **en este código el nombre de un símbolo no permite inferir ni su tipo ni su firma.** `[grupo_fiel-20260817]`
- **✅ Los `<modulo>LogicService` del componente raíz son la fuente BARATA de las VGs del módulo — mejor que la nube y que el componente de tab.** `ng.getComponent(document.querySelector('app-inventarios')).inventariosLogicService` expone como **valores planos** `expirationBatch`, `suggestedOrderByDispatchAndReturn`, `userMustActivateGPS`, `disabledEnterprise`, `stockValid/ToSave/ToSend`, `cannotSendClientStock`, `disableSaveButton`. Permitió **confirmar `expirationBatch=true` sin depender del comportamiento** y cerrar el veredicto con dos evidencias independientes (validación observada + modelo). Acota la regla de `[el_palmar-20260805]` ("las dos fuentes fiables son el componente y la BD de nube"): el **logic service del módulo** es la más barata de las tres, y sobrevive al cambio de tab porque cuelga del componente **raíz**. Buscarlo por `Object.keys(comp)` → la clave que termina en `LogicService`. `[grupo_fiel-20260817]`
  - ⚠ **ACOTACIÓN — NO es universal: no todos los módulos exponen su logic service.** En kron funcionó en DEVOLUCIONES (`returnLogic.returnTypes` dio los 4 tipos activos sin tocar la UI ni la nube), pero **DEPÓSITOS no tiene ninguna clave `*LogicService`** en su componente raíz. ⇒ **es un atajo a intentar, no una fuente garantizada**: si `Object.keys(comp)` no devuelve la clave, caer al componente/nube sin gastar más intentos. `[kron-20260817]`
- **🔴 El HOST/PLAYA solo es observable por el HOOK DE PAYLOAD en el build v1.0/db19 — `localStorage` NO tiene ninguna clave de URL.** Las 18 claves de `localStorage` son `lastUpdate, sincronizarHome, user, createTables, globalConfiguration, coUser, connected, db_version, token, password, idUser, hardCurrency, login, localCurrency, connectionType, lastLoginImage, versionApp, recuerdame` — **ninguna es `WsUrl`/`servidor`/`host`**, y `globalConfiguration` tampoco (0 claves matchean `/url|host|serv|ip|port/i`). No gastar intentos buscándolo ahí. **Receta: en el hook de `Capacitor.nativePromise` guardar `options.url` de TODA llamada en un buffer aparte** (`window.__qaAllUrls`), no solo los POST filtrados por `/service\//` + `post`: los `syncservice/getsync` aparecen solos, sin crear ningún registro, así que **el host se tiene desde el primer segundo** en vez de depender de enviar una transacción. Corroborable contra el `WsUrl` activo de `../../claves.env` (líneas comentadas = playas inactivas). `[grupo_fiel-20260817]`
- 🔴 **ACOTACIÓN al `<modulo>LogicService`: NO es universal — comprobar `Object.keys` antes de contar con él.** La receta descubierta en inventarios (más arriba) **no existe en DEPÓSITOS**: `ng.getComponent(document.querySelector('app-depositos'))` expone `depositService, autoSend, services, synchronizationServices, messageService, subs` y **ninguna clave termina en `LogicService`**. Buscarla siempre por `Object.keys(comp).find(k=>/LogicService$/.test(k))` y, si no aparece, caer a los oráculos del DOM (los `disabled` de `.imagenGuardar`/`.imagenEnviar`, `required`/`value` de los controles) — que es lo que cerró las VGs de depósitos. `[grupo_fiel-20260817]`
- 🔴 **ACOTACIÓN a `sqlite_sequence`: solo sirve en tablas con AUTOINCREMENT.** `SELECT seq FROM sqlite_sequence WHERE name IN ('deposits','deposit_collects')` devuelve **`[]` aun teniendo filas en ambas** ⇒ esas tablas no son AUTOINCREMENT y el oráculo (idempotencia, prueba negativa, borrado-vs-nunca-llegó) **no aplica ahí**. Antes de usarlo, verificar que la tabla tenga fila en `sqlite_sequence`; si no, caer a `count(*)` + los `co_*` supervivientes. `[grupo_fiel-20260817]`
- ✅ **4.ª evidencia de "la variante de overlay la fija el CONTROL": en DEPÓSITOS los DOS `ion-select` abren `ion-popover` (1 click).** Moneda (2 opciones) y Banco (4 opciones) resolvieron con **1 click** cada uno, **sin un solo `ion-alert` de radios** — en la misma corrida y el mismo build en que pedidos, cobros y devoluciones sí abrían `ion-alert` con `Cancel`/`OK`. Refuerza la receta única: **probar `ion-popover` primero y, si da `[]`, leer el `ion-alert` activo.** ⚠ Los 3 `ion-select` de este form traen `value` **OBJETO** y **ninguno** tiene `formcontrolname` ⇒ la vía programática por string está descartada de entrada. `[grupo_fiel-20260817]`
- ⚠ **`app-message` NO es oráculo de "la app no avisó" cuando el alert SÍ se renderiza — solo de "el click no llegó".** Durante todo DEPÓSITOS `appMsg()` devolvió el mensaje del módulo **anterior** (`Denario Inventarios / "¡EL Inventario se borro con exito!"`, `alertMessageOpen=false`) mientras los 4 `ion-alert` reales del módulo (guardado + 3 pasos del envío) funcionaban perfecto y se clickearon sin un reintento. ⇒ el uso válido sigue siendo el de `[el_palmar-20260805]` (probar que un click **no** se registró), pero **leer siempre además el `ion-alert` visible**: un `app-message` viejo con `open=false` **no prueba** que la app no avisó. `[grupo_fiel-20260817]`
- ⚙ **GRADUABLE — 3.ª confirmación del `Agregar` de `ion-modal#eventModal` por shadowRoot, y ahora en 3 clientes / 2 servidores.** `b.shadowRoot.querySelector('button').click()` resolvió al **1.er intento** en las 3 visitas creadas en grupo_fiel (El Yaque), igual que en el_palmar (Isla Coche) y difranca (El Yaque). El anti-patrón ya está en `RUNTIME §3`; con esta 3.ª evidencia **corresponde bajarlo a helper** (`h.clickModalButton(pg, sel, texto)`) en vez de reinlinarlo por agente. `[el_palmar-20260805][difranca-20260807][grupo_fiel-20260817]`
- ✅ **5.ª evidencia de "la variante de overlay la fija el CONTROL": en VISITAS los DOS `ion-select` del `#eventModal` abren `ion-popover` (1 click).** Actividad (11 opciones) y Motivo (5 opciones) resolvieron con **1 click** cada uno vía `ion-item`+`ion-radio`, **sin un solo `ion-alert` de radios** — en la MISMA corrida y build en que pedidos, cobros y devoluciones sí abrían `ion-alert` con `Cancel`/`OK`, y alineado con depósitos (que también dio popover). ⚠ Sus `value` son **objetos** (`{idType,naType,requiredEvent,requiredSignature}` y `{idType,idMotive,naMotive}`) ⇒ la vía programática por string está descartada de entrada, pero **no hace falta**: el click real resuelve. Receta única confirmada: **probar `ion-popover` primero y, si da `[]`, leer el `ion-alert` activo.** `[grupo_fiel-20260817]`
- 🔴 **El tope del campo Comentario NO es estable ni dentro del MISMO servidor y build — leer las DOS fuentes.** En El Yaque v1.0/db19, difranca midió `maxlength=120` **con `comp.textCommentMaxLength` NO expuesto** en `app-visita`; grupo_fiel, mismo servidor y mismo build, trae `comp.textCommentMaxLength=**255**` **y** el `ion-input` del `#eventModal` con `maxlength="255"`. ⇒ **No memorizar el valor ni inferirlo del build**: leer `comp.textCommentMaxLength` y, si no está, el atributo `maxlength` del input — y no reportar el tope como incumplimiento de la VG `longitudComentario` (lo fija la constante `TEXT_COMMENT_MAX_LENGTH` de la APK). Refina `[difranca-20260807]` y `[alipascua-20260804]`. `[grupo_fiel-20260817]`
- ⚠ **ACOTACIÓN a `sqlite_sequence` (2.ª tabla sin AUTOINCREMENT): `visits` NO tiene fila, `incidences` SÍ.** `SELECT name,seq FROM sqlite_sequence WHERE name IN ('visits','incidences')` devuelve **solo `incidences`** aun con filas en ambas ⇒ en VISITAS la prueba negativa / idempotencia por `seq` hay que hacerla sobre **`incidences`**. Funcionó impecable para certificar que un "Salir sin guardar" sobre visita nueva **no insertó nada** (`seq` 2→2, más fuerte que "la lista sigue igual"). Suma a `deposits`/`deposit_collects` en la lista de tablas donde el oráculo no aplica. ✅ **2.ª confirmación del reparto `visits` NO / `incidences` SÍ** en otro servidor (kron, Isla Coche): `incidences.seq` 3→3 certificó DM-VIS-022. **Lista viva de tablas SIN fila en `sqlite_sequence`: `deposits`, `deposit_collects`, `visits`.** `[el_palmar-20260805][grupo_fiel-20260817][kron-20260817]`
- ⚠ **Con OTRO vendedor trabajando en el mismo tenant, el baseline-diff debe filtrarse por `id_user` — y hay que traer TODA fila nueva igual, para saber de quién es.** En VISITAS/grupo_fiel el otro usuario (`id_user=474`) consumió los ids **108, 109 y 110** mientras corría el módulo; el `max(id)` global saltó de 107 a 110 **sin que la corrida creara nada**. Un diff por `id > baseline_global` habría atribuido 3 registros ajenos. Receta: baseline y diff **filtrados por el vendedor propio**, más un diff global **solo para clasificar** las filas por `id_user`. Corolario: **el `max(id)+1` NO predice tu Ref** (se esperaba 108, salió **111**) — la Ref se lee de la 3.ª alerta de envío o de la BD, nunca se calcula. `[grupo_fiel-20260817]`

---
