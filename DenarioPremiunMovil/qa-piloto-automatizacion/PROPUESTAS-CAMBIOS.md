# Propuestas de cambios — QA Denario (estructura y memoria)

> **Para qué sirve:** registrar **antes** de aplicarlos los cambios a la *estructura* (RUNTIME, helpers, smoke, guiones, prompts, schema, CLAUDE) o a la *memoria* (module-selectors, YAMLs de cliente). Así ambos equipos ven, discuten y aprueban los cambios, y la rama compartida no diverge. Ver gobernanza en `MANUAL-CORRIDAS.md` §6.
>
> **Reportes de corrida NO van aquí** — esos son libres (carpetas únicas).

## Cómo usarlo
1. Antes de aplicar un cambio a estructura/memoria, agrega una fila en "Pendientes" con: fecha · quién · archivo(s) · qué · por qué.
2. Avísale al otro equipo (mensaje/llamada).
3. Cuando se aprueba e integra en la rama canónica, muévelo a "Aplicados" con la fecha de integración.

---

## Pendientes (propuestos, sin aprobar)

| Fecha | Quién | Archivo(s) | Cambio propuesto | Por qué |
|-------|-------|-----------|------------------|---------|
| 2026-06-17 | QA (Grecia) + Claude | `denario-cdp-helpers.js`, `smoke-cobros.md`, `module-selectors.md` (COBROS) | **Grupo 1 — Cobros:** abrir cobro por flujo UI real (`nuevoCobro(N)` + esperar `paymentMethodList>0`), NO `goToNuevoCobro`; selección de botón VG-aware; tabs/back reales; usar `mockCameraAdjunto` para ENVIAR (no SKIP) con fail-fast | El init programático dejaba `paymentMethodList` vacía → 277 tool-uses de atajos (cobros 83 min). Confirmado en fuente (`cobros-container.component.ts`: `loadPaymentMethods()` async). Baja a ~25 min + sube fidelidad + cobros llegan a la nube |
| 2026-06-17 | QA (Grecia) + Claude | `RUNTIME.md §10`, `automation/db/local-query.js` (nuevo), `smoke-*.md` (7) | **Grupo 2 — Oráculo BD v2:** nube con espera/reintento (sync asíncrono); diff de baseline (toda fila nueva); verificación **por items co_type-aware** (anticipo/retención/IGTF/IVA con su estructura); lectura de **BD local** (`adb run-as`, read-only) para ciclo SAVED→cola→SENT | Hoy se perdió el cobro id=14 (autosend asíncrono). Contemplar **todo movimiento** (no solo el esperado), línea por línea, incluido lo que no llega a la nube (adjunto/Guardados). Confirmado el mecanismo en `auto-send.service.ts` |
| 2026-06-17 | QA (Grecia) + Claude | `automation/clientes/alta-cliente.md` (nuevo) | **Guía de alta de cliente:** CSV `global_configuration`→`vgs`; consultas BD read-only→`modules.*` (pre-poblado); qué queda solo para corrida (divergencias UI). Tabla de fuentes CSV/BD/corrida | Que el próximo cliente no re-descubra el proceso; 1ª corrida −20–40% al pre-poblar desde BD en vez de discovery en vivo |
| 2026-06-17 | QA (Grecia) + Claude | (decisión) | **Descartar pruebas negativas dedicadas** (VG en false): se cubren con N/A + chequeo config-vs-BD del oráculo, sin navegar UI | El defecto `expirationBatch` (config false, UI lo mostró) se cazó **incidentalmente**, no con un caso dedicado. Casos negativos = relleno que gasta tiempo |
| 2026-06-19 | QA (Grecia) + Claude | `prompt-orquestador-smoke.md` (L226, INICIO Agente 1), `RUNTIME.md` (§2 fila S4), `denario-cdp-helpers.js` (JSDoc `fetchCreds`) | **Fix #1 — Alinear `fetchCreds` (precisión):** quitar `await h.fetchCreds()` del INICIO del Agente 1 Login y de la fila S4 de Skills; dejar solo el parseo inline de `secrets/qa-credentials.env` (ya es la regla en §1). Marcar `fetchCreds()` del helper `@deprecated` en contexto unsafe | S4 y el prompt del Agente 1 contradicen §1 → el login intenta `fetchCreds()` en `browser_run_code_unsafe` (sin `require`/`fs`), **falla y auto-corrige en CADA corrida**. −1 fallo determinista/corrida + cero ambigüedad |
| 2026-06-19 | QA (Grecia) + Claude | `automation/db/cotejo-payload.js` (L129 `equalScalar`), `automation/db/cotejo-bd.js` (L80 `equal`) | **Fix #2 — Tolerancia de montos (precisión):** `Number(na)===Number(nb)` → `Math.abs(Number(na)-Number(nb))<0.01` en ambos comparadores | Igualdad exacta de floats sobre montos recalculados (IGTF/conversión/retención) genera `BD-FIELD-MISMATCH` falsos por redondeo nube↔local. Baja ruido y sube confianza del oráculo en los campos más críticos (montos) |
| 2026-06-19 | QA (Grecia) + Claude | **nuevo** `automation/cdp/module-selectors/` (`_comunes.md` + 10 por módulo); punteros en `prompt-orquestador-smoke.md` (10 bloques LECTURA), `prompt-consolidar-hallazgos.md`, `RUNTIME.md` (§"Sí leer"/§8), `CLAUDE.md`, `README.md`, `MANUAL-CORRIDAS.md` | **Mejora #3 — Partir `module-selectors.md` (604 L) por módulo (~40-70 L c/u, tokens):** cada agente lee `_comunes.md` + su `{modulo}.md` en vez del monolito; el Agente 11 escribe al archivo del módulo | Cada agente relee casi todo el monolito → ~6.000 L de lectura/corrida. Split ahorra ~5.000 L/corrida y mantiene la memoria "afilada" (principio del MANUAL). Riesgo bajo (split puro, sin reescribir contenido) |
| 2026-06-19 | QA (Grecia) + Claude | **nuevo** `automation/cdp/helpers-inline.js`; `RUNTIME.md` (§1 bootstrap, §3 anti-patrón); `denario-cdp-helpers.js` (cabecera = fuente de autoría) | **Mejora #4 — Helpers como bundle auto-instalable (tokens):** IIFE que registra `window.__qaH` una vez vía `pg.evaluate`; skills puras-DOM devuelven coords y el agente hace `mouse.click`/`keyboard`. Sustituye el reinlinado verbatim de funciones por uso | Hoy cada uno de los 10 agentes lee 692 L de helpers e inlina funciones en cada `eval`. Instalar `__qaH` una vez recorta tokens y reduce drift. Requiere validar 1 corrida (separar puro-DOM vs acción-Playwright por skill) |
| 2026-06-19 | QA (Grecia) + Claude | **nuevo** `automation/db/resolve-run-context.js`; `prompt-orquestador-smoke.md` (Paso 0 + plantillas de agente); `smoke-cobros.md` (y resto smoke) | **Mejora #5 — Pre-vuelo de datos a los 7 transaccionales (tiempo):** resolver en Paso 0 emite `{RUN_DIR}run-context.json` (clientes/documentos/productos por módulo); los agentes lo leen y van directo. Generaliza el pre-vuelo que hoy solo tiene cobros; degrada al discovery actual si la BD no responde | La exploración en runtime es el cuello de tiempo (cobros 83→25 min al pre-resolver). Aditivo y no bloqueante (mismo blindaje del oráculo BD) |
| 2026-06-19 | QA (Grecia) + Claude | `RUNTIME.md` (§3, §4, §6); `prompt-orquestador-smoke.md` (formato reporte + consolidado) | **Mejora #6 — Techo de intentos + estado `BLOCKED` (tiempo+precisión):** regla universal "no insistir >2 intentos con un selector/flujo → marcar `⛔ BLOCKED` y seguir". Añadir BLOCKED al enum de resultado y a los conteos | Solo `ensureAdjunto` hace fail-fast hoy; cobros llegó a 277 tool-uses por insistir en atajos. `BLOCKED` separa "flaky de automatización" de FAIL real → preserva la precisión de la métrica |
| 2026-06-19 | QA (Grecia) + Claude | **nuevo** `automation/reports/aggregate.js`; `prompt-orquestador-smoke.md` (formato reporte); `RUNTIME.md` (§6) | **Mejora #8 — Ledger `{RUN_DIR}_results.jsonl` (medición):** 1 línea JSON por caso (run_id, modulo, caso, resultado, ms, intentos, bd) además del `.md`; `aggregate.js` calcula tendencia (flakiness/ms/módulo) | Los reportes markdown no son agregables → no se puede medir si las mejoras bajan tiempo/tokens ni detectar regresión del propio suite. Instrumento para "mejorar progresivamente" |
| 2026-06-19 | QA (Grecia) + Claude | **nuevo** `automation/orchestrator/run-smoke.mjs` *o* Workflow tool (decisión mayor) | **Estratégico #7 — Orquestación determinista (firmeza):** mover la secuencia (UI seq + BD background + consolidación) de prompt interpretado a harness con control de flujo explícito. Camino A: driver Node. Camino B: Workflow nativo (resumible con `resumeFromRunId`). Requiere opt-in y validar device-singleton (1 emulador → UI sigue secuencial) | "Firmeza": orden garantizado, resumibilidad si `adb forward` cae a mitad de corrida, resultados estructurados, sin re-leer 696 L de lógica cada corrida |
| 2026-07-23 | QA (Grecia) + Claude | `RUNTIME.md` (§ CDP), `denario-cdp-helpers.js` (`connectCdp`/wrapper `browser_run_code_unsafe`), `prompt-orquestador-smoke.md` (techo por módulo) | **#11 — Watchdog / fail-fast de CDP (tiempo):** en la corrida `[ferrenuestro-20260723]` el wall-clock fue **~15.7h** dominado por **2 cuelgues de CDP** (COBROS ~2.7h, PRODUCTOS ~9.9h), no por volumen de casos. Un `browser_run_code_unsafe` sin respuesta no se detecta ni corta solo → el módulo queda colgado hasta el techo de sesión. Propuesta: timeout por `browser_run_code_unsafe` + reintento acotado de `connectOverCDP`, y **abortar el módulo tras N cuelgues** (marcar BLOCKED) en vez de esperar indefinidamente. Añadir un techo de wall-clock por módulo al orquestador | Un cuelgue de CDP puede multiplicar por ~30× la duración de un módulo; hoy no hay corte automático. Baja el riesgo de corridas de horas por un hang, sin afectar módulos sanos |
| 2026-06-22 | QA (Grecia) + Claude | `automation/cdp/denario-cdp-helpers.js` (`installPayloadCapture`) *o* `automation/db/cotejo-payload.js` | **#10 — Calibrar captura de payload de Cobros:** el payload de COBROS en `_payloads.jsonl` es un **resumen** (`{caso, coCollection, coType, coClient, coCurrency, nuAmountTotal, nuAmountFinal, nuIgtf, docs[], pago{}}`), no la estructura anidada `{data:{collection:{collectionDetails[], collectionPayments[]}}}` que espera el motor. (a) ajustar `installPayloadCapture` para volcar la estructura completa en cobros **o** (b) que `cotejo-payload.js` acepte el formato resumen. Además: capturar 1 envío de anticipo (co_type 1) / retención (2) / IGTF (3) para calibrar su fieldMap | Corrida 20260622: cobros se coteja con `query.js` directo (equivalente, no campo-a-campo "puro" del motor). Es el único transaccional que aún no pasa por el motor |
| 2026-06-22 | QA (Grecia) + Claude | `prompt-orquestador-smoke.md` (Paso 0, pre-vuelo BD) | **#11 — Gate del GRANT BD en el Paso 0:** reemplazar el chequeo `SELECT 1` por uno que cuente tablas legibles (`count(*) FILTER (WHERE has_table_privilege('user_read', ...))` sobre `pg_tables`) y exigir `legibles == total`. Si no cuadra → informar "BD sin GRANT → transaccionales BD-N/A" y seguir (NO bloquear, el oráculo es aditivo) | `SELECT 1` pasa aunque no haya GRANT (no toca tablas). Corrida 20260622 arrancó con 0/182 legibles y el GRANT se aplicó contrarreloj. El gate evita arrancar creyendo que hay oráculo |
| 2026-06-19 | QA (Grecia) + Claude | `automation/db/cotejo-bd.js`, `RUNTIME.md` §10/§10.b, plantilla "Agente BD" del orquestador | **#9 — Motor de cotejo: payload por defecto, BD local como fallback:** `cotejo-payload.js` (payload) es el motor **principal** del cotejo campo-a-campo. `cotejo-bd.js` (BD local) se degrada a **fallback**: corre **solo** cuando un registro figura Enviado pero NO se capturó su payload. No correr ambos por defecto. `local-query.js` se mantiene aparte (verifica estado/ciclo: enviado/en cola/rechazado/duplicado) | Cierra la deuda de dos motores que se solapan. El payload es la evidencia más directa de "lo enviado" y no depende de `adb run-as`; la BD local queda como red de seguridad si la captura falla |

---

## Aplicados en copia local (2026-06-19 · autorización de la responsable QA · PENDIENTE integrar a maestra + avisar al equipo §6)

> Estos cambios ya están **escritos en esta copia de trabajo** pero **no** integrados a la maestra ni pusheados. Coordinar con el equipo antes de compartir (gobernanza §6). Revisar el `git diff` antes de cualquier commit.

| # | Estado | Archivos | Notas de aplicación |
|---|--------|----------|---------------------|
| #1 fetchCreds | ✅ aplicado | `prompt-orquestador-smoke.md` (INICIO Agente 1), `RUNTIME.md` (S4), `denario-cdp-helpers.js` (JSDoc) | Login ya no intenta `fetchCreds()` en contexto unsafe |
| #2 tolerancia montos | ✅ aplicado | `cotejo-payload.js`, `cotejo-bd.js` | `Math.abs(a-b)<0.01` en ambos comparadores; `node --check` OK |
| #3 split selectores | ✅ aplicado | **nuevo** `module-selectors/` (11 archivos) + punteros en orquestador (10), consolidar, RUNTIME, CLAUDE, README, MANUAL, alta-cliente, INTEGRACION, LEEME | Monolito `module-selectors.md` (604 L) borrado; split verificado (sin pérdida de contenido; secciones globales movidas a `_comunes.md`) |
| #4 bundle helpers | ✅ aplicado (aditivo) | **nuevo** `helpers-inline.js` + `RUNTIME.md` §1 | Camino preferente documentado; inlining queda como fallback (no rompe). **Validar en 1 corrida** antes de quitar el fallback |
| #5 pre-vuelo datos | ✅ aplicado (aditivo) | **nuevo** `resolve-run-context.js` + `prompt-orquestador-smoke.md` (Paso 0) | cobros completo; otros módulos → `null` (agente usa discovery). No bloqueante |
| #6 techo intentos + BLOCKED | ✅ aplicado | `RUNTIME.md` §3/§4/§6, `prompt-orquestador-smoke.md` (formatos) | Nuevo estado `⛔ BLOCKED` en enum y conteos |
| #8 ledger | ✅ aplicado | **nuevo** `aggregate.js` + `RUNTIME.md` §6, `prompt-orquestador-smoke.md` | `{RUN_DIR}_results.jsonl` por caso; `aggregate.js` mide tendencia |
| #7 orquestación determinista | ⏸ NO aplicado | — | Queda solo propuesto: requiere opt-in + validar device-singleton |

### 2026-06-22 · autorización de la responsable QA · post-mortem corrida insumar `20260622_115712`

| # | Estado | Archivos | Notas de aplicación |
|---|--------|----------|---------------------|
| BD-1 allowlist comandos BD | ✅ aplicado | **nuevo** `.claude/settings.json` | Allowlistea `query.js`/`cotejo-payload.js`/`resolve-run-context.js`/`local-query.js`/`aggregate.js` + lectura `automation/**` + temporal `automation/db/_tmp_*.json`. **Causa raíz** de que los agentes BD en background se auto-denegaran (no había settings → todo prompt → background no puede responder → auto-deny). Todo read-only sobre BD |
| BD-2 agente BD devuelve markdown | ✅ aplicado | `prompt-orquestador-smoke.md` (plantilla Agente BD + instrucciones orquestador 3b y nota paralelo) | El agente BD corre en paralelo (background) y **DEVUELVE** la sección "Verificación BD"; el orquestador (foreground) la **anexa** y borra el temporal. Conserva el paralelismo, elimina las intervenciones manuales que hubo en 20260622 |

---

## Aplicados (aprobados e integrados)

| Fecha integración | Quién propuso | Archivo(s) | Cambio | Notas |
|-------------------|---------------|-----------|--------|-------|
| 2026-06-11 | equipo | RUNTIME.md, prompt-orquestador-smoke.md, denario-cdp-helpers.js | Ruta del helper ahora **relativa** (`automation/cdp/denario-cdp-helpers.js`) en vez de absoluta → portable | Misma convención que RUNTIME/smoke (ya funcionaba). Sin impacto en tokens/tiempo. Docs de dev/build conservan rutas de ejemplo (ilustrativas) |
