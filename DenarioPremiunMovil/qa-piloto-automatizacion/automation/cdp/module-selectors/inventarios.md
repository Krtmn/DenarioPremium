> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo INVENTARIOS

### Identidad
- Ruta: `/inventarios` · Componente raíz: `app-inventarios`
- Botones home: INVENTARIO, BUSCAR
- Tabs nuevo: General / Inventario / Resumen / Adjuntos (4) · Enviado: General / Resumen / Adjuntos (3, sin Inventario)

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Cliente | `ion-input#clienteSelect` → modal (click real); buscar `input[placeholder="Clientes..."]` + type + click `.clear-search` + click `<p>` del nombre | `[ins-2606][ins-2610]` | mismo patrón Pedidos; selección por `<p>` del nombre (no centro del item) habilita las 4 tabs |
| Familias/categorías | Tab Inventario muestra grupos → click expande productos | `[gmp-2606][ins-2606]` | |
| Click producto (abre modal captura) | requiere **Pointer+Mouse combinado**: `pointerdown/pointerup`(PointerEvent) + `click`(MouseEvent) sobre el `ion-item` + `mouse.click` en coords reales | `[gmp-2611]` | `mouse.click` simple NO abre el `inventory-type-stocks-modal`; el `ion-item` es `button=""` (chevron-forward) pero el zone listener AOT no engancha con click simple |
| Modal captura | clase `inventory-type-stocks-modal` en `ion-modal` | `[ins-2606][gmp-2606]` | `querySelector('inventory-type-stocks-modal')` = null (es clase, no custom element) |
| Inputs modal captura | Cantidad `placeholder="Ingrese cantidad"` (type=number) + Lote `placeholder="Ingrese lote"` (type=text); ids `ion-input-NNN` **dinámicos** | `[ins-2610][jerez-2026-07-06][ferrenuestro-2026-07-07]` | localizar por placeholder, NO por id fijo. jerez/ferrenuestro igual a insumar (con placeholders); ferrenuestro: tipo fijo "Exhibición - 1" SIN segmento; `ion-datetime id=expDate0` default HOY; header icons close/checkmark/trash/add. `expirationBatch=TRUE` confirmado en UI |
| Cantidad/Lote/Fecha | **`h.fillNgModelKeyboard`** (`pg.click(sel,{clickCount:3})` + `keyboard.type`) | `[ins-2606][gmp-2606][ins-2610]` | usan `[(ngModel)]`, NO reactive forms |
| Fecha vencimiento | `ion-datetime-button` → overlay → `h.confirmDatetime` (Aceptar en shadowRoot) | `[ins-2606][gmp-2606]` | |
| Aceptar modal | `ion-icon[name="checkmark-outline"]` en header del modal | `[gmp-2606]` | |
| Pedido Sugerido | `ion-button.botonAddAmarillo` en Tab Resumen | `[ins-2606][ins-2622]` | si `suggestedOrderByDispatchAndReturn=true`. El modal `inventario-sugerido-modal` muestra "Sugerido UNIDADES: N,NN" + "Días desde último Inventario" / "Días para siguiente Inventario" (`quUnitSuggested` vive aquí, NO en Tab General). Cerrar con `modal.dismiss(null,'cancel')` evita crear el pedido `[ins-2622]` |
| Guardar / Enviar | `.imagenGuardar` / `.imagenEnviar` (header fijo) | `[gmp-2606][ins-2606][ins-2610]` | Guardar: confirm (Cancelar/Aceptar) → éxito "Inventario guardado con éxito". Enviar: "¿Desea enviar el Inventario?" → "El Inventario será enviado" (2 alertas) `[ins-2610]` |
| Item lista BUSCAR | `pg.mouse.click` sobre coords (dispatchEvent no navega) | `[gmp-2606][ins-2610]` | ítems `ion-item` "Nro. Ref.: N Cliente: COD - NOMBRE Estatus: X Fecha:"; searchbar `ion-searchbar` "Inventarios..." filtra realtime. Botón BUSCAR del home = coords del `ion-button` (no del `ion-col`); navega in-place (URL sigue `/inventarios`) |
| `input[type="text"]` nativo para Lote en modal inventario | `ion-input[type="text"]` falla como selector CSS en este build (tipo = propiedad Angular, no atributo DOM); usar `input[type="text"]` nativo hijo dentro del modal o coords del rect | `[prc-2606]` | Universal confirmado |
| Modal cliente sin `show-modal` en algunas condiciones | Usar `ion-modal p` (no `ion-modal.show-modal p`) para buscar nombres de cliente; la clase `show-modal` puede no estar presente dependiendo del estado del modal | `[prc-2606]` | # candidato — confirmar en otras playas |
| Alerta "cambio de cliente" al re-seleccionar en form ya iniciado | `ion-alert` con texto: "Se ha detectado cambio del cliente por lo que deberá iniciar nuevamente la transacción." CANCELAR/ACEPTAR | `[prc-2606]` | Universal — confirmar en otras playas |
| Picker de cliente — validar visibilidad real antes de clicar | al reabrir el picker una 2ª vez, filtrar los `<p>` de nombre por `getBoundingClientRect().width>0` **antes** del click (los ítems ocultos dan rect 0 → click al vacío) | `[latino_cosmetica-20260714][ferrenuestro-20260723]` | universal (2ª confirmación) — evita clics en items rect=0 al recuperar el estado del picker en un 2º inventario |

| Alert de geolocalización al abrir el form + **el 1.er click en INVENTARIO no abre el formulario** | al clickear INVENTARIO sale `¡Alerta! / Para poder grabar la ubicación, te recomendamos activar el servicio de localización en tu dispositivo` [ACEPTAR] **tapado por un `ion-loading`** (ver `_comunes.md`): hay que `dismiss()` el loading, aceptar el alert y **volver a clickear INVENTARIO** (el 1.er click se consume y deja la app en el home de inventarios). Patrón fiable: click INVENTARIO → bucle ≤8×1,5 s {si hay alert: dismiss loading + click Aceptar; cortar cuando `ion-segment-button` visibles > 0} | `[latino_cosmetica-20260729]` | ⚠ sin esto el módulo parece "no abrir el formulario" y se pierde el caso DM-INV-002 |
| ✅ **La lista BUSCAR SÍ navega en este build** (revierte el BLOCKED de el_valle) | dispatch Pointer+Mouse sobre el `ion-item` **+** `pg.mouse.click` en el centro del `ion-label` (no del `ion-item`) abre el formulario del Guardado en ~4 s | `[latino_cosmetica-20260729]` | Contradice `[el_valle-20260728]` ("no navega por CDP, bloquea DM-INV-026"): con `window.ng=true` y el `ion-label` como target sí abre. **Desbloquea DM-INV-026** (reconfirmado: abre en tab **General**, defecto conocido cosmético) |
| Envío: **3 alertas**, la 3.ª trae el Nro.Ref del servidor | `¿Desea enviar el Inventario?` [Cancelar/Aceptar] → `El Inventario será enviado` [OK] → **`Inventario nro. <REF> enviado exitosamente`** [OK] → navega al home de inventarios | `[latino_cosmetica-20260729]` | insumar reportaba 2 alertas; acá la 3.ª es la que **da la Ref** (18) sin necesidad de volver a la lista. Leer el mensaje por `.alert-message` |
| Tablas LOCALES (SQLite del device) | cabecera `client_stocks` · detalle **`client_stocks_details`** · unidades **`client_stocks_details_units`** | `[latino_cosmetica-20260729]` | ⚠ corrige el smoke: el detalle NO es `client_stock_detail` (nube) ni `client_stocks_detail`; **doble plural** en ambas hijas. Un nombre errado aborta TODA la transacción sqlitePlugin (se pierden las demás queries del mismo batch) |

| ⚠ **Lista de productos: el ítem puede estar BAJO EL FOLD → click al vacío** | `scrollIntoView({block:'center'})` → esperar ~900 ms → **re-leer** el rect → validar `0<y<744` antes de `pg.mouse.click`. Real: PCE03 devolvió `y=795` (viewport 744) y el `inventory-type-stocks-modal` "no abría", sin error | `[gmp-20260730]` | ver `_comunes.md` — es la causa nº1 de falso BLOCKED en este módulo. Con las coords corregidas, **`pg.mouse.click` simple basta**: no hizo falta el Pointer+Mouse combinado de `[gmp-2611]` |
| Sub-segmentos del Tab Inventario aparecen **al entrar a una familia** | nivel familias = solo contadores (Favoritos/Destacados/Inventario) + `input[placeholder="Búsqueda de productos"]`; **UBICACIÓN (EXHIBICIÓN/DEPÓSITO) + FILTRO (TODOS/INVENTARIADOS)** se renderizan recién dentro de la familia | `[gmp-20260730]` | no buscarlos en el primer nivel |
| Botón Aceptar del modal de captura | **`.save-btn`** (en `ion-buttons[slot=end]` del header del modal) — más estable que buscar el `ion-icon[name="checkmark-outline"]`. Hacer `pg.mouse.move()` antes del click | `[gmp-20260730]` | header icons: close/checkmark/trash/add |
| ⚠ `st_delivery = NULL` en cabeceras bajadas del servidor | los `client_stocks` sincronizados desde la nube traen **`st_delivery=NULL`** + `st_client_stock=1`; solo los creados en el device traen `1` (enviado) / `3` (guardado) | `[gmp-20260730]` | un `WHERE st_delivery=1` **NO** devuelve los sincronizados — no leerlo como "no enviados" |
| 🟢 **`expirationBatch=false`: 1.er CONTRAFACTUAL medido con el campo VACÍO** | **oráculo barato y NO destructivo: `input[placeholder="Ingrese lote"].required` (llega `false`) + `inventariosLogicService.expirationBatch`** | `[run_vzla-20260818]` | Con la VG en `false` el **`.save-btn` acepta con el lote vacío**, sin la alerta *"Complete cantidad, unidad, fecha y lote para continuar."*, y la nube guarda `nu_batch=''` + `da_expiration`=HOY (**dos** capturas). ⇒ **cierra por el otro lado la regla graduada en `RUNTIME §3`: la VG SÍ discrimina.** Cuando el lote sí se llena, también cuadra |
| 🔴 **El buscador de productos del Tab Inventario NO filtra on-keyup en este build — EXIGE la lupa** | teclear deja las N familias; el click en `ion-icon[name="search-circle-sharp"]` las reduce | `[run_vzla-20260818]` | **Contradice `[kron-20260817]`** (allá sí filtraba al teclear) ⇒ **medir por corrida, no extrapolar**. En el MISMO módulo conviven las dos mecánicas: clientes y productos exigen lupa, mientras el `ion-searchbar` de la lista BUSCAR filtra **on-keyup** |
| **La lista BUSCAR de INVENTARIOS SÍ filtra por Nro. Ref** | `ion-searchbar` "Inventarios…" con `52` → 1 resultado | `[run_vzla-20260818]` | ⚠ Contrasta con DEVOLUCIONES de la misma corrida (`351`→0): **el mismo componente cubre la Ref en un módulo y no en el otro** ⇒ es implementación por módulo, **no** limitación del build |
| **`requiredComment` no aplica a INVENTARIOS — y la UI lo rotula sola** | además de `required=false`, bajo el input aparece **"Mín. 0 - Máx. 255 caracteres"** | `[run_vzla-20260818]` | **Oráculo de coste 0: leer ese texto en vez de provocar el rechazo.** `maxlength` real 255 vs VG `longitudComentario=250` (ver la regla de las 3 fuentes en `_comunes.md`) |
| **`client_avg_product` = pre-chequeo obligatorio del Pedido Sugerido** | `SELECT count(DISTINCT co_client), count(*) FILTER (WHERE co_client='<cliente>') FROM client_avg_product` **antes** de juzgar el `inventario-sugerido-modal` | `[kron-20260817][run_vzla-20260818]` | Acá: 24 filas / 2 clientes / **0 para el cliente de la corrida** ⇒ sin insumo el modal no puede rotular cantidad y **no es defecto**. Evita levantar el mismo falso hallazgo por 11.ª vez. Cerrar con `dismiss(null,'cancel')` para no crear un pedido |

- ✅ **difranca (EL YAQUE, 2026-08-13) — QUIEBRE DE INVENTARIO (cantidad 0), funcionalidad v21. Ref 20 Enviada.**
  - 🔴 **ORÁCULO DE BUILD, barato y directo:** leer el atributo `min` del
    `ion-modal input[placeholder="Ingrese cantidad"]`. **`min="0"` ⇒ la APK trae la v21**
    (commit `0a654f43`); `min="1"` ⇒ es anterior. No hace falta abrir el APK ni mirar el tag.
  - **El modal de captura de este build es MULTI-FILA (multi-lote).** Cada fila es un
    `ion-card.capture-row-card` con su propio `expDate{i}`, un `.delete-row-btn` (trash) y un
    `.add-lot-button` (`add-outline`) al pie para agregar filas. La fila nace con **unidad
    preseleccionada** (`productUnitList[0]`) y **fecha = HOY**; solo cantidad y lote llegan vacíos.
  - ⚠ **El buscador del modal de clientes NO tiene placeholder en este build.** El selector
    `ion-modal input[placeholder="Clientes..."]` **devuelve null** y se lee como "el modal no abrió".
    Real: **`#clienteSelectModal input.search-input.inputsSearch`**. El modal es `#clienteSelectModal`.
    Sigue exigiendo click en la lupa `search-circle-sharp` (6.ª confirmación: no filtra on-keyup).
  - **Cantidad 0 y −1, medidos:** `0` → aceptado, `quStock:0`, `validateCantidad:true`, modal cierra.
    `−1` → **rechazado** con `Inventario` / "Complete cantidad, unidad, fecha y lote para continuar."
    [OK] y el modal **queda abierto**; el `input` reporta `validity.rangeUnderflow=true`.
  - **Round-trip del 0 (lo que el commit realmente arriesgaba):** Guardar → BUSCAR → reabrir conserva
    `quStock:0` + lote + fecha, el modal reabre con `value="0"`, y el producto figura
    **"Inventariado: Exhibición"**. Oráculo fino del filtro de `inventoriedProducts`: el contador
    del nivel de familias dice **"Inventario 2"** con un producto en 0 y otro en 7 — antes del fix
    habría contado solo 1. **Usar ese contador para verificar el filtro sin abrir nada.**
  - **BD local sin `sqlite3`:** en este device `run-as … sqlite3` NO existe (`local-query.js` falla con
    *"exec failed for sqlite3"*). **Fallback que funciona:** pedir el `SQLiteObject` por CDP desde el
    componente (`ng.getComponent(document.querySelector('app-inventarios'))` → recorrer sus props
    buscando `typeof o.executeSql === 'function'`) y correr el SQL con `executeSql`. Sirve también para
    leer `client_avg_products` y elegir el producto del pedido sugerido.
  - **Pedido sugerido con `suggestedOrderByDispatchAndReturn=false`** (rama vieja, `getClientAvgStock`
    sobre la tabla local **`client_avg_products`**, NO sobre facturas): si `average>0` →
    `sugerido = average − actual` (quiebre ⇒ repone todo: actual 0, avg 12 → **12** ✅). Si `average=0`
    → **`sugerido = currentStock`**, o sea 0 para un quiebre y "lo que ya tiene" para uno con stock.
    ⚠ **Elegir el producto por `client_avg_products.average>0`** o el caso no prueba nada.
  - El `inventario-sugerido-modal` **no** renderiza "Sugerido UNIDADES: N" (6.ª playa). Cerrar con
    `dismiss(null,'cancel')`: su **ACEPTAR crea un PEDIDO**.
  `[difranca-20260813]`

- ✅ **grupo_fiel (EL YAQUE, v1.0/db19, `window.ng=TRUE`, 1 empresa): 16/16 PASS, 0 FAIL, 0 BLOCKED.** Inventario **Ref 2** (MP GELATO C.A. `J-504863246`, comentario `inv2 QA smoke`, **3 líneas / 2 productos**) → nube `id_client_stock=2`, `st_client_stock=1`, det=2/units=3; local `st_delivery=1`, colas en 0 ⇒ **guardado→enviado confirmado**, cotejo campo-a-campo **BD-FIELD-OK** 3/3.
  - 🔴 **`expirationBatch=true` BLOQUEA EN INVENTARIOS — 2.ª confirmación del alcance POR MÓDULO, en la MISMA corrida.** Alerta exacta `Denario Inventario` / *"Complete cantidad, unidad, fecha y lote para continuar."* `[OK]` con el **LOTE vacío** (la fecha nunca falta: `expDate0` nace en HOY). En DEVOLUCIONES de la misma corrida **no** exigió nada. Reproduce `[el_palmar-20260805]` literalmente. **Medir siempre con el campo VACÍO, no mirando si el input existe.**
  - 🔴 **`requiredComment` NO aplica a INVENTARIOS:** `ion-input#responsable` llega `required=false` y **`.imagenGuardar`/`.imagenEnviar` ya están `disabled=false` con el comentario vacío**. `maxlength=**255**` en esta APK (vs 120 en alipascua) ⇒ la constante **no es fija dentro de "v1.0/db19"**. Cierra el mapa: `requiredComment` = solo COBROS.
  - 🔴 **La lista BUSCAR NO reabrió por click real (2 intentos, sin interceptores) — sí por componente.** `elementFromPoint` caía dentro del `ion-item`, 0 `ion-loading`, 0 modales, y aun así no navegó. Contradice `[latino_cosmetica-20260729]`/`[el_palmar-20260805]`. **Vía que funciona con `window.ng=true`: `c.beforeOpenStock(c.getOriginalIndex(item))` + `ng.applyChanges(c)`** (⚠ `getOriginalIndex` recibe el **ITEM**, no el índice — ver `_comunes.md`). DM-INV-026 reconfirmado: abre en tab **General**, cosmético.
  - ✅ **Mismo producto en DOS ubicaciones: la app las trata separadas y el Tab Resumen lo delata sin abrir nada.** Cabecera real `Sel · Código · Producto · **Exhibición** · **Depósito** · Acción`, una fila por producto (`1.5LTS  8 CAJA │ 3 CAJA`, `330ML  5 CAJA │ -`). Al reabrir 1.5LTS ya inventariado en `exh` desde el segmento `Depósito`, **el modal abre VACÍO** (no arrastra la captura previa) y el ítem pasa a rotular `Inventariado: Exhibición / Depósito`. En BD comparten `co_client_stock_detail` y son 2 `client_stocks_details_units` distintas. **Usar la tabla del Resumen como oráculo visual barato de no-fusión.**
  - **Oráculo de build v21 (`min="0"` en cantidad) confirmado en El Yaque** — modal multi-fila `ion-card.capture-row-card` + `.add-lot-button` + `.save-btn`, unidad preseleccionada CAJA y fecha = HOY; solo cantidad y lote nacen vacíos. Reconfirma `[difranca-20260813]` en otro cliente de la misma playa.
  - ⚠ **El buscador del modal de clientes SÍ tiene `placeholder="Clientes..."` acá** (en difranca era `null`) ⇒ **anclar por `#clienteSelectModal input.search-input.inputsSearch`, que sirve en ambos**, nunca por el placeholder. Sigue exigiendo click en la lupa `search-circle-sharp` (7.ª confirmación).
  - ⚠ **`PRD-BUSCADOR-NO-REPUEBLA` NO aplica a la lista BUSCAR de este módulo:** filtra **on-keyup** y **vaciar repuebla solo** (`GELATO`→1, `ZZZZ`→0, vacío→2).
  - **Selector de empresa con 1 sola empresa: `disabled=true` + `value` OBJETO + `ng-valid` ⇒ no tocar nada**; el envío viajó con `idEnterprise:1`/`coEnterprise:"00001"`. Amplía la fila DEVOLUCIONES·INVENTARIOS·DEPÓSITOS·VISITAS de la tabla de variantes (allí llegaba habilitado).
  - **Pedido sugerido con `suggestedOrderByDispatchAndReturn=false`: el botón aparece igual (9.ª playa) y el modal SÍ rotula `Sugerido CAJA: N`** — pero N = **`currentStock`**, no un promedio: los 25 códigos de `client_avg_product` no existen en `client` (cruce 0/25) ⇒ `average=0` ⇒ fallback de la rama vieja (`[difranca-20260813]`). **No es 0 y no es defecto**: es el fallback por datos huérfanos. Cerrar con `dismiss(null,'cancel')` — su ACEPTAR crea un PEDIDO.
  - ⚠ **`na_product` NO existe en `client_stock_detail` de la NUBE** (sí en la local `client_stocks_details`): un `SELECT d.na_product` aborta el query. Hermano del `co_unit` de `[el_palmar-20260805]`.
  - ⚠ **Guardar habilita con el form casi vacío:** con solo el cliente y **cero capturas**, `.imagenGuardar` ya está `disabled=false` ⇒ se puede guardar un inventario sin productos.
  - `signatureStock=true` **no exigió firma** para Enviar; `nuAttachments=0`/`hasAttachments="false"` y el envío completó ⇒ adjunto **no obligatorio**. Envío = **3 alertas, la 3.ª da la Ref** (6.ª confirmación). Trash **sin confirmación previa**, borra también en local.
  `[grupo_fiel-20260817]`

- ✅ **kron (ISLA COCHE, v1.0/db19, `window.ng=TRUE`, 1 empresa con `enterpriseEnabled=TRUE`): Inventario Ref 3 Enviado, cotejo BD-FIELD-OK.**
  - 🔴 **`expirationBatch=true` exige LOTE en INVENTARIOS — 3.ª confirmación ⇒ ⬆️ GRADUADO a `RUNTIME.md §3`.** Alerta con el lote vacío, la fecha nace en HOY, y DEVOLUCIONES de la misma corrida no exige nada. 3 clientes / 2 servidores (el_palmar · grupo_fiel · kron). **Medir por módulo y con el campo VACÍO.**
  - ⚠ **El título de la alerta de validación pierde el prefijo `Denario` en este tenant:** acá es **`Inventario`** a secas; en el_palmar/grupo_fiel era `Denario Inventario`. Guardar/Enviar sí usan `Denario Inventario`, el borrado `Denario Inventarios` (plural) y la 3.ª de envío `Denario Premium`. ⇒ **no matchear títulos de alert por igualdad — leer el `.alert-message`.**
  - 🔑 **La variante del selector de EMPRESA cambia POR MÓDULO dentro del mismo tenant.** En CLIENTES de kron llega `disabled=true` auto-asignado; en **INVENTARIOS llega `disabled=false`**, con el objeto empresa completo como `value` (`{coEnterprise:'KRON_ADM',…}`), **sin `formcontrolname`** y `ng-valid` ⇒ **no requiere receta**. Amplía la tabla de variantes de `_comunes.md`. **`enterpriseEnabled=TRUE` y aun así idéntico a grupo_fiel con la VG en `false`.**
  - ✅ **La lista BUSCAR SÍ reabre por click real acá** (Pointer+Mouse sobre el `ion-item` + `pg.mouse.click` en el centro del `ion-label`, ~5 s, a la primera) — **contradice `[grupo_fiel-20260817]`** y reconfirma latino_cosmetica/el_palmar. ⚠ **`app-inventarios` de kron NO expone `beforeOpenStock` ni `getOriginalIndex`** ⇒ esa vía **no es un fallback disponible acá**: probar el click real PRIMERO.
  - ⚠ **TRES buscadores con TRES comportamientos en el mismo módulo — no unificarlos.** `#clienteSelectModal` **NO** filtra on-keyup (30 ítems antes y después) y exige `ion-icon[name="search-circle-sharp"]` (**8.ª confirmación**); en cambio `input[placeholder="Búsqueda de productos"]` (Tab Inventario) **y** el `ion-searchbar` de la lista BUSCAR **sí filtran on-keyup**.
  - 🔑 **El "Inv. Actual" del pedido sugerido SUMA las ubicaciones:** 7 en `exh` + 3 en `dep` → el `inventario-sugerido-modal` reporta `Inv. Actual 10`. **El inventario las mantiene separadas; el sugerido las consolida** (complementa el hallazgo de no-fusión de `[grupo_fiel-20260817]`).
  - ✅ **Oráculo de build v21 (`min="0"` en cantidad) confirmado en Isla Coche** — 3.ª playa (difranca El Yaque · grupo_fiel El Yaque · kron Isla Coche). Modal multi-fila `ion-card.capture-row-card` + `.add-lot-button` + `.save-btn`; unidad preseleccionada y fecha=HOY.
  - ⚠ **Guardar habilita con el formulario casi vacío — 2.ª confirmación:** con solo el cliente y **cero capturas** `.imagenGuardar` está `disabled=false` y el guardado completa (sirve para fabricar el Guardado de DM-INV-028); **`.imagenEnviar` en cambio sigue `disabled=true` hasta que hay al menos una captura.** Reconfirma `[grupo_fiel-20260817]` y **agrega el matiz que lo distingue**.
  - ⚠ **`coordenada` viajó VACÍA** (`"coordenada":""`), a diferencia de el_palmar/alipascua donde llegó el GPS real. No apareció alerta de geolocalización y el 1.er click abrió el form ⇒ consistente con "depende del permiso de ubicación del device". **Observación, no defecto.**
  - ⚠ **`st_delivery=NULL` en las cabeceras bajadas del servidor — reconfirma `[gmp-20260730]`:** el Ref 2 (cargado por QA en la web) llega a `client_stocks` local con `st_delivery=NULL`; solo el creado en el device trae `1`.
  - ✅ **Anclar el cliente por `co_client` resuelve los homónimos:** tecleando `J504480975` en `#clienteSelectModal input.search-input.inputsSearch` + lupa, el modal devuelve **1 solo ítem** entre los tres "MINIMARKET BICENTENARIA" de la cartera. **Nunca filtrar por nombre en este tenant.**
  - ✅ **La corrección del emparejamiento de `cotejo-payload.js` se ejercitó en su primer caso real y funcionó:** mismo producto en 2 ubicaciones (`exh`/`dep`) → **BD-FIELD-OK** sin colisión de claves. Ver `automation/db/COTEJO-BD.md`.
  `[kron-20260817]`

### Flujo mínimo probado
```
1. Click INVENTARIO → seleccionar cliente → tabs habilitan
2. Tab Inventario → click familia → click producto → modal inventory-type-stocks-modal
3. fillNgModelKeyboard Cantidad (+ Lote/Fecha si expirationBatch) → checkmark-outline
4. Tab Resumen verifica captura → Guardar (alert confirm) → Enviar (alert confirm)
```

### VG → DOM effects
| VG | true | false |
|----|------|-------|
| `expirationBatch` | campos Lote + Fecha venc en modal | solo Cantidad |
| `suggestedOrderByDispatchAndReturn` | botón Pedido Sugerido en Resumen | DM-INV-017 N/A |

### Anti-patrones confirmados
- ⚠ **Popover residual `ion-popover.select-popover`** (tipo de stock "Exhibición") queda abierto al abrir `inventory-type-stocks-modal` e **intercepta los clicks** de Cantidad/Lote ("ion-popover intercepts pointer events"). Hacer `dismiss()` de todos los `ion-popover` visibles **ANTES** de `fillNgModelKeyboard`. `[gmp-2611]`
- `h.fillIonInput` en campos del modal inventario → ngModel no se actualiza, guarda vacío. Usar `fillNgModelKeyboard`. `[ins-2606]`
- Borrado Guardado: trash `ion-button[color="danger"]` dentro del item → **directo sin confirmación previa** ("¡EL Inventario se borro con exito!") — distinto a Devoluciones/Pedidos. Back desde Guardado SIN cambios sale directo (no dispara dirty-guard). Reconfirmado dm-electronica. `[ins-2606][gmp-2606][ins-2610][gmp-2611][dm-electronica-20260713]`
- Tras `modal.dismiss()`, el `save-btn` del formulario responde solo si se hace `pg.mouse.move()` antes del click (el listener no re-engancha sin movimiento de puntero previo). `[rom-2606]`
- Si los clicks físicos no responden en el modal de captura, cerrar con el método del componente: `ionModal.dismiss(null,'cancel')`. `[rom-2606]`
- ⚠ **Alert residual de envío/guardado intercepta el botón BUSCAR del home:** tras cerrar el OK de Guardar/Enviar puede quedar un `ion-alert` (texto vacío) overlay que desplaza/bloquea BUSCAR (clicks no navegan). `dismiss()` de todos los `ion-alert` visibles ANTES de clickear BUSCAR. `[dth-2612]`
- **Guardar NO navega fuera del formulario:** tras "Inventario guardado con éxito" + OK la app permanece en el formulario (tab Inventario sigue editable); para ir a BUSCAR hacer Back al home de inventarios primero. `[dth-2612]`

### Notas por cliente
- ✅ **run_vzla (LA TORTUGA v1.0/db19, `window.ng=TRUE`, 1 empresa `FERRE_N`, `expirationBatch=FALSE`): inventario Ref 53 Enviado, 15 PASS / 1 N/A.** Cabecera 16/16 campos, 2 detalles y 3 unidades cuadran payload↔nube; diff de baseline por `id_user=470` exactamente **+1**; **1 solo POST**, sync inmediata, 2.º pase de baseline al cierre sin cambios. Patrones:
  - 🟢 **`expirationBatch=false` NO exige lote ni fecha — VERIFICADO, no asumido** (ver la fila de la tabla): es el **contrafactual** de la regla graduada en `RUNTIME §3` con el_palmar/grupo_fiel/kron. `[run_vzla-20260818]`
  - ✅ **Mismo producto en 2 UBICACIONES, validado hasta el motor de cotejo — 2.º caso real (tras kron), ahora con 2 productos en el mismo inventario.** `LLA-01` en exh 7 + dep 3: la app las trata separadas en las 5 capas (modal vacío al reabrir desde la otra ubicación · rótulo `Inventariado: Exhibición / Depósito` · tabla del Resumen con 2 columnas · 2 filas en la local · 2 filas en la nube) y **`cotejo-payload.js` devolvió `BD-FIELD-OK` con 0 mismatches** pese a que ambas unidades comparten `co_product_unit='LLA-011'` (solo difieren en `co_client_stock_detail_unit`) ⇒ **valida el emparejamiento por PK de negocio**. **Oráculo visual sin abrir nada: la tabla del Tab Resumen con columnas Exhibición │ Depósito.** `[run_vzla-20260818]`
  - 🔴 **La guarda de GPS bloquea también la navegación de INVENTARIOS — ~87 s con caché fría**, contra 30,3 s (pedidos) y 43,1 s (devoluciones) del mismo device y corrida. 3.er módulo afectado ⇒ el defecto es **de la guarda**, no del módulo. Matiz: acá **sí hay un `ion-loading` visible durante toda la espera, pero con mensaje VACÍO**. La reapertura de un Guardado desde la lista (2,97 s) **no** atraviesa la guarda. ✅ **GRADUADO a `RUNTIME §3` — techo de espera ≥ 120 s.** `[run_vzla-20260818]`
  - 🔴 **`pg.waitForFunction` ignora el `timeout` y corta a 30 s sobre el `pg` de CDP** ⇒ para cualquier espera > 30 s **sondear a mano en bucle con `page.waitForTimeout`**. ✅ **GRADUADO a `RUNTIME §11`** (ver también `_comunes.md`). `[run_vzla-20260818]`
  - **Selector de empresa: 6.ª confirmación "sin `formcontrolname` + objeto completo", `disabled=true` con 1 empresa.** ⚠ **Difiere de `[kron-20260817]`**, donde INVENTARIOS llegaba `disabled=false` ⇒ reconfirma el corolario: **leer `formcontrolname` + `disabled` + `value` + `ng-invalid` en CADA form**; con `value` objeto y `ng-valid`, no tocar nada. `[run_vzla-20260818]`
  - **DM-INV-026 reconfirmado** (el Guardado abre en tab **General**, cosmético) y round-trip §9 perfecto a los 2,97 s: cliente, comentario, las 3 capturas y el modal de `LLA-01` (cantidad 7, **lote vacío**, fecha HOY) intactos. `[run_vzla-20260818]`
- Defecto conocido DM-INV-026 (v6.6.14): formulario Guardado abre en tab General en vez de Inventario — no re-marcar FAIL. Reapertura Ref 0 vía CDP: el 1er click Pointer+Mouse SÍ abrió el form en insumar (round-trip Guardar→reabrir confirmado, cantidad conservada). `[gmp-2606][ins-2606][ins-2622]`
- Inventario guardado local sin sincronizar: Nro.Ref:0. `[ins-2606]`
- insumar: el modal de captura tiene segmento interno **Exhibición / Depósito / Todos** (tipo de stock); el producto capturado queda etiquetado "Inventariado: Exhibición". `[ins-2610]`
- ⚠ **don-theo: el `inventory-type-stocks-modal` NO tiene segmento** Exhibición/Depósito/Todos (a diferencia de insumar) — el tipo aparece como label fijo "Exhibición - 1". Header icons: close/checkmark/trash/add. Inputs Cantidad (`input type=number`) y Lote (`input type=text`) **sin placeholder ni id** → localizar por `type` dentro del modal; Fecha venc = `ion-datetime-button` label "Fecha de vencimiento". `[dth-2612]`
- ⚠ insumar (VG `suggestedOrderByDispatchAndReturn`): el botón "Pedido Sugerido" abre `inventario-sugerido-modal` cuyo **Aceptar genera un PEDIDO** y navega a `/pedido` (no solo cierra). Para inspeccionar sin crear pedido, evitar Aceptar o salir con clickBack sin guardar. `[ins-2610]`
- **piercar: `expirationBatch=TRUE` en campo real** — pese a CSV con `false`, la UI muestra Cantidad + Lote + Fecha vencimiento en `inventory-type-stocks-modal`. ⚠ Divergencia UI vs config — anotar y confirmar con desarrollo. La VG en YAML debe actualizarse a `true` tras verificación. `[prc-2606]`
- **piercar: `inventory-type-stocks-modal` tiene tipo fijo "Exhibición - 1"** sin segmento seleccionable (igual a don-theo, distinto a insumar que tiene segmento). Header icons: close/checkmark/trash/add. `[prc-2606]`
- **piercar: `st_client_stock=1` = Enviado** — otras playas usan st=2; tabla de estados difiere en piercar. Confirmar en próxima corrida. `[prc-2606]`
- piercar: cliente "7 CARS" no tiene sucursal → alerta bloqueante para inventario (igual que PEDIDOS/VISITAS). Usar "ANA MANZANARES" (MANZANARES) como cliente_test. `[prc-2606]`
- **jerez: `suggestedOrderByDispatchAndReturn` ACTIVA en UI pese a CSV `false`** — botón "Pedido Sugerido" (`ion-button.botonAddAmarillo`) aparece y funciona; modal `inventario-sugerido-modal` con "Sugerido PIEZA: N / Despacho / Días para siguiente Inventario". Misma divergencia UI-vs-config que piercar `expirationBatch` — ⚠ anotada como discrepancia en el YAML jerez, **VERIFICAR con desarrollo antes de cambiar la VG** (no a ciegas). `[jerez-2026-07-06]`
- **jerez: reapertura de Guardado carga datos ASYNC (~3s)** — al reabrir un inventario Guardado, cliente/capturas salen vacíos si se lee <2s; a ~3s cargan completos (cliente + Resumen "N PIEZA" + producto "Inventariado"). Esperar antes de juzgar round-trip. Consistente con la latencia de reapertura de VISITAS. `[jerez-2026-07-06]`
- **jerez: `inventory-type-stocks-modal` tipo fijo "Exhibición" SIN segmento** Exhibición/Depósito/Todos (como piercar/don-theo, distinto a insumar) PERO **con** inputs por placeholder "Ingrese cantidad"(number)/"Ingrese lote"(text) (como insumar). Header icons close/checkmark/trash/add; Fecha venc `ion-datetime` id=`expDate0` default HOY. `[jerez-2026-07-06]`
- **jerez: searchbar lista BUSCAR filtra por cliente/código/ref, NO por estatus** — "DANIELA"→2, "Enviado"→0 (pese a existir un Enviado), "ZZZZ"→0, vacío→2. `[jerez-2026-07-06]`
- **ferrenuestro: modal de cliente requiere click en `ion-icon[name="search-circle-sharp"]` para filtrar** — el `input[placeholder="Clientes..."]` NO filtra on-keyup; escribir texto y luego clicar el ícono lupa dispara la búsqueda. Selección por `<p>` del nombre habilita las 4 tabs. Reconfirmado dm-electronica (input NO filtra on-keyup; texto + click lupa filtra). `[ferrenuestro-2026-07-07][dm-electronica-20260713]`
- ⚠ **dm-electronica: `expirationBatch=false` NO oculta los campos Lote + Fecha de vencimiento del `inventory-type-stocks-modal`** — el modal muestra input "Ingrese lote" (text) + "Fecha de vencimiento" (`ion-datetime-button`) además de Cantidad, PERO **NO son obligatorios**: Aceptar (`checkmark-outline`) tuvo éxito con lote/fecha vacíos y el payload envió `nuBatch=""` + `daExpiration` en default de hoy. Es decir `expirationBatch=false` funciona a nivel de **validación** (opcional) pero los CAMPOS igual se renderizan. Divergencia UI-vs-config del mismo tipo que piercar `expirationBatch=TRUE` — ⚠ **VERIFICAR con desarrollo**. Tipo fijo "Exhibición" sin segmento; Tab Inventario con sub-segmentos UBICACIÓN (Exhibición/Depósito/Todos) + FILTRO (Inventariados) + input "Búsqueda de productos" (estructura idéntica a ferrenuestro). `[dm-electronica-20260713]`
- **dm-electronica: `st_client_stock=1` = Enviado** (servidor El Yaque, igual piercar; otras playas usan st=2). Nro.Ref UI = `id_client_stock` (nube) = 1 confirmado. `[dm-electronica-20260713]`
- **latino_cosmetica (La Tortuga v6.6.18): `st_client_stock=1` = Enviado** — inventario Ref 7 (ANNELI CA 13, 3058 BELOTTI ×5) persistió con `id_client_stock=7`, `st=1` (⚠ el payload trae `stClientStock=0`/`stDelivery=2`; corroborar por `id`+presencia en nube, NO por st global). Extiende el patrón `st=1` de El Yaque a La Tortuga. ⚠ `clientStock=true` REAL (módulo operable end-to-end, override 2022=false descartado). `expirationBatch=false` renderiza Lote+Fecha pero opcionales (Aceptar OK con vacíos, `nuBatch=""`) — reconfirma dm-electronica. Payload camelCase plano (cotejo espera clave anidada 'clientStock' → ajustar config de cotejo). `[latino_cosmetica-20260714]`
- **ferrenuestro: Tab Inventario con sub-segmentos UBICACIÓN (EXHIBICIÓN/DEPÓSITO) + FILTRO (TODOS/INVENTARIADOS)** + input `placeholder="Búsqueda de productos"`; familias con conteo (AGRICOLA 319…); click en familia navega a su lista filtrada por ubicación; familia sin stock de esa ubicación muestra "No hay productos disponibles" (ej. DESCUENTOS VARIOS). `[ferrenuestro-2026-07-07]`
- **ferrenuestro: `clientStock=false` NO oculta el módulo** — el módulo Inventarios aparece en HOME y es 100% operable end-to-end en UI (divergencia config↔UI, igual a piercar `expirationBatch` / jerez `suggestedOrderByDispatchAndReturn`). ⚠ El registro **SÍ persistió** en la nube (`client_stock` id=101) — durante la corrida se marcó BD-SAVED/"Por enviar" porque la **sync es diferida** (~3min, apareció tras la ventana de poll), NO porque `clientStock=false` sea gate de servidor. VERIFICAR con desarrollo si el módulo debe ocultarse cuando `clientStock=false`. `[ferrenuestro-2026-07-07]`
- ✅ **[OVERRIDE por MIGRACIÓN — ferrenuestro La Tortuga v6.6.18, window.ng=TRUE]: sync clientstock INMEDIATA + POST capturado** — tras re-apuntar el device a La Tortuga, el hook `nativePromise` SÍ capturó `clientstockservice/clientstock` (cabecera+detail+detail_unit) y el Nro.Ref servidor (101) llegó en la MISMA corrida — ya NO diferida como en Isla Coche (`[ferrenuestro-2026-07-07]`, donde client_stock no se interceptó). Coincide con dm-electronica/latino (captura + sync inmediata). Inventario TALADRO 080178 ×5 UND, lote QA0723, ubicación Exhibición; `expirationBatch=true` confirmado en payload (nuBatch+daExpiration). Cotejo BD N/A (DSN Isla Coche viejo) → cotejo por payload + UI. Defecto conocido DM-INV-026 (reapertura en tab General) reconfirmado, no FAIL. `[ferrenuestro-20260723]`
- **latino_cosmetica 2ª corrida (Isla Coche v1.0 / db 19, `window.ng=TRUE`): módulo 100% conducible, 16/16 PASS.** Inventario **Ref 18** (ANNELI CA 13 · 3058 BELOTTI ACOND CEBOLLA ×6 UNIDAD · lote `QA0729` · venc 29/07/2026 · ubicación `exh`) → local `id_client_stock=18`, `st_delivery=1`, `pending_transactions=0`, `failed_transactions=0` ⇒ **guardado→enviado confirmado**; payload `clientstockservice/clientstock` capturado con `quStock:6`/`nuBatch:"QA0729"`/`daExpiration`/`daysSinceLast:1`/`daysUntilNext:1` (⚠ el payload manda `stClientStock=0`+`stDelivery=2`: corroborar por `id`+`st_delivery` local, NO por `st` del payload). **Round-trip §9 PASS**: cantidad, lote y fecha sobreviven Guardar→reabrir. `expirationBatch=false` reconfirmado a nivel validación (2.ª captura aceptada con lote **vacío**). Trash del Guardado borra también en la BD local (`st_delivery=3`/`id=0` → 0 filas). `[latino_cosmetica-20260729]`
- ⚠ **latino_cosmetica: el `inventario-sugerido-modal` NO renderiza la línea "Sugerido UNIDADES: N"** — solo `Moneda:` + `Días desde último Inventario: 1` + `Días para siguiente Inventario: 1` + `<cod> - <producto>` + ACEPTAR. Coherente con `suggestedOrderByDispatchAndReturn=false` (el botón aparece igual — divergencia ya documentada en 5 playas, NO es hallazgo nuevo). `quUnitSuggested`/días viven acá, no en Tab General (reconfirma `[ins-2622]`). Cerrar con `dismiss(null,'cancel')`; **el modal se abre DUPLICADO** si se hace dispatch Pointer+Mouse *y* `mouse.click` — descartar ambos. `[latino_cosmetica-20260729]`
- **globalmp La Tortuga (app 1.0 / db 19, `window.ng=TRUE`): módulo 100% conducible, 16/16 PASS, 0 cuelgues.** Inventario **Ref 104** (AS04 ABASTO EL SITIO DSG · PCE03 PASTA ESP. TALLARIN CORTO ×15 **CAJA** · lote `LOTEQA30` · venc 31/07/2026 · `ubicacion:"exh"` · empresa **00002**) → local `id_client_stock=104`, `st_delivery=1`, colas en 0 ⇒ **guardado→enviado confirmado**; payload `clientstockservice/clientstock` capturado (`quStock:15`, `nuBatch`, `daExpiration`, `daysSinceLast:1`, `daysUntilNext:1`). **Round-trip §9 PASS** (cantidad+lote+fecha sobreviven Guardar→reabrir). Envío = **3 alertas**, la 3.ª da la Ref (reconfirma latino_cosmetica). ⚠ **NO apareció la alerta de geolocalización y el 1.er click en INVENTARIO SÍ abrió el formulario** (contrasta latino_cosmetica ⇒ depende del permiso de ubicación del device, no del build). Selección de cliente por **click real en el `<p>` del nombre** funcionó a la primera (sin `setClientfromSelector`), pero **el input del modal NO filtra al teclear: hace falta clickear la lupa `search-circle-sharp`** (3.ª confirmación, ahora en La Tortuga ⇒ es del build). Trash del Guardado borra también en BD local. `[gmp-20260730]`
- ⚠ **globalmp: `expirationBatch=TRUE` NO gobierna la validación — el modal ACEPTA con Lote VACÍO.** Probado con dato vivo: solo Cantidad=15, Lote vacío → el checkmark cerró sin error y el producto quedó "Inventariado". **No** salió *"Complete cantidad, unidad, fecha y lote para continuar."* (el rechazo que documentó `[el_valle-20260728]` con la VG en true). Es la **3.ª playa** donde `expirationBatch` no se comporta como se documentó (dm-electronica y latino_cosmetica lo mostraron con la VG en `false`). ⇒ La regla "`expirationBatch` gobierna la VALIDACIÓN, no la VISIBILIDAD" del `_comunes.md` **solo se sostuvo en el_valle**; en el resto los campos son opcionales con la VG en cualquier valor. **VERIFICAR con desarrollo antes de tocar la VG.** `[gmp-20260730]`
- ⓘ **globalmp: contraste PEDIDO SUGERIDO vs módulo PEDIDOS (útil para acotar la VG).** El agente de PEDIDOS de la misma corrida reportó que **no existe tab "Resumen" ni botón "Pedido Sugerido"** en ninguna tab del módulo Pedidos. En **INVENTARIOS ocurre lo contrario**: la tab **Resumen SÍ existe** (1 de las 4 del form) y el botón **`botonAddAmarillo` SÍ aparece** ahí y es funcional. ⇒ `suggestedOrderByDispatchAndReturn` gobierna el botón **dentro del formulario de inventario**, no una superficie del módulo Pedidos: **la ausencia en Pedidos no prueba que la VG esté inactiva**. El modal `inventario-sugerido-modal` **no renderiza la línea "Sugerido UNIDADES: N"** (solo Moneda + días + producto), igual que latino_cosmetica. `[gmp-20260730]`
- **alipascua (EL YAQUE, app 1.0 / db 19, `window.ng=TRUE`): módulo 100% conducible, 15/16 PASS + 1 N/A, 0 BLOCKED, 0 cuelgues.** Inventario **Ref 5** (V28556138 RENZO FERNANDO MARTINEZ MEJIAS · `7599220000011` CLORO BLANCURA ×**12 UNIDAD** · `ubicacion:"exh"` · lote **vacío** · empresa `ALIP_BSD`) → nube `id_client_stock=5`, `st_client_stock=1`, det=1/units=1; local `st_delivery=1`, colas en 0 ⇒ **guardado→enviado confirmado**. Cotejo campo-a-campo **BD-FIELD-OK** (incluye `coordenada` GPS real y `days_since_last/until_next=1`). Envío = **3 alertas**, la 3.ª da la Ref (4.ª confirmación). ⚠ **No apareció la alerta de geolocalización y el 1.er click en INVENTARIO SÍ abrió el form** (como globalmp). El **modal de captura abre con `mouse.click` simple** (sin Pointer+Mouse), pero requiere `scrollIntoView`+re-leer rect. El **searchbar de la lista BUSCAR SÍ filtra on-keyup** (RENZO→1 / ZZZZ→0 / vacío→3), a diferencia del **modal de clientes**, que exige click en la lupa `search-circle-sharp` (4.ª confirmación). Campo Comentario del form = `input[placeholder="Comentario:"]` (id dinámico), **no obligatorio** y con `maxlength` de producto 120 (ver `_comunes.md`). `expirationBatch=false` renderiza Lote+Fecha pero **aceptó con lote vacío** (`nu_batch=""` en nube) — 7.ª playa con la divergencia. DM-INV-026 reconfirmado (abre en tab General, cosmético). Trash sin confirmación previa, borra también en local. `[alipascua-20260804]`
- ⚠ **alipascua: pendiente `clientStock` ya aceptado por el servidor que se RE-POSTEA sin fin.** `pending_transactions` conservó `co_transaction=1785872185773.0` (type `clientStock`) pese a que ese registro ya estaba en la nube (`id_client_stock=4`, `st_client_stock=1`); el hook capturó **22 POST idénticos** en ~2,7 h (≈1 cada 7 min). Localmente quedó con **`st_delivery=2`** (ni 1=enviado ni 3=guardado), que parece ser lo que impide cerrar el pendiente. Sin daño de datos (el servidor deduplica por `co_client_stock`), pero la cola no se limpia. ⚠ Al contar POST capturados en este módulo, **descontar los reintentos de pendientes ajenos** antes de concluir "envío duplicado". VERIFICAR con desarrollo. `[alipascua-20260804]`
- ✅ **el_palmar (Isla Coche v1.0/db19, `window.ng=TRUE`, 2 empresas): Inventario Ref 17 Enviado, 0 reintentos de alert.** Patrones nuevos:
  - 🔴 **`expirationBatch=true` BLOQUEA EN INVENTARIOS Y NO EN DEVOLUCIONES — misma VG, misma corrida.** Alerta exacta: `Denario Inventario` / *"Complete cantidad, unidad, fecha y lote para continuar."* `[OK]`. **El campo que efectivamente bloquea es el LOTE**: la fecha nunca queda vacía porque trae default. ⇒ **el alcance de esta VG es POR MÓDULO** — no extrapolar. `[el_palmar-20260805]`
  - **Selector de EMPRESA preseleccionado, SIN `formcontrolname`, con el OBJETO empresa completo como `value`** (`{idEnterprise:1, coEnterprise:"1002", …, coCurrencyDefault:"USD"}`), `ng-valid` ⇒ **no requiere receta ninguna** (igual que devoluciones/depósitos/visitas, distinto de clientes y de pedidos — ver la tabla de 4 variantes en `_comunes.md`). La empresa efectiva llegó correcta en UI, payload y nube; el `enterprise_default` local apuntando a **otra** empresa **no se materializó**. `[el_palmar-20260805]`
  - **Etiquetas de alert (leídas, no predichas):** Guardar `[Cancelar, **Aceptar**]` → `[**OK**]` · Enviar `[Cancelar, **Aceptar**]` → `[**OK**]` → `[**OK**]` · Validación de captura `[**OK**]` · Borrado `[**OK**]` (sin confirmación previa). ⚠ **Los TÍTULOS son inconsistentes dentro del mismo módulo:** `Denario Inventario` (guardar/enviar/validación) · `Denario Inventarios` (borrado, **en plural**) · `Denario Premium` (la 3.ª de envío, la de la Ref). `[el_palmar-20260805]`
  - **Envío = 3 alertas y la 3.ª da la Ref del servidor** (`Inventario nro. **17** enviado exitosamente`) — **5.ª confirmación**; evita volver a la lista para obtener la Ref. `[el_palmar-20260805]`
  - **`ion-datetime` del modal (detalle completo en `_comunes.md`):** el `ion-datetime-button` tiene `textContent` **vacío** — la etiqueta legible (`"5 ago 2026"`) vive en `shadowRoot > button#date-button`; al abrirlo el `ion-datetime` pasa a visible dentro de un `ion-modal.md` y sus `Cancelar`/`Aceptar` están en `dt.shadowRoot.querySelectorAll('ion-button')`, **no** en el DOM claro. Los `.calendar-day` exponen `data-day`/`data-month`/`data-year`, y **`data-month` viene sin cero a la izquierda** (`"8"`). `[el_palmar-20260805]`
  - **La lista BUSCAR navega con dispatch Pointer+Mouse sobre el `ion-item` + `mouse.click` en el `ion-label`** (2.ª confirmación tras latino_cosmetica) — reabre el Guardado en ~5 s, desbloquea DM-INV-026. **El modal de captura abre con `pg.mouse.click` SIMPLE** (3.ª confirmación) **pero exige `scrollIntoView({block:'center'})` + ~1 s + RE-LEER el rect**: el producto objetivo llegó a `y=761` con viewport 744 — **es la causa nº1 de falso BLOCKED en este módulo**. `[el_palmar-20260805]`
  - **Searchbar de la lista BUSCAR filtra on-keyup; el modal de CLIENTES exige click en la lupa `search-circle-sharp`** — 5.ª confirmación de que son **dos comportamientos distintos en el mismo módulo**: no unificarlos. `[el_palmar-20260805]`
  - ⚠ **`maxlength` del Comentario = 255 en esta APK** (vs 120 en alipascua, misma "v1.0/db19") ⇒ **no dar la constante por fija dentro de una versión de build**: medirla. `[el_palmar-20260805]`
  - ⚠ **BD:** `transaction_statuses` de el_palmar **no tiene columna `tx_status`** (`SELECT … tx_status` → `column does not exist`) ⇒ **descubrir el esquema antes de traducir estados**. Y en la **nube** `client_stock_detail_unit` **no tiene `co_unit`**: la unidad viaja en **`co_product_unit`** (`160000019-FAR`) — un `SELECT u.co_unit` aborta el query. Columnas reales: `id/co_client_stock_detail_unit`, `co/id_client_stock_detail`, `co_product_unit`, `id_product_unit`, `ubicacion`, `qu_stock`, `da_expiration`, `nu_batch`, `co/id_enterprise`, `co_operation`, `da_update`. `[el_palmar-20260805]`
  - **1.er click en INVENTARIO abrió el formulario y NO hubo alerta de geolocalización** (consistente con globalmp/alipascua, opuesto a latino_cosmetica) ⇒ **depende del permiso de ubicación del DEVICE, no del build**. Con `userMustActivateGPS=false` la coordenada real igual viajó en el payload. `[el_palmar-20260805]`
- **ferrenuestro: botón "PEDIDO SUGERIDO" (`botonAddAmarillo`) presente pese a `suggestedOrderByDispatchAndReturn=false`** — misma divergencia UI-vs-config que jerez/piercar; modal `inventario-sugerido-modal` funcional ("Días desde último Inventario / Días para siguiente Inventario"). Cerrar con `dismiss('cancel')` para no crear pedido. VERIFICAR VG con desarrollo antes de cambiarla. `[jerez-2026-07-06][ferrenuestro-2026-07-07]`

---
