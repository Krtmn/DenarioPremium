> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo PRODUCTOS

### Identidad
- Ruta: `/productos` · Sub-vistas: `product-structures-list` (estructuras) → `product-list` (productos) → detalle
- Solo lectura — sin registros creados
- Selector tipo estructura: `ion-select` (valores: LINEA = idTypeProductStructure 2)

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Selector tipo | `ion-select` en estructuras — es el **2º** ion-select visible (sel[0]=empresa, sel[1]=tipo) | `[gmp-2606][ins-2606][rom-2606][ins-2610]` | cambio de tipo: tomar `value` (objeto) del `ion-select-option` por texto y asignarlo a `.value` + `ionChange`. `selectIonPopover` por texto NO aplica (valor es objeto) `[ins-2610]` |
| Estructuras (lista) | `product-structures-list ion-item.listaItems` (título en `h3.font-ListProduct` + `ion-badge` conteo) | `[ins-2610][gmp-2611]` | ⚠ `.product-structure-title` **NO existe** — el título real es `h3.font-ListProduct`. Cada ítem "NOMBRE {conteo}" (ej. "ALIMENTOS 158"); click → `product-list` con `mouse.click` en coords reales. 34 estructuras en globalmp |
| Productos (lista) | `product-list ion-item` (clase `item md item-lines-default`, **NO** `.listaItems`) | `[gmp-2611]` | cada item: Nombre + "Código: XXX" + "Precio: N BS" + "Precio: N USD" + "Inventario: N". El placeholder "No hay productos disponibles" coexiste como 1er ion-item → filtrar `!/No hay/i` |
| Campo búsqueda | `input.search-input.inputsSearch` (placeholder "Búsqueda de productos", NO ion-input) — **fuera de `product-list`** (`closest('product-list')` = null), localizar por clase global | `[gmp-2606][gmp-2611]` | limpiar con `click({clickCount:3})` + **Backspace×N** (⚠ `Control+A` NO surte efecto) + `keyboard.type` + `press('Enter')`; sin Enter no re-filtra |
| Estructura → productos | click en ítem estructura → `product-list` | `[gmp-2606]` | |
| 🔴 **`productos-search` NO tiene lupa — su único `ion-button` es una TRAMPA** | La `.search-row` trae exactamente 3 interactivos: **`ion-button.back-button`** (`ion-icon[name=arrow-back-outline]`, ~26,95) que **vuelve a `product-structures-list`**; el `input.search-input.inputsSearch` (~200,95); y **`button.clear-search`** (`ion-icon[name=close-circle]`, ~325,95). **El disparador de búsqueda es `Enter`, no un botón** | `[run_vzla-20260818]` | ⚠ Tomar "el primer `ion-button` de la fila de búsqueda" como lupa **saca de la lista sin aviso** (costó 2 mediciones y explica retornos a estructuras que parecían espontáneos). **Contrasta con INVENTARIOS/PEDIDOS de la MISMA corrida, donde sí hay `ion-icon[name="search-circle-sharp"]` y es obligatoria** ⇒ **los buscadores no se unifican: medir por módulo** |
| 🔴 **`button.clear-search` limpia el texto pero NO re-dispara la búsqueda** | click en `button.clear-search` deja `input.value===''` y `comp.searchText===''` pero la lista **conserva el resultado anterior** (13 de 13) | `[run_vzla-20260818]` | Variante nueva de `PRD-BUSCADOR-NO-REPUEBLA`: **el camino `Enter` deja 0 ítems; el camino X deja el filtro viejo. Ninguno restaura la estructura** ⇒ recuperar el baseline re-entrando a la estructura. `# candidato — confirmar en próxima corrida` |
| 🔴🔴 **`globalConfig.variables` es un `Map` de 176 claves LEGIBLE en runtime** | `window.ng.getComponent(document.querySelector('product-detail')).globalConfig.variables` → `Map(176)`; recorrer con `for (const [k,v] of V)` | `[run_vzla-20260818]` | **Cierra el hueco de `[el_palmar-20260805]`/`[difranca-20260807]`** ("las VGs de producto no están en `localStorage` ni en la tabla local"): sí lo están, **en el servicio**, y se leen desde cualquier componente. ⚠ **Trae SOLO las claves del dump GLOBAL** — las exclusivas del override de cliente (`listProductsBy`, `showProductByGrid`, `setQuUnit1`, …) **no aparecen**, lo que **corrobora la regla "gana el global"** |
| **`productService.catalog*` expone las VGs de catálogo YA RESUELTAS** | `catalogShowStock`, `catalogQuUnitDecimals`, `catalogUnitByPriceList`, `catalogValidateWarehouses`, `catalogShowProductImages`, `catalogDisplayProductPoints`, **`catalogProductMinMul`**, **`catalogHideStock0`**, **`catalogHideProdWithoutPrice`** + datasets `catalogListaList`, `catalogListaPricelist`, `catalogListaUnitInfo`, `catalogProdMinMulMap` | `[run_vzla-20260818]` | **Amplía `[difranca-20260807]`**, que decía que `hideProdWithoutPrice` y `productMinMul` "hay que buscarlas en otro lado": están acá |
| **El buscador de PRODUCTOS está ACOTADO a la estructura abierta** | el filtro corre sobre `idProductStructureList`, no sobre el catálogo entero | `[run_vzla-20260818]` | `LLAVE` = 81 en el catálogo, **13** en el subárbol CERRADURAS, y la UI muestra 13. Al construir el oráculo de DM-PRD-006 **filtrar el `SELECT` por `id_product_structure IN (idProductStructureList)`**. Leerlo del componente + `count(*) GROUP BY id_product_structure` **reproduce el badge exacto** ⇒ diagnóstico de 1 paso para separar "rollup" de "lista truncada" (reconfirma `[el_palmar-20260805]`) |
| 🔴 **Limpiar la búsqueda NO restaura la lista de la estructura** | vaciar `input.search-input.inputsSearch` (Backspace×N) + `Enter` deja `product-list` en **0 ítems** con el empty-state, en vez de volver a los N productos de la estructura | `[latino_cosmetica-20260729]` | Persiste tras ~7 s (no es settle). **Workaround para automatización:** para recuperar la lista, re-buscar un término amplio (ej. la marca) o re-entrar a la estructura — **no** asumir que el campo vacío es el estado "sin filtro". Impacta el baseline de DM-PRD-009: medirlo tras un término válido, no tras limpiar. # candidato — confirmar en otras playas |
| ⚠ `product-list` re-renderiza solo los ítems VISIBLES al volver del detalle | tras el back detalle→lista el DOM trae ~9 de 59 `ion-item` (virtualización de render) | `[latino_cosmetica-20260729]` | No usar el conteo de `ion-item` como oráculo de datos inmediatamente después de un back; medir tras scroll/settle. No es pérdida de datos |
| ⚠ Placeholder residual | "No hay productos disponibles" permanece como 1er `ion-item` en `product-list` aun con productos | `[ins-2610]` | al iterar, filtrar `!/No hay/i.test(text)` para no clickearlo |
| ⚠ Empty-state en búsqueda sin resultados — **2 formas por build** | en La Tortuga v6.6.18 "No hay productos disponibles" aparece como `<p class="search-empty-state ion-text-center">` **FUERA** de `ion-list` (con `ion-list` completamente vacío), NO como 1er `ion-item` | `[latino_cosmetica-20260714]` | # candidato universal — al verificar búsqueda vacía (DM-PRD-007) **buscar el texto en todo `product-list`**, NO asumir que es un `ion-item`. Tolerar ambos patrones (gmp/ins = ion-item; La Tortuga = `<p>`) |
| Detalle producto | nombre, código, precio, unidad, estructura, almacén, inventario | `[gmp-2606][gmp-2611]` | campos núcleo: Nombre/Código/Estructura/Unidad de venta/Lista de precio/Precio/Almacén/Inventario |
| 🔴 **Detalle = `product-detail` (`ProductDetailComponent`) — monta a los ~2,5 s y DESMONTA `product-list`** | `document.querySelector('product-detail')`; oráculo de navegación = **diff de custom elements visibles**, NO la URL ni `offsetParent` | `[grupo_fiel-20260817]` | **`app-product-detail` NO existe** (nombre sin prefijo `app-`). 3 trampas que costaron 3 casos ⛔ BLOCKED en cascada: **(1) TIMING** — con la espera estándar de **1.200 ms** el nodo **todavía no existe**; hace falta **~2.500 ms**. **(2) `product-list` no se oculta, se DESMONTA** — junto con `productos-search` desaparece del DOM y `product-detail` ocupa su lugar dentro de `app-productos` ⇒ un chequeo por `offsetParent` o por "¿sigue `product-list`?" es ambiguo en la transición. **(3) La URL NO cambia**: los 3 niveles (estructuras/lista/detalle) se quedan en `/productos` ⇒ **la ruta no sirve de oráculo en este módulo**. Receta que lo resolvió al 1.er intento — enumerar el DOM en vivo **después** del click en vez de adivinar nombres: `[...document.querySelectorAll('*')].filter(e => e.tagName.includes('-') && !/^ION-/.test(e.tagName) && e.getBoundingClientRect().width>0).map(e=>e.tagName.toLowerCase())` → antes `[app-productos, productos-header, productos-search, product-list, …]` · después `[app-productos, productos-header, **product-detail**, swiper-container, swiper-slide, …]`. Confirmar con `window.ng.getComponent(el).constructor.name` → `ProductDetailComponent`. ⚠ `swiper-container`/`swiper-slide` aparecen solo con `showProductImages=true` (carrusel) — **no** son parte del oráculo |
| Lista de precios | `product-detail ion-select` "Lista de precio"; cambio = asignar `.value = opt.value` (**objeto** `{idList,coList,naList,...}`) + `ionChange` | `[gmp-2606][gmp-2611]` | `selectIonPopover` por texto NO aplica (value es objeto); el precio visible se recalcula. ⚠ ver defecto romher |
| Botón atrás | `productos-header > a` (`<img>` SIN clase `.fechaAtras`, top<80) → `pg.mouse.click(coords)`; **fallback ferrenuestro:** localizar la `img[src*="flecha-blanca"]` → `closest('a')` (coords ~34,51) | `[gmp-2606][gmp-2611][ins-2622][ferrenuestro-2026-07-07]` | `h.clickBack` NO sirve aquí. ⚠ **Back desde `product-list` o `product-structures-list` sale DIRECTO a HOME** (no pasa por estructuras como nivel intermedio); detalle→product-list SÍ es un nivel de back. Reconfirmado en insumar `[ins-2622]` y dm-electronica `[dm-electronica-20260713]`. ⚠ **ferrenuestro:** `productos-header > a` NO matcheó (el `<a>` no es hijo directo del header) → localizar por la img `flecha-blanca.png` es robusto. ✅ **GRADUADO `[gmp-20260730]` (2ª playa independiente):** en La Tortuga v1.0/globalmp `productos-header > a` **tampoco** matchea. **Usar la img como vía PRIMARIA y `productos-header > a` como fallback, no al revés:** `Array.from(document.querySelectorAll('img')).filter(i => /flecha/i.test(i.src) && i.getBoundingClientRect().width > 0 && i.getBoundingClientRect().top < 80)[0].closest('a')` → coords **~34,51**. ⚠ A diferencia de VENDEDORES, acá el `<a>` padre **sí existe** (`closest('a')` opera) `[ferrenuestro-2026-07-07][gmp-20260730]` |
| Scroll infinito | `ion-infinite-scroll` (disabled si todo cabe en 1ª página); disparar `ionInfinite` 2× con settle ~1.8s carga páginas sucesivas (50→108) | `[gmp-2606][prc-2606][ins-2622]` | piercar: 77 productos en SENSORES → `ionInfinite` se dispara; spinner desaparece al agotar catálogo. ⚠ insumar: **medir baseline TRAS el settle**, no inmediatamente tras limpiar la búsqueda (la lista pasa transitoriamente por 0) `[ins-2622]` |
| `scrollIntoView` antes de click en estructura fuera de viewport | `item.scrollIntoView({behavior:'instant', block:'center'})` + wait 300ms + `mouse.click` en coords frescos | `[prc-2606]` | # candidato — las estructuras debajo del fold no reciben el click sin scroll previo |
| `ion-select` detalle producto (lista de precios) = popover interface | Asignar `.value = opt.value` (objeto completo `{idList,coList,naList,…}`) + `ionChange` + `popover.dismiss()`; sin necesidad de abrir popover primero | `[gmp-2606][gmp-2611][prc-2606]` | refuerza patrón existente; popover interface (NO action-sheet) |
| Descartar `ion-popover` residual antes de back en `productos-header > a` | `document.querySelectorAll('ion-popover').forEach(p => p.dismiss())` + wait 800ms antes del click; si hay popover visible el click no navega | `[prc-2606]` | # candidato — confirmar en otras playas |

### Flujo mínimo probado
```
1. /productos → product-structures-list (ion-select tipo)
2. Click estructura → product-list → buscar (focus+type+Enter)
3. Click producto → detalle (precio, lista de precios)
4. Back: detalle→lista OK; lista→estructuras = FAIL conocido (ver abajo)
```

### Anti-patrones confirmados
- Búsqueda NO filtra on-keyup/ionChange programático — requiere `keyboard.type()` + `press('Enter')`. `[gmp-2606]`
- Back de productos NO usa `.fechaAtras` — usar `header.querySelector('a')` + mouse.click. `[gmp-2606]`
- 🔴 **NO buscar "la lupa" en `productos-search`: no existe.** Su único `ion-button` es el **back a estructuras** y clickearlo saca de la lista. El disparador es `Enter`. Y `button.clear-search` **no re-dispara** la búsqueda. `[run_vzla-20260818]`

### Notas por cliente
- globalmp/romher/don-theo: solo 1 tipo de estructura (LINEA) → DM-PRD-002 N/A estructural. `[gmp-2606][rom-2606][dth-2612]`
- **globalmp: el detalle de producto muestra solo precio BS** (sin USD), aunque la lista (`product-list`) sí muestra BS+USD. Comportamiento del módulo para este cliente — campos núcleo presentes → PASS. 3 listas de precio (Precio 1/2/3); en 0611 Precio 2 = Precio 3 numéricamente. El defecto romher DM-PRD-013 (selector no refleja en pantalla) **no reproduce** en globalmp. `[gmp-2611]`
- **insumar: 2 tipos de estructura (Línea + Sub-Línea)** → DM-PRD-002 **ejecutable y PASS** (sel[1]=tipo). Excluye a insumar de la nota N/A anterior. `[ins-2610]`
- **Defecto abierto DM-PRD-013** (romher): selector Lista de precio cambia valor interno pero precio en pantalla no se actualiza. En globalmp SÍ cambia (PASS). Divergencia por cliente/datos. `[rom-2606]` vs `[gmp-2606]`
- **don-theo: DM-PRD-013 ejecutable y PASS** — 3 listas (CONTADO P0002 / CREDITO P0001 / FABRICA); el precio visible cambia al cambiar lista (objeto `{idList,coList,naList,...}` a `.value` + `ionChange`). El defecto romher NO reproduce. Detalle muestra solo precio Bs (sin USD), igual que globalmp; la `product-list` sí muestra Bs+USD. Estructura única LINEA con `idTypeProductStructure 1`. Back desde `product-list` sale directo a HOME (reconfirma `[gmp-2606]`). `[dth-2612]`
- insumar: detalle de producto tiene un único `ion-select` = "ALMACEN 01" (**sin Lista de Precios**) → DM-PRD-013 N/A estructural en insumar. Precio USD+BS como texto fijo ("Precio Unidad - UNIDADES 1,85 US$ / 958,23 BS"), no recalculable por selector. `[ins-2610]`
- insumar: lista muestra "Precio + IVA" (no observado en hidroponias). `[ins-2606]`
- **piercar: 24 estructuras (catálogo repuestos automotrices)** — AMORTIGUADORES, BOBINAS, BOMBAS DE ACEITE/AGUA, BUJIAS, CABLES, SENSORES, etc. Solo 1 tipo (Linea) → DM-PRD-002 N/A estructural. `userCanChangePriceList=TRUE` — 2 listas: GRAN MAYOR (P1) y MAYOR (P2); cambio de precio confirmado DM-PRD-013 PASS. Detalle muestra solo precio USD (igual globalmp/don-theo). `[prc-2606]`
- **ferrenuestro: 17 estructuras, 1 tipo (LINEA idTypeProductStructure 1)** → DM-PRD-002 N/A estructural (selector tipo con 1 opción). `userCanChangePriceList=true` → DM-PRD-013 PASS: 2 listas "PRECIO 1" (idList 1, coList P0001) / "PRECIO 2" (idList 2, coList P0002); precio recalcula 86,40→63,94 $ (defecto romher NO reproduce). ⚠ **detalle Y `product-list` muestran solo precio USD ($), SIN Bs** — a diferencia de gmp/don-theo/jerez (lista con Bs+USD); campos núcleo presentes → PASS. `quPageProduct=50` (paginación 50→100→150). texto_busqueda "TALADRO". `[ferrenuestro-2026-07-07]`
- ⚠ **ferrenuestro 2026-07-23 (La Tortuga v6.6.18, window.ng=TRUE): DM-PRD-013 REGRESIÓN DE DATO — 1 sola lista de precio (vs 2 en julio)** — esta sesión `priceListService.productlists` trajo **1 sola lista** (PRECIO 1, idList 1) en 3 productos de HERRAMIENTAS ELECTRICAS (080178, ESMERIL 080401, WINCHE), vs. `[ferrenuestro-2026-07-07]` que confirmó 2 listas (PRECIO 1/2) y PASS con recálculo 86,40→63,94 $ en el mismo 080178. El `ion-select` "Lista de precio" se ve correctamente `disabled=true` cuando solo hay 1 lista (comportamiento correcto de la UI, NO bug de render) → **DM-PRD-013 N/A estructural esta corrida**. Causa raíz = **datos/config del servidor** (PRECIO 2 ya no asociado a estos productos o la cuenta `leidy` perdió acceso). ⚠ **A VERIFICAR (config)** en la próxima corrida: si persiste, cambiar la nota de ferrenuestro de "2 listas PASS" a "N/A estructural (1 lista)". `window.ng.getComponent(el)` disponible (window.ng=TRUE) — útil para inspeccionar `comp.lists`/`priceListService.productlists` cuando un selector no responde. `[ferrenuestro-20260723]`
- **dm-electronica: 2 tipos de estructura (Linea id 1 / Sub-Línea id 2)** → DM-PRD-002 **ejecutable y PASS** (como insumar): sel[1]=tipo con 2 opciones; Linea→1 estructura "LINEA BLANCA 363"; Sub-Línea→3: MAYOR/MENOR/UNICO. Excluye a dm-electronica de la nota N/A por tipo único. sel[0]=empresa "BOTZ" (`enterpriseEnabled=true`). **DM-PRD-013 N/A estructural** — `product-detail` con **1 sola** lista de precio ("PRECIO LISTA 1", idList 1) → no hay 2ª lista a la cual cambiar (no reproduce ni descarta el defecto romher). Detalle muestra solo US$; `product-list` muestra US$+BS (patrón backend El Yaque, como gmp/don-theo/piercar/ferrenuestro/jerez). `quPageProduct=50` (50→100→150). texto_busqueda "A/A" (28 aires). `[dm-electronica-20260713]`
- **latino_cosmetica (La Tortuga, catálogo cosmética): 2 tipos de estructura (Marca id 1 default / Categoria id 2)** → DM-PRD-002 **ejecutable y PASS** (como insumar/dm-electronica): sel[1]=tipo con 2 opciones; Marca→3 estructuras (BELOTTI 74/PROKPIL 70/ROIAL 8), Categoria→13 (CERA/CRESPOS/CUIDADO FACIAL/DEPILACION/LINEA CAPILAR/…). sel[0]=empresa "LATINOCOSMETICA C.A." (`enterpriseEnabled=true`). ⚠ el YAML asumía "Linea"; el default real es **Marca** — corregido. `userCanChangePriceList=false` → **1 sola lista "DETAL"** → DM-PRD-013 N/A estructural. Detalle Y `product-list` muestran solo precio USD (SIN Bs, patrón como piercar/ferrenuestro). texto_busqueda "BELOTTI" (47→56 con infinite-scroll). `[latino_cosmetica-20260714]`
- ✅ **el_palmar (Isla Coche v1.0/db19, `window.ng=TRUE`, 2 empresas): 57 productos listados, cotizados y convertidos.** Patrones nuevos:
  - 🔴 **El contador del badge de estructura es un ROLLUP del subárbol — contrastarlo SIEMPRE contra el listado antes de concluir "faltan datos".** `product_structures.qu_products` en estructuras de **nivel 1** agrega los productos de sus **hijas**, que es donde realmente están asignados (`products.id_product_structure` **no apunta a ninguna estructura de nivel 1**). PRODUCTOS lo resuelve expandiendo a `idProductStructureList: Array(n)` = estructura + descendientes; **un módulo que filtre por la estructura sola da 0 ítems con el badge en 8/30.** Receta de diagnóstico: `SELECT id_product_structure, count(*) FROM products GROUP BY 1` vs `SELECT id_product_structure, qu_products FROM product_structures` — si los ids del badge no aparecen en el primero, **el vacío es de código, no de dato**. `[el_palmar-20260805]`
  - 🔴🔴 **`unit_pricelist` vacía NO implica "sin productos" — leer `unitByPriceList` PRIMERO.** La tabla solo se consulta si la unidad de venta se resuelve por lista de precios: con **`product-list.unitByPriceList === false`** la app **ni la toca**. En el_palmar PRODUCTOS listó, cotizó y convirtió los **57 productos con `unit_pricelist` en 0 filas** en device **y** nube. ⇒ **REFUTA** la hipótesis que el reporte de PEDIDOS de esta misma corrida levantó como causa raíz (ver el bloque de anti-patrones de `pedidos.md`). **Antes de pedirle a datos que cargue esa tabla, leer la bandera del componente.** `[el_palmar-20260805]`
  - **Búsqueda de productos: NO normaliza los diacríticos del término.** Al automatizar DM-PRD-006 **usar el término SIN tilde**, o el caso se lee como "no filtra". Verificar siempre `input.value` + codepoints + `comp.searchText` antes de imputar el fallo al teclado de Playwright. `# candidato universal`. `[el_palmar-20260805]`
  - **Back de PRODUCTOS: `img[src*=flecha]` + `closest('a')`, coords ~(34,51)** — `productos-header > a` **no matchea tampoco acá** (3.ª playa). Filtrar `width>0 && top<80`. Niveles confirmados: detalle→`product-list` es un back real; `product-list`→**HOME directo** (no pasa por estructuras); estructuras→HOME. `[el_palmar-20260805]`
  - **`ion-select` de tipo de estructura y de lista de precio: el `value` es un OBJETO** (reconfirma) — asignar `sel.value = opt.value` (objeto completo) + `new CustomEvent('ionChange',{bubbles:true,detail:{value:opt.value}})`. **No hace falta abrir el popover.** `[el_palmar-20260805]`
  - **Empty-state de búsqueda = `<p class="search-empty-state ion-text-center">`, FUERA de `ion-list`** (2.ª build, igual que La Tortuga v6.6.18) ⇒ buscar el texto en todo `product-list`, **no como `ion-item`**. `[el_palmar-20260805]`
  - ✅ **GRADUADO de `# candidato` a CONFIRMADO — limpiar la búsqueda NO restaura la lista.** 2.ª playa independiente, con build y servidor distintos. `[latino_cosmetica-20260729][el_palmar-20260805]`
  - ⚠ **Las VGs de PRODUCTO no están en `localStorage.globalConfiguration`** (0 coincidencias con `/prod|price|stock|hide|unit|min|mul|curren|rate|page/i` sobre las 180 claves) **ni son legibles en la tabla `global_configuration` local** (las ~318 filas devuelven los 4 campos NULL). **Leerlas del componente Angular:** `ng.getComponent(document.querySelector('product-list'))` expone `showStock`, `productStock0`, `unitByPriceList`, `currencyModuleEnabled`, `defaultCurrency`, `endPro`. `[el_palmar-20260805]`
- ✅ **difranca (El Yaque v1.0/db19, `window.ng=TRUE`, 3 empresas, 1.487 productos): catálogo listado y buscado.** Patrones nuevos:
  - ✅ **`PRD-BUSCADOR-NO-REPUEBLA` GRADUADO a confirmado-universal — 3.ª playa independiente** (latino_cosmetica · el_palmar · difranca), 3 builds y 3 servidores distintos. Vaciar el buscador + `Enter` deja `product-list` en **0 ítems con empty-state** en vez de restaurar la estructura. **Para automatizar: nunca usar "campo vacío" como estado sin-filtro; re-entrar a la estructura para recuperar el baseline.** `[latino_cosmetica-20260729][el_palmar-20260805][difranca-20260807]`
    - ⚠ **ACOTADO `[grupo_fiel-20260817]`: "universal" significa universal DE PRODUCTOS, no de todos los módulos** — ver la 4.ª playa más abajo y `RUNTIME §3`.
  - 🔴 **ANTES de reportar "la búsqueda con tilde falla", verificar que el DATO tenga tildes.** En difranca `Baño`→0 / `Bano`→39 **parecía** reproducir el defecto de el_palmar, pero **0 de 1.487 productos tienen ningún acento** ni en device ni en nube (el nombre real es `BaNo`). Receta obligatoria antes del veredicto: `SELECT count(*) FROM products WHERE na_product GLOB '*[áéíóúÁÉÍÓÚñÑ]*'`. Un **0** ahí convierte el caso en **N/A por dato**, no FAIL. **Acota `[el_palmar-20260805]`** (que sí tenía diacríticos reales). `[difranca-20260807]`
  - 🔴 **`<FFFD>` (U+FFFD) en nombres de estructura puede venir de la NUBE — cotejar antes de culpar al sync.** `product_structures` local y `product_structure` de nube traen **el mismo U+FFFD con idéntica longitud** ⇒ el móvil **no corrompe, refleja**. Diagnóstico barato: `Array.from(s).map(c=>c.codePointAt(0))` dentro del `evaluate` (**65533** = U+FFFD) + el mismo `SELECT` contra la nube. `[difranca-20260807]`
  - ⚠ **Estructuras con NOMBRE DUPLICADO en distinto nivel:** "BBK" existe como **Línea** (id 46, badge 114) y como **Sub-Línea** (id 107, badge 27). Un `find(/BBK/)` sobre la lista matchea la que esté renderizada según el tipo elegido ⇒ **anclar por `id_product_structure`, no por nombre**, cuando importe cuál es. `[difranca-20260807]`
  - **VGs de producto legibles en `product-list` (reconfirma y AMPLÍA):** `ng.getComponent(document.querySelector('product-list'))` expone `showStock`, `productStock0`, `unitByPriceList`, `currencyModuleEnabled`, `defaultCurrency`, `endPro` y **`idProductStructureList`** (el subárbol expandido — útil para explicar el badge-rollup de `[el_palmar-20260805]`). ⚠ **NO** expone `hideProdWithoutPrice`, `productMinMul` ni `conversionByPriceList`: esas hay que buscarlas en otro lado. `[difranca-20260807]`
  - **Reconfirmados (4.ª playa):** back = `img[src*=flecha]` + `closest('a')` filtrando `width>0 && top<80`, coords ≈(34,51) — `productos-header > a` **tampoco** matchea acá; niveles detalle→`product-list` back real, `product-list`→**HOME directo**, estructuras→HOME. `ion-select` de tipo y de lista de precio con `value` **OBJETO** (`sel.value = opt.value` + `ionChange`, sin abrir el popover). Empty-state = `<p class="search-empty-state">` **FUERA** de `ion-list` (3.ª build). Limpieza del buscador = `click({clickCount:3})` + **Backspace×N** + `keyboard.type` + **`press('Enter')`** (sin Enter no re-filtra; `Control+A` no surte efecto). `[difranca-20260807]`
- ✅ **grupo_fiel (El Yaque v1.0/db19, `window.ng=TRUE`, 1 empresa, catálogo mínimo de 4 productos): detalle mapeado por primera vez con su nombre real.** Patrones nuevos:
  - 🔴 **El componente del detalle es `product-detail`, monta a ~2,5 s y DESMONTA `product-list`** — ver la fila de la tabla de selectores. Dejó **3 casos ⛔ BLOCKED en cascada** (DM-PRD-012/020/021) en la corrida, **rescatados después** por el agente de VENDEDORES con la receta de enumerar custom elements en vivo. **Nunca probar nombres de componente a ciegas: enumerarlos.** `[grupo_fiel-20260817]`
  - **1 sola estructura ("Desechable", badge 4) y 1 solo tipo** → DM-PRD-002 N/A estructural. `listProductsBy="lineas"` **confirmado como default por UI**. `[grupo_fiel-20260817]`
  - **`userCanChangePriceList` efectivo: 2 listas y el precio SÍ recalcula** → DM-PRD-013 **PASS** (el defecto romher no reproduce): `{idList:1, coList:'02', naList:'Precio 2 - Nota de Entrega'}` (**default**) y `{idList:2, coList:'03', naList:'Precio 3 - Factura Fiscal'}`; `1.5LTS` pasa de **4,87 → 4,20 USD** y vuelve al restaurar. ⚠ **El agente de PRODUCTOS lo había marcado N/A por "1 sola lista" sin haber abierto el detalle** — moraleja: **un N/A de lista de precios no es válido si el detalle nunca se inspeccionó.** `[grupo_fiel-20260817]`
  - **El `ion-select` de Unidad de venta llega `disabled=true` con 1 opción (CAJA)** — comportamiento correcto de la UI cuando hay una sola unidad, **no** bug de render (mismo criterio que `[ferrenuestro-20260723]` para la lista única). `[grupo_fiel-20260817]`
  - **Detalle muestra solo USD; `product-list` muestra USD+BS** (patrón backend El Yaque, como gmp/don-theo/piercar/ferrenuestro/jerez/dm-electronica). El Bs de la lista corresponde a la **lista de precio activa** (`1.5LTS` = 3.756,66 Bs en la 02) → **al reportar un precio, decir siempre de qué lista es**. `[grupo_fiel-20260817]`
  - **Reconfirmados (5.ª playa):** back = `img[src*=flecha]` (`flecha-blanca.png`) + `closest('a')`, coords **(34,51)** — `productos-header > a` **tampoco** matchea; niveles detalle→`product-list` back real, `product-list`→**HOME directo**. `[grupo_fiel-20260817]`
  - 🔴 **`PRD-BUSCADOR-NO-REPUEBLA` — 4.ª playa (3 servidores, 3 builds) Y ACOTADO A PRODUCTOS.** Sigue reproduciendo en `product-list`, pero **en este build NO aplica a los demás módulos**: medido en la misma corrida, `app-pedidos-lista` (3→34), COBROS (`filteredItems` 0→6), DEVOLUCIONES (2 ítems) e INVENTARIOS (`GELATO`→1, `ZZZZ`→0, vaciar→2, filtra **on-keyup** sin lupa) **repueblan todos solos al vaciar**. ⇒ es un defecto **universal de PRODUCTOS**, no de la app entera: **no extrapolar el workaround a otras listas**. Graduación y acotación registradas en `RUNTIME §3`. `[latino_cosmetica-20260729][el_palmar-20260805][difranca-20260807][grupo_fiel-20260817]`
- ✅ **kron (Isla Coche v1.0/db19, `window.ng=TRUE`, 1 empresa `KRON_ADM`, catálogo chocolates 147 productos): 9 estructuras, 1 tipo.** 8 PASS / 0 FAIL / 2 N/A. Patrones nuevos:
  - 🔴 **`listProductsBy` NO nombra el tipo de estructura — designa el MODO de agrupar. ACOTA `[grupo_fiel-20260817]`.** En kron la VG vale `"lineas"` y el `ion-select` de tipo trae **una sola opción rotulada `Modelo`** (`idTypeProductStructure:1`, `coTypeProductStructure:'001'`) ⇒ DM-PRD-002 **N/A estructural**. En grupo_fiel el rótulo coincidió con la VG **por casualidad del dato**; no es una regla. **Leer el rótulo del `ion-select` en runtime, nunca derivarlo de la VG.** `[kron-20260817]`
  - 🔴🔴 **RECETA ANTI-FALSO-FAIL — `badge − listados = productos sin precio` cuando `hideProdWithoutPrice=true`.** GRAGEADOS badge **32** vs `product-list` **28** parece `PRD-LISTA-CORTA-CATALOGO`; **no lo es**. El `SELECT` por estructura con `count(*) FILTER (WHERE EXISTS (SELECT 1 FROM price_list … nu_price>0))` **predijo 9 de 9** estructuras (GOLOSINAS 10→4, GOTAS 44→39, GRAGEADOS 32→**28**, UNTABLES 32→31; el resto sin brecha). **El badge cuenta el total de la estructura; la lista solo los cotizables.** Correr ese query **antes** de imputar lista truncada. Catálogo kron: **147 total / 131 con precio**; la suma de los 9 badges da **147 exacto** ⇒ acá los badges **NO son rollup** (jerarquía plana), a diferencia de `[el_palmar-20260805]`. `[kron-20260817]`
  - 🔴 **`PRD-BUSCADOR-NO-REPUEBLA` — 5.ª playa y 4.º SERVIDOR (Isla Coche).** Medido contra baseline 28 en GRAGEADOS: `BALLS`→10 · `ZZZZZZZ`→0+empty-state · **vaciar+Enter→0 con `input.value===""` y `comp.searchText===""`** (estado idéntico al baseline). Recuperación verificada **re-entrando al módulo desde HOME** (9 estructuras + input limpio). Servidores acumulados: La Tortuga · El Yaque · Isla Coche. `[kron-20260817]`
  - **`ion-infinite-scroll.disabled` es el oráculo de "catálogo agotado", NO el `length`:** pasó `false`→`true` tras un `ionInfinite` + `ng.applyChanges`, con el count estable en 28 (28 < `quPageProduct=50` ⇒ 1 sola página). Cortar por el flag evita el falso "el scroll no cargó nada". ⚠ **`comp.endPro` se quedó en 20 con 28 ítems renderizados ⇒ NO es el puntero de render: no usarlo como oráculo de paginación.** `[kron-20260817]`
  - 🔴 **Detalle solo **BS**, `product-list` **BS+USD** — patrón INVERSO al de El Yaque/La Tortuga.** Con `comp.defaultCurrency='BS'` el `product-detail` rotula `78.186,50 BS` (sin USD), mientras la lista trae ambas. Contrasta con gmp/don-theo/piercar/ferrenuestro/jerez/dm-electronica/grupo_fiel (detalle solo USD). ⇒ **la moneda del detalle sigue a `defaultCurrency`, no al servidor**; al reportar un precio, decir siempre moneda **y** lista. `[kron-20260817]`
  - **`ion-select` con opción única llega `disabled=true` — 3.ª evidencia, y acá en AMBOS a la vez:** Unidad de venta (`BULTO`) y Lista de precio (`PRECIO 1`, `{idList:1,coList:'P1'}`). UI correcta ante opción única ⇒ DM-PRD-013 **N/A estructural, no BLOCKED**. Corroborado en nube: `SELECT id_list,co_list,na_list FROM list WHERE co_operation<>'D'` → **1 fila**. ⚠ Cumplida la lección de grupo_fiel: **el N/A se emitió con el detalle abierto e inspeccionado**. `[ferrenuestro-20260723][grupo_fiel-20260817][kron-20260817]`
  - **Reconfirmados (6.ª playa):** `product-detail` monta a **~2,5 s** y **desmonta** `product-list`+`productos-search`, con la **URL invariante** en `/productos` ⇒ oráculo = diff de custom elements visibles (`swiper-container`/`swiper-slide` aparecen por `showProductImages=true`). Back = `img[src*=flecha-blanca]` + `closest('a')` coords **(34,51)** — `productos-header > a` **tampoco** matchea; **detalle→`product-list` conserva el filtro de búsqueda intacto** (10 ítems de BALLS), `product-list`→**HOME directo**, estructuras→HOME. Empty-state = `<p class="search-empty-state">` **fuera** de `ion-list` (4.ª build). VGs en `ng.getComponent(product-list)`: `showStock:true`, `productStock0:false`, `unitByPriceList:false`, `currencyModuleEnabled:true`, `defaultCurrency:'BS'`, `endPro:20`. `[kron-20260817]`
- ✅ **run_vzla (La Tortuga v1.0/db19, `window.ng=TRUE`, 1 empresa `FERRE_N`, 36 estructuras): las VGs de producto quedan legibles de punta a punta.** Aportes en la tabla de selectores (`productos-search` sin lupa · `clear-search` no re-dispara · `globalConfig.variables` Map(176) · `productService.catalog*` · buscador acotado al subárbol). Reconfirmados: **back = `img[src*=flecha-blanca]` + `closest('a')` ≈(34,51) — acá el `<a>` padre SÍ existe, y `productos-header > a` sigue sin matchear (7.ª playa)**; niveles detalle→`product-list` back real **conservando el filtro**, `product-list`→**HOME directo**, estructuras→HOME. Empty-state `<p class="search-empty-state ion-text-center">` **fuera** de `ion-list` (**5.ª build consecutiva** ⇒ buscar el texto en todo `product-list`). `ion-select` de tipo y de lista de precio con `value` **OBJETO** (`sel.value = opt.value` + `ionChange`, sin abrir popover; Marca↔Sub-Linea, 36↔107). ✅ **`scrollIntoView({behavior:'instant',block:'center'})` + ~400 ms antes de clickear una estructura fuera del fold GRADÚA de `# candidato` `[prc-2606]` a confirmado** — con 36 estructuras casi todas caen bajo el fold. `[run_vzla-20260818]`
- **jerez: 8 estructuras, 1 tipo (LINEA idTypeProductStructure=1)** → DM-PRD-002 N/A estructural. `product-structures-list` sel[0]=empresa (3 opciones "INV JEREZ MOTORS VALERA/CARACAS/TURMEREMO"), sel[1]=tipo (LINEA única). `userCanChangePriceList=true` → DM-PRD-013 PASS: 2 listas "Precio 1" (idList 1, coList "01") / "Precio 3" (idList 2, coList "02"); precio recalcula (defecto romher no reproduce). Detalle muestra solo precio USD (lista muestra USD+BS, igual gmp/don-theo/piercar); almacén = "Ferreteria UNIFICADO VALERA 2 Valera". `[jerez-2026-07-06]`

---
