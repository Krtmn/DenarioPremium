> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo CLIENTES

### Identidad
- Ruta: `/clientes` · Componente raíz: **`app-clientes`** (las sub-vistas son internas — NO existe `app-client-home`)
- Sub-vistas: `app-client-list` (lista), `app-client-detail` (detalle)
- Botones home: CLIENTES, CLIENTE POTENCIAL, BUSCAR CLIENTE POTENCIAL

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Campo búsqueda | `input[type="text"][placeholder="Clientes..."]` (plain input, NO ion-input) | `[gmp-2606][ins-2606]` | focus + `keyboard.type()` + click botón search |
| Botón búsqueda | `ion-icon[name="search-circle-sharp"]` / `.clear-search` (coords ~317,95) | `[gmp-2606][ins-2606][rom-2606]` | requerido — la lista NO filtra on-keyup |
| Botón home CLIENTES | `ion-button.colorBorderBuscar` interno (por textContent) — **NO** el `ion-col` contenedor | `[ins-2610]` | el ion-col padre (y≈107, w≈350) absorbe el click sin navegar; clicar coords del ion-button exacto |
| Detalle: tab Doc. Venta | `ion-segment-button[value="docVentas"]` — asignar `ion-segment.value='docVentas'` + `ionChange` (+ `mouse.click` en segment-button como respaldo) | `[gmp-2606][gmp-2611][dm-electronica-20260713]` | renderiza tabla `.documents-table-panel--ready`; leyenda Vigente/Vencido/A favor |
| Detalle: tabla Doc. Venta | `.doc-ventas-tab` → `.documents-view` → `.documents-table-panel--ready` / `.documents-table-scroll` / `.documents-table-stack` | `[ins-2610][run_vzla-20260818]` | documentos NO son `ion-item`; tabla con header Tipo/Nº Doc/Moneda/Días Venc/Tasa/Monto/Saldo/Fecha Doc/Fecha Venc/Comentario. 🔴 **La leyenda literal es de 3 textos — `Vigente` / `Vencido` / `A favor`, NO "Documento vigente/Documento vencido"** (como decía el smoke extract): un match por `/Documento vigente/` devuelve `false` y se lee como "la tab no cargó". Los vencidos se marcan en el modelo con `colorRow:"Red"` |
| **Reapertura del potencial Guardado — coordenada canónica** | `x = rect.x + 0.35·w` sobre el `ion-item` (evita el trash de la derecha) | `[el_palmar-20260805][difranca-20260807][kron-20260817][run_vzla-20260818]` | **6.ª corrida** en que funciona a la primera y devuelve los 9 campos **y** la empresa (real (129,238) sobre un ítem de `x=10,w=340`) |
| Alert guardado/borrado | `.alert-title` (título) + `.alert-message` (mensaje) | `[ins-2610]` | textContent llega vacío justo tras el click; esperar ~800ms de render antes de leer |
| Form potencial: empresa | `ion-select[formcontrolname="idEnterprise"]` | `[gmp-2606]` | requerido (`ng-invalid` vacío); usar `h.selectIonPopover` |
| Form potencial: campos | 9 ion-inputs (`formcontrolname`): naClient, nuRif, txAddress, txAddressDispatch, txClient, naResponsible, emClient, nuPhone, **naWebSite** | `[gmp-2606][ins-2606][gmp-2611]` | los 8 primeros + idEnterprise (ion-select) = 9 controles requeridos que habilitan Guardar/Enviar; **naWebSite es opcional** (Guardar habilita sin llenarlo) `[gmp-2611]` |
| Guardar / Enviar | `ion-button.imagenGuardar` / `.imagenEnviar` | `[gmp-2606]` | disabled hasta los 9 controles válidos |
| Reabrir potencial Guardado para Enviar | el ítem Guardado NO tiene botón Enviar inline; click en la **zona izquierda** del `ion-item` (≈35% del ancho, evitando el trash a la derecha) reabre el formulario con campos precargados y Enviar habilitado | `[ins-2622][jerez-2026-07-06][ferrenuestro-2026-07-07][hidroponias-20260710][latino_cosmetica-20260714]` | desbloquea el envío (DM-CLT-026) sin depender de reapertura inestable; la nota `reapertura_ref0_cdp_inestable` no aplicó (navegó bien) |
| Form potencial: empresa exige **value numérico** | `ion-select[formcontrolname="idEnterprise"]`: sus `ion-select-option` tienen `value` **numérico** (1/2/3). Asignar `sel.value = 1` (number) + `ionChange{value:1}` → `ng-valid`, Guardar/Enviar habilitados | `[jerez-2026-07-06][ferrenuestro-2026-07-07][hidroponias-20260710][dm-electronica-20260713][latino_cosmetica-20260714]` | ⚠ asignar string `'1'` deja el control `ng-invalid`/`selVal=undefined` y los botones `disabled`. Refina el anti-patrón de auto-selección `[dth-2612][ins-2622]`: aquí SÍ hay opciones pero exige el **tipo** correcto. Reconfirmado con **1 sola empresa** en ferrenuestro, hidroponias (HIDROPONIAS VENEZOLA) y **latino_cosmetica** (LATINOCOSMETICA C.A. value=1): la opción única NO se auto-selecciona y exige el number explícito |

| 🔴 Form potencial: empresa **auto-asignada y `disabled`** (build Isla Coche v1.0 / db19) | `ion-select[formcontrolname="idEnterprise"]` llega con clase **`select-disabled`** y `sel.disabled === true`; el control queda **fuera de la validación** ⇒ **Guardar/Enviar habilitan con solo los 8 `ion-input`**, sin tocar el select. El envío igual viaja con `id_enterprise=1` / `co_enterprise="00001"` (verificado en BD local). | `[latino_cosmetica-20260729]` | ⚠ **Invierte** la fila anterior (value numérico + `ionChange`), que vale para La Tortuga/El Yaque v6.6.18. **Probar `sel.disabled` PRIMERO**: si es `true`, no pelear con el value — ya está resuelto. Si es `false`, aplicar `sel.value = 1` (number) + `ionChange`. 🔴 **ACOTADO `[el_palmar-20260805]`: el `disabled=true` vale solo con UNA empresa.** Con 2+ empresas el MISMO build v1.0/db19 entrega el select **editable, vacío y obligatorio** ⇒ Guardar/Enviar NO habilitan hasta setearlo (medido con los 8 inputs ya llenos). **Nunca leer "el botón no habilita" como FAIL/BLOCKED sin haber mirado `sel.value`.** |

| Coords estables de los 3 botones home + Guardar/Enviar | CLIENTES (180,107) · CLIENTE POTENCIAL (180,176) · BUSCAR CLIENTE POTENCIAL (180,245) · `ion-button.imagenGuardar` (267,32) · `.imagenEnviar` (326,32) | `[gmp-20260730]` | resolución del device Infinix HOT 60i; igual sacar coords por `getBoundingClientRect` si cambia el device |

### Flujo mínimo probado
```
1. Click módulo → app-clientes (3 botones)
2. app-client-list → buscar: focus input + keyboard.type + click search
3. Click ítem → app-client-detail (saldos, tab Doc. Venta)
4. clickBack: detail→list→clientes (no salta a HOME)
5. Potencial: fillIonInput ×8 + selectIonPopover idEnterprise → Guardar/Enviar habilitan
```

### Anti-patrones confirmados
- `fillIonInput` solo NO basta para la búsqueda: hace falta click en botón search. `[ins-2606]`
- No esperar que idEnterprise sea un ion-input — es ion-select, invisible en la lista de inputs. `[gmp-2606]`
- ⚠ **idEnterprise con una sola empresa NO se auto-selecciona** — aunque solo haya 1 opción, Guardar/Enviar siguen `disabled` hasta asignar `value` explícito + `ionChange`. `[dth-2612][ins-2622]`
- ⚠ **idEnterprise exige `value` NUMÉRICO, no string** — `sel.value='1'` deja el control `ng-invalid` (selVal=undefined) → botones disabled; `sel.value=1` (number) + `ionChange` valida. Confirmado con multi-empresa (3 opciones val 1/2/3) Y con **1 sola empresa** en ferrenuestro (value=1: opción única tampoco se auto-selecciona). `[jerez-2026-07-06][ferrenuestro-2026-07-07]`
- 🔴 **`value=null` + shadowRoot "Seleccione..." en un select `disabled=true` NO significa "sin asignar" — y forzar el `value` es lo que rompe.** En el form **nuevo** de potencial de grupo_fiel (1 empresa) `idEnterprise` llega `disabled=true`, **`value=null`** y el shadowRoot rotula literalmente **"Seleccione..."**: se lee como "la empresa no está asignada" y tienta a `s.value=1`. **Es falso** — al reabrir el registro Guardado el MISMO select devuelve `value=1` y rotula `"GRUPO FIEL, S.A. (GR"`, y el payload ya había viajado con `idEnterprise:1`/`coEnterprise:"00001"`. ⇒ **con `disabled=true` no tocar nada y no juzgar por `value` ni por el rótulo**; el oráculo válido es el par (botones antes/después) y el payload. Matiz nuevo sobre `[latino_cosmetica-20260729]`, que no había medido ni el `value` ni el rótulo. `[grupo_fiel-20260817]`
- ✅ **DISCRIMINADOR CERRADO (4 corridas coherentes) — dentro de CLIENTES la variante de `idEnterprise` la fija el Nº DE EMPRESAS:** **1 empresa ⇒ `disabled=true`**, auto-asignada, fuera de la validación (`[latino_cosmetica-20260729][grupo_fiel-20260817]`); **2+ empresas ⇒ `disabled=false`, `value=null`, `ng-invalid`**, obligatoria (el_palmar 2 · difranca 3). El corolario de difranca ("la fija el formulario") sigue valiendo **entre módulos**; **dentro de CLIENTES** el predictor es el nº de empresas. Regla operativa sin cambios: **leer `sel.disabled` SIEMPRE antes de decidir.** `[grupo_fiel-20260817]`
  - 🔴 **5.ª corrida — y queda probado que `enterpriseEnabled` NO tiene NADA que ver:** kron corre con `enterpriseEnabled=**TRUE**` y **1 sola empresa**, y entrega el select **exactamente igual** que grupo_fiel, que tenía `enterpriseEnabled=**FALSE**` y 1 empresa: `disabled=true` + `select-disabled`, `value=null`, **`ng-invalid=false`**, shadowRoot **"Seleccione..."**, 1 opción `{value:1 (number), "CHOCOLATES KRON, C.A"}`. ⇒ **la VG es irrelevante para esta variante; el único predictor es el nº de empresas.** Corolario práctico: un perfil que marque `enterpriseEnabled=true` con 1 empresa **no es una incoherencia a investigar** — el selector aparece, viene resuelto y todo caso de *cambio* de empresa es N/A. `[kron-20260817]`
  - ✅ **3.ª confirmación del anti-patrón, con el par medido de punta a punta `[kron-20260817]`:** con el form vacío Guardar/Enviar `disabled=true`; tras llenar **solo los 8 `ion-input`** y **sin tocar el select**, ambos `disabled=false` en el mismo tick; el payload viajó `idEnterprise:1`/`coEnterprise:"KRON_ADM"`; la nube guardó `id_enterprise=1`/`co_enterprise='KRON_ADM'`; y al **reabrir el Guardado** el MISMO select devolvió `value=1` (number) rotulando `"CHOCOLATES KRON, C.A"`. **Los 3 oráculos válidos son: el par de botones, el payload y la reapertura — nunca el `value` ni el rótulo del form nuevo.**

### Notas por cliente
- Enviar potencial = **3 alertas**: "¿Desea enviar nuevo Cliente Potencial?" (Cancelar/Aceptar) → "El cliente potencial será enviado" (OK) → "Cliente potencial nro. {ref} creado exitosamente" (OK). El registro pasa de Estatus Guardado → Enviado (sin trash) con el correlativo real. `[gmp-2606][rom-2606][ins-2610][gmp-2611]`
- **Correlación Ref UI = `id_client` (PK secuencial) en BD:** Nro. mostrado en alert "cliente potencial nro. X" = `id_client` de la tabla `potential_client`. El campo `co_client` es epoch timestamp (no el Ref). Confirmado piercar: UI "nro. 5" = `id_client=5`. # candidato — confirmar en 2+ corridas para graduar. `[prc-2606]`
- **`st_potential_client=1` en BD para registros Enviados:** el campo adopta valor 1 en servidor; los Guardados locales (Ref:0) nunca llegan a la BD. # candidato — confirmar en próxima corrida. `[prc-2606]`
- **Títulos mixtos en alertas de envío potencial:** 1ª alerta = "Denario Clientes" (¿Desea enviar nuevo Cliente Potencial?), 2ª y 3ª = "Denario Premium" (El cliente potencial será enviado → Cliente potencial nro. N creado exitosamente). Confirmado piercar + globalmp + don-theo + jerez. `[prc-2606][jerez-2026-07-06]`
- ⚠ **Destino post-envío:** queda en el **home de clientes**, NO navega a HOME principal — confirmado en insumar Y globalmp 0611 (matiza la nota previa `[gmp-2606]` que decía "navegación a HOME"; esa observación no se reprodujo). `[ins-2610][gmp-2611]`
- ⚠ **Tras Guardar, el form NO navega a la lista** — queda en el formulario; el registro Guardado solo es visible navegando a BUSCAR CLIENTE POTENCIAL. Confirmado don-theo e **hidroponias** La Tortuga, reconfirmado **ferrenuestro** La Tortuga. `[dth-2612][hidroponias-20260710][ferrenuestro-20260723]`
- **Dirty-guard al hacer `clickBack` desde el form potencial con cambios sin guardar (universal)** — sale un alert (título "Denario Clientes", message vacío) con 3 botones: **"Guardar y salir" / "Salir sin guardar" / "Cancelar"**. ⚠ Útil: **"Guardar y salir"** persiste el registro como Guardado Y sale del form → deja un Guardado deletable **sin re-tipear** los 9 campos (útil para montar DM-CLT-031). `[ferrenuestro-20260723]`
- **ferrenuestro (La Tortuga v6.6.18, window.ng=TRUE): la LISTA de clientes muestra solo "Saldo $"** — el saldo en Bs solo aparece en el DETALLE (multiCurrency: detalle con Saldo Bs. + Crédito $). No es FAIL. Alerts de guardado/borrado cierran con botón **"OK"** (el 1er paso del confirm de envío usa "Aceptar"). Potencial Ref 93 Enviado (POST potentialclient capturado; cotejo BD N/A por DSN Isla Coche viejo → PAYLOAD-OK). Correlación Ref UI = id_client reconfirmada. `[ferrenuestro-20260723]`
- Borrado de potencial Guardado: **directo sin confirmación previa** (solo alert de éxito). El trash (`ion-button[color="danger"]` dentro del `ion-item`) aparece **solo** en Estatus Guardado, ausente en Enviado. Confirmado también en **hidroponias**. `[gmp-2606][ins-2606][ins-2610][gmp-2611][ins-2622][hidroponias-20260710]`
- Mensajes exactos globalmp: guardado "Denario Cliente / ¡Cliente Potencial Guardado con exito!"; borrado "¡Cliente Potencial se borro con exito!" ("exito" sin tilde). `[gmp-2611]`
- don-theo coincide en mensajes: borrado título **"Denario Clientes"** (plural) "¡Cliente Potencial se borro con exito!"; guardado título "Denario Cliente" (singular). Confirma globalmp. `[dth-2612]`
- ✅ **El searchbar del listado de clientes filtra por `na_client` (substring, case-insensitive), NO por código** (confirmado en 2 corridas → universal): en dm-electronica "ABEDCO"→ABEDCO,C.A; "IMPORT"→EL TIO IMPORT; "00091" (código)→0. En latino_cosmetica "ANNELI"→1, "LOOKS"→0 (no sincronizado). Buscar SIEMPRE por nombre, no por código. `clientsOrderBy=na_client`. `[dm-electronica-20260713][latino_cosmetica-20260714]`
- ✅ **Sync PARCIAL por usuario/vendedor** (confirmado en 2 corridas → universal): el device solo sincroniza el subconjunto de clientes asignados al vendedor; la BD nube tiene todos pero el cliente designado por código puede NO aparecer en la app. dm-electronica (usuario 002): IMPORTADORA MAR-CHAZ 00091 en BD, ausente en device. latino_cosmetica (usuario 001): DISTRIBUIDORA LOOKS 4 (149) y MUNDO MAYOR DOS (398) NO sincronizan pese a tener docs en nube → usar ANNELI CA (13) / CABELLO COSMETICOS (37). Los agentes deben **DESCUBRIR el cliente de prueba en la UI**, no fiarse de un código de BD. `[dm-electronica-20260713][latino_cosmetica-20260714]`
- **latino_cosmetica (La Tortuga v6.6.18, `window.ng=TRUE`): cliente potencial Ref 9 Enviado BD-OK** (Test-CLT-SMOKE, RIF J987654321, idEnterprise=1); Guardar y Enviar SIN firma pese a `signatureClient=true`; envío = 3 alertas → "Cliente potencial nro. 9 creado exitosamente"; borrado directo sin confirmación previa. multiCurrency REAL (lista/detalle con "Saldo BSD" Y "Saldo $"). `[latino_cosmetica-20260714]`
- **latino_cosmetica en Isla Coche (build `1.0` / db 19, `window.ng=TRUE`) — 2ª playa del mismo slug: potencial Ref 4 Enviado.** 12/12 PASS. `Test-CLT-SMOKE-134438`, RIF J987654321, empresa LATINOCOSMETICA C.A. (**select disabled**, ver tabla). Envío = **3 alertas** ("Denario Clientes / ¿Desea enviar nuevo Cliente Potencial?" Cancelar-**Aceptar** → "Denario Premium / El cliente potencial será enviado" **OK** → "Denario Premium / Cliente potencial nro. 4 creado exitosamente" **OK**). Guardado = "Denario Cliente / ¡Cliente Potencial Guardado con exito!" (**OK**). Borrado **directo sin confirmación previa**: "Denario Clientes / ¡Cliente Potencial se borro con exito!". **Guardar y Enviar SIN firma** pese a `signatureClient=true` (reconfirma 0714). **multiCurrency REAL en lista Y detalle** ("Saldo BSD" + "Saldo $") — contrasta con ferrenuestro-20260723 (solo "Saldo $" en lista). BD local: `id_client=4` = Nro. Ref UI, `co_client=1785347054572.0`, `co_user="00014"` (≠ `co_login` 100), `pending_transactions`/`failed_transactions` VACÍAS ⇒ salió de la cola. ⚠ **`st_potential_client=2`** para Enviados en la tabla LOCAL — contrasta con la nota `[prc-2606]` (`=1` en servidor): posible dominio distinto local↔nube, dirimir en la capa web. `[latino_cosmetica-20260729]`
- 🔴 **DEFECTO (1ª detección): la LISTA de clientes muestra los saldos con la etiqueta CRUZADA y una conversión de más.** Lo rotulado **"Saldo BS"** es el saldo en **USD**, y lo rotulado **"Saldo USD"** es ese importe **dividido otra vez por la tasa**. El **DETALLE es el correcto**. Confirmado en 2 clientes de globalmp con la tasa 737,88 del tab Doc. de Venta: AS04 lista `USD 2,84 / BS 2.096,23` vs detalle `USD 2.096,23 / BS 1.546.766,19`; CB10 lista `USD 0,62 / BS 458,55` vs detalle `USD 458,55 / BS 338.354,87`. Triangulado contra los documentos (un solo doc de CB10 ya arrastra 22,24 USD ⇒ 0,62 USD es imposible). ⚠ **Al validar DM-CLT-002 NO basta con "la lista no está vacía": comparar los saldos de la lista contra el detalle del mismo cliente.** Verificar en la próxima playa si es del build v6.6.18/La Tortuga o específico de este cliente. `[gmp-20260730]`
- ⚠ **El dirty-guard "Guardar y salir" deja en la LISTA de potenciales (no en el home de clientes) Y la lista NO refresca:** el registro recién guardado **no aparece** aunque sí persistió en `potential_clients` (`id_client=0`). Se hace visible al salir de la lista y volver a entrar. Mismo patrón que el defecto de render de DEPÓSITOS (DM-DEP-018/019/020). Al montar DM-CLT-031 con este atajo, **re-entrar a la lista antes de buscar el trash**. `[gmp-20260730]`
- **globalmp (La Tortuga v6.6.18, `window.ng=TRUE`, `enterpriseEnabled=true` con 2 empresas): potencial Ref 184 Enviado.** 11 PASS / 1 FAIL (el FAIL es H-1, saldos de la lista). `Test-CLT-SMOKE-103002`, RIF J987654321, **`idEnterprise=2` COMERCIALIZADORA GLOBAL M&P** — `sel.disabled=false` ⇒ aplica la regla del **value NUMÉRICO** (`sel.value=2` + `ionChange`), NO la variante `select-disabled` de Isla Coche. Mensajes idénticos a la familia globalmp (guardado "Denario Cliente / ¡Cliente Potencial Guardado con exito!" OK; envío 3 alertas con "Aceptar"→OK→OK; borrado directo sin confirmación). **Sin firma** pese a no tener `signatureClient` registrado. BD local: `id_client=184` = Nro. Ref UI (correlación reconfirmada), `co_client=1785421775750.0`, **`st_potential_client=2`** para Enviados (reconfirma latino_cosmetica-20260729 vs `=1` de piercar en servidor), `co_user="YC01"`, colas vacías. POST `potentialclientservice/potentialclient` capturado **1 sola vez** con `data` completo. `[gmp-20260730]`
- ⚠ **`potential_clients` local: `has_attachments` vuelve como STRING `"false"`** — consistente con el quirk general de booleanos-como-string de `_comunes.md`. `[gmp-20260730]`
- **el_palmar (Isla Coche, build `1.0`/db19, `window.ng=TRUE`, `enterpriseEnabled=true` con 2 empresas): potencial Ref 31 Enviado, 12/12 PASS, 0 FAIL.** `Test-CLT-SMOKE-135439`, RIF J987654321, **empresa 1002 CENTRAL EL PALMAR, S.A. (`idEnterprise=1`)**. Es el caso que **acota la fila `select-disabled` de latino_cosmetica**: mismo build v1.0/db19 pero con DOS empresas ⇒ `sel.disabled=false`, `value=null`, `ng-invalid` ⇒ **aplica la regla del value NUMÉRICO**. Medición: con los 8 `ion-input` llenos y el select vacío, Guardar/Enviar **seguían `disabled=true`**; tras `sel.value=1`+`ionChange` habilitaron ambos. **Etiquetas de alert (¡ojo, contradicen alipascua!): guardado `[OK]` · envío paso1 `[Cancelar,`**Aceptar**`]` → pasos 2-3 `[OK]` · borrado `[OK]`**, borrado **directo sin confirmación previa**. Mensajes idénticos a la familia globalmp ("¡Cliente Potencial Guardado con exito!" / "¡Cliente Potencial se borro con exito!"). **Round-trip §9 perfecto**: al reabrir el Guardado por la zona izquierda (x = rect.x+0.35·w, real 129,228) vuelven los 9 campos **y la empresa** intactos. BD nube: `id_client=31` = Nro. Ref UI (**correlación reconfirmada**), `co_client=1785952445854.0`, `co_enterprise='1002'`/`id_enterprise=1` ✅, `co_user='1276'`, **`st_potential_client=1`** (⇒ **2ª confirmación en SERVIDOR de la nota `[prc-2606]`; el `=2` de latino_cosmetica/globalmp es de la tabla LOCAL — son dominios distintos**). POST `potentialclientservice/potentialclient` capturado **1 sola vez con `data`** (hook heredado del agente LOGIN, no reinstalado). `[el_palmar-20260805]`
- **el_palmar — el form de potencial NO ofrece adjuntos y SÍ guarda GPS:** 0 botones/iconos de cámara en `app-client-new-potential-client`, payload con `nuAttachments:0`/`hasAttachments:false` ⇒ `userCanUploadFiles`/`showCamera` son **N/A en CLIENTES** (nada puede bloquear el Enviar). En cambio el payload **sí** lleva `coordenada:"11.0490651,-63.865006"` (`coordenadaClient:null`) **sin ninguna exigencia ni alert de GPS**, coherente con `userMustActivateGPS=false` ⇒ responde `userCanSaveGPS`. `requiredComment` no tiene efecto acá (no hay campo comentario; su alcance es COBROS `tipo=C`). `[el_palmar-20260805]`
- ⚠ **el_palmar NO reproduce el defecto de saldos cruzados de `[gmp-20260730]` ni el de render de lista:** lista y detalle muestran los MISMOS saldos (VES 16.846.777,9664 / USD 25.800,1300) y se verifica `VES = USD × tasa 652,9726`; además la **suma de los 13 documentos del tab Doc. de Venta da exactamente el Saldo USD** del cliente (oráculo de coherencia interna). Y la lista de potenciales **sí refresca** tras guardar/enviar/borrar sin salir y re-entrar. ⇒ ambos defectos quedan acotados a la familia globalmp / v6.6.18. **Oráculo recomendado para DM-CLT-002/013: sumar la columna Saldo de los documentos y cotejarla contra el Saldo del detalle** — es barato y caza el cruce de etiquetas de una. `[el_palmar-20260805]`
- ⚠ **el_palmar — el device sincroniza un SUBCONJUNTO de documentos (reconfirma "sync parcial por vendedor"):** para 1000000803 la UI muestra **13 docs / 25.800,13 USD**, mientras la nube tiene **40 docs / 79.542,42 USD** en la empresa 1002 (y otros 17 / 180.314,50 en la 1003, fuera de alcance). **No es defecto** — la app es internamente consistente. Corolario: **los conteos/montos de documentos que vengan en el perfil del cliente hay que tomarlos como orientativos**, no como oráculo; el oráculo es la propia UI. ⚠ La tabla de nube se llama **`document_sale`** (no `document`), con `nu_balance` + `co_currency` + `co_enterprise`: filtrar SIEMPRE por empresa y moneda o los totales no cierran. `[el_palmar-20260805]`
- **difranca (El Yaque, build `1.0`/db19, `window.ng=TRUE`, `enterpriseEnabled=true` con **3 empresas**): potencial Ref 60 Enviado, 12/12 PASS, 0 FAIL.** `Test-CLT-SMOKE-122750`, RIF J987654321, **empresa DDHP_A12 `*DISTRIBUIDORA DIAZ` (`idEnterprise=2`)**. **3.ª confirmación de la variante "editable + vacío + obligatorio"** en CLIENTES (tras el_palmar con 2 empresas): `sel.disabled=false`, `value=null`, `ng-invalid`, shadowRoot "Seleccione...". Medición del par: con los **8 `ion-input` ya llenos** y el select vacío, Guardar/Enviar **seguían `disabled=true`**; tras `sel.value=2` (**number**) + `ionChange` habilitaron ambos en el mismo tick. ⇒ **la variante la fija el FORMULARIO, no el nº de empresas ni el servidor.** Las 3 opciones traen `value` **numérico** (2/3/4). ✅ **La 4ª empresa borrada en BD (`DDH_A12`, `co_operation='D'`) NO aparece en el selector.** ⚠ Las etiquetas se ven **truncadas a 19 caracteres** (`*DISTRIBUIDORA DIAZ`, `DISTRIBUIDORA DH VI`) porque la UI rotula **`lb_enterprise`**, no `na_enterprise` — cotejar contra `lb_enterprise` o el mismatch es falso positivo. **Etiquetas de alert (idénticas a el_palmar): guardado `[OK]` · envío paso 1 `[Cancelar, Aceptar]` → pasos 2-3 `[OK]` · borrado `[OK]` directo sin confirmación previa**; mensajes de la familia globalmp ("¡Cliente Potencial Guardado con exito!" / "¡Cliente Potencial se borro con exito!"). **Round-trip §9 perfecto** (reapertura por la zona izquierda, real 129,228: vuelven los 9 campos **y** la empresa como `2` number). BD nube: `id_client=60` = Nro. Ref UI (**correlación reconfirmada**), `co_client='1786120036250.0'`, `co_enterprise='DDHP_A12'`/`id_enterprise=2` ✅, `co_user='206'`, **`st_potential_client=1`** en nube vs **`=2`** en la tabla LOCAL para el MISMO registro (**reconfirma que son dominios distintos**, no un mismatch). Colas `pending_transactions`/`failed_transactions` vacías. POST `potentialclientservice/potentialclient` capturado **1 sola vez con `data`** (hook heredado del agente LOGIN, no reinstalado). ✅ **La lista de potenciales SÍ refresca** tras guardar/enviar/borrar (no reproduce el defecto de render de `[gmp-20260730]`). `[difranca-20260807]`
- **difranca — el form de potencial NO ofrece adjuntos NI firma, y SÍ guarda GPS** (reconfirma el_palmar): 0 controles de cámara/adjunto y 0 canvas de firma en `app-client-new-potential-client` pese a `userCanUploadFiles=true`, `showCamera=true` y `signatureClient=true`; payload con `nuAttachments:0`/`hasAttachments:false` ⇒ esas 3 VGs son **N/A en CLIENTES** (nada puede bloquear el Enviar). En cambio el payload **sí** lleva `coordenada` pese a `userCanSaveGPS=false`. `[difranca-20260807]`
- ⚠ **Tabla LOCAL `potential_clients`: NO existe `da_created`** — la columna de fecha es **`da_potential_client`**; un `SELECT` con el nombre equivocado aborta la transacción `sqlitePlugin` (`no such column`). Esquema real (22 cols): `id, id_client, co_client, id_user, co_user, na_client, nu_rif, tx_address, tx_address_dispatch, tx_client, na_responsible, em_client, nu_phone, na_web_site, da_potential_client, st_potential_client, id_enterprise, co_enterprise, coordenada, coordenada_client, nu_attachments, has_attachments`. `[difranca-20260807]`
- ✅ **Llenado de los 8 campos por TECLADO REAL, sin `fillIonInput`:** `pg.focus('ion-input[formcontrolname="X"] input')` + `pg.keyboard.type(val,{delay:25})` validó los 8 controles a la primera. ⚠ Al leer el estado del form, `document.querySelectorAll('ion-input')` **arrastra `login`/`password` de `app-login`** (sigue montado, con valores reales): filtrar por `offsetParent!==null` o anclar al componente. `[difranca-20260807]`
- ✅ **difranca NO reproduce los 2 defectos de saldo/crédito del tag 20** pese a ser el escenario exacto de ambos (`showCreditLimit=true`, tasa 752,09, cliente de mayor deuda): lista y detalle muestran los MISMOS valores (BSD 47.023.577,39 / US$ 62.523,87), `Σ allDocuments.nuBalance` (149 docs) = **62.523,87** = `saldoFuerte` exacto, `saldoLocal` = ese × 752,09, y el Crédito US$ de la UI = `client.nu_credit_limit` de BD **exacto** (3.008.377,20) sin multiplicación extra. ⇒ **CLT-LISTA-SALDOS-CRUZADOS y CLT-CREDITO-MULTIPLICADO quedan acotados a la familia globalmp / v6.6.18.** `[difranca-20260807]`
- **kron (Isla Coche, build `1.0`/db19, `window.ng=TRUE`, `enterpriseEnabled=true` con **1 sola empresa**): potencial Ref 78 Enviado, 12/12 PASS, 0 FAIL.** `Test-CLT-SMOKE-150841`, RIF J987654321, empresa **`KRON_ADM` "CHOCOLATES KRON, C.A" (`idEnterprise=1`, select `disabled`)**. Vendedora `scarlet` / `co_user 'VE0002'` / **30 clientes** (la lista trae exactamente esos 30 ⇒ no es lista corta). **`clientsOrderBy=co_client`** ⇒ la lista viene ordenada **por CÓDIGO**, no por nombre (contrasta con dm-electronica/latino_cosmetica) — pero **el searchbar sigue filtrando por `na_client`** (`BICENTENARIA` → 30→3). **Etiquetas de alert idénticas a el_palmar/difranca**: guardado `[OK]` "Denario Cliente / ¡Cliente Potencial Guardado con exito!" · envío paso 1 `[Cancelar, Aceptar]` → pasos 2-3 `[OK]` · borrado `[OK]` **directo sin confirmación previa**. **Round-trip §9 perfecto** (reapertura zona izquierda, real 129,238: vuelven los 9 campos **y** la empresa como `1` number). BD nube: `id_client=78` = Nro. Ref UI (**correlación reconfirmada**), `co_client='1786993684489.0'`, `co_enterprise='KRON_ADM'`/`id_enterprise=1` ✅, `co_user='VE0002'`, **`st_potential_client=1`** en nube vs **`=2`** en local (**5.ª confirmación de dominios distintos**, no mismatch). Cotejo **16/16 campos payload↔nube**. Colas vacías, sync **INMEDIATA**. ✅ **La lista de potenciales SÍ refresca** (no reproduce el defecto de render de `[gmp-20260730]`). Form **sin adjuntos ni firma** pese a `userCanUploadFiles`/`showCamera`/`signatureClient` = true (reconfirma el_palmar/difranca); payload **sí** lleva `coordenada`. `[kron-20260817]`
- 🔴 **kron — DEFECTO NUEVO: las líneas `Crédito Disp.` del DETALLE traen las etiquetas de moneda CRUZADAS y la conversión INVERTIDA** (hermano de `CLT-LISTA-SALDOS-CRUZADOS`, pero en el detalle y **conviviendo con saldos correctos**). Con `nu_credit_limit=0` (verificado en BD), `Crédito Disp. BS` muestra el importe en **USD** y `Crédito Disp. USD` muestra ese importe **dividido** otra vez por la tasa. Medido en **2 clientes**: `J504480975` (saldo 87.234.295,07 BS / 113.134,08 USD → muestra `-113.134,08` BS y `-146,72` USD) y `J075129342` (29.713.891,83 / 38.535,92 → `-38.535,92` y `-49,98`); tasa 771,07. Modelo: `availableCreditConversion`. ⇒ **Al validar DM-CLT-009 no alcanza con comparar los saldos lista↔detalle: revisar también las 4 líneas de Crédito.** `[kron-20260817]`
- ✅ **Oráculo BARATO de coherencia de saldos (mejora la receta de `[el_palmar-20260805]`): leerlo del COMPONENTE, sin paginar la tabla.** `ng.getComponent(document.querySelector('app-client-detail'))` expone `allDocuments`, `saldoLocal`, `saldoFuerte`, `availableCreditConversion`, `nuCreditLimitConversion`, `documentSalesTotalRows`. En kron `Σ allDocuments.nuBalance` (20 docs) = **113.134,08** = `saldoFuerte` **exacto**, y `saldoLocal` = ese × 771,07 ⇒ saldos sanos en 1 sola lectura, sin sumar la columna de la UI. `[kron-20260817]`
  - 🔴 **ACOTADO `[run_vzla-20260818]`: elegir el campo según `hardCurrency`.** En tenants **mono-moneda** (`hardCurrency=""`) el saldo vive en **`saldoLocal`** y **`saldoFuerte` queda en `0`** ⇒ leer solo `saldoFuerte` (como en kron, que era multi-moneda) devuelve 0 y se lee como "el saldo no cargó". **`hardCurrency` vacío ⇒ `saldoLocal`.**
- ⚠ **kron: hay TRES "MINIMARKET BICENTENARIA", no dos** — además de CCS (`J504480975`) y VALENCIA (`J505383973`) existe **`J409215121` MINIMARKET BICENTENARIA, C.A.** (165.823,96 USD, el mayor de los tres) y los tres salen en la misma búsqueda. **Anclar por `co_client` es obligatorio.** `[kron-20260817]`
- **run_vzla (La Tortuga, build `1.0`/db19, `window.ng=TRUE`, `enterpriseEnabled=true` con **1 sola empresa** `FERRE_N` "CORPORACION FERRE 19"): potencial Ref 194 Enviado, 12/12 PASS, 0 FAIL.** Select `disabled=true` auto-asignado (**6.ª corrida** del discriminador "1 empresa ⇒ disabled"), presente **solo** en el form de potencial. Cotejo **16/16 campos payload↔nube**, sync **inmediata**, colas vacías, sin duplicados, diff de baseline por `id_user=470` exactamente **+1**. Round-trip §9 perfecto. Los ~7 alerts del módulo se resolvieron **sin un solo reintento** recorriendo `['Aceptar','OK','Eliminar']` por **igualdad exacta case-insensitive** + filtro `width>0` (reparto idéntico a el_palmar/difranca/kron). Patrones:
  - ✅ **`st_potential_client = 0` para el Guardado LOCAL — completa la tabla de dominios:** local `0`=Guardado / `2`=Enviado · nube `1`=Enviado. `[run_vzla-20260818]`
  - 🟡 **Observación abierta (`por confirmar`): el listado de clientes viene ordenado por SALDO DESC, no por `clientsOrderBy`.** 1.ª medición de `clientsOrderBy="due_date"` en toda la serie y el orden observado no lo respeta. Sin caso de smoke que lo cubra y sin oráculo de especificación ⇒ **no reportar como defecto; medirlo antes de asumir el orden de la lista**. `[run_vzla-20260818]`
  - ✅ **`CLT-CREDITO-MULTIPLICADO` no reproduce por ausencia estructural:** tenant mono-moneda (`hardCurrency=""`), no existe el par de líneas BS/USD y la aritmética de la única línea cuadra contra BD. Y el saldo negativo del detalle (−22,50) **no es defecto**: coincide al céntimo con `Σ document_sale.nu_balance` en nube (la cifra "1.258,92" del perfil era la suma solo de los positivos) ⇒ **al validar saldos, sumar TODOS los documentos, incluidas las notas de crédito**. `[run_vzla-20260818]`
- dm-electronica: mensajes idénticos a globalmp/don-theo (guardado "Denario Cliente / ¡Cliente Potencial Guardado con exito!"; envío 3 alertas; borrado "Denario Clientes / ¡Cliente Potencial se borro con exito!" directo sin confirmación). Empresa idEnterprise activa pese a `enterpriseEnabled=false` del dump (manda la UI). `[dm-electronica-20260713]`

---
