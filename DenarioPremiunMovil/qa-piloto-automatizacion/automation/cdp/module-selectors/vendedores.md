> Parte de `module-selectors/` — leer junto con `_comunes.md` (convención global).

## Módulo VENDEDORES

### Identidad
- Ruta: `/vendedores` · Componente raíz: `app-vendedores`
- Solo lectura · contenido: acordeones por empresa
- **Verificar `modules.vendedores.aplica` / `vgs.esVendedor`**

### Selectores probados
| Elemento | Selector CSS / técnica | Corrida | Notas |
|----------|------------------------|---------|-------|
| Entrada HOME → Vendedores | tile = `app-home a.ion-text-center` que envuelve `<p class="nombreModulos">Vendedores</p>`; click vía `MouseEvent` en el `<a>` | `[dth-2612]` | confirma estructura de tiles de HOME en v6.6.14 |
| Heading rol | `<h1>Vendedor</h1>` en `app-vendedores` | `[gmp-2606]` | confirma `esVendedor=true` (no usar ng.getComponent en prod) |
| Acordeones empresa | `ion-accordion.accordion-collapsed` ↔ `.accordion-expanded`; content en `[slot="content"]`. **Expandir: asignar `grp.value = acc.value` al `app-vendedores ion-accordion-group` + `ionChange`** (contraer con `grp.value = undefined`) | `[gmp-2606][ins-2610][gmp-2611]` | oráculo de expansión por `getBoundingClientRect().height` (0 colapsado, >0 expandido) — no depender de `offsetParent`. ⚠ **`mouse.click` en coords del header NO expande** en este build (queda `accordion-collapsed`) — CORRIGE `[gmp-2606]` ("click header ~180,101"). Misma técnica `grp.value=acc.value` ya usada en COBROS/DEPÓSITOS. Los acordeones no declaran `value` propio: Ionic asigna `ion-accordion-NNN` → leer `acc.value` en runtime. Reconfirmado don-theo y dm-electronica (El Yaque v6.6.18: altura 48↔441; `mouse.click` en header NO expande) `[gmp-2611][dth-2612][dm-electronica-20260713]` |
| Contenido KPIs | `ion-grid`/`ion-col` (puede venir vacío si API no devuelve) | `[gmp-2606]` | vacío = N/A, no FAIL |
| 🔴 **Botón atrás — `img.fechaAtras` SIN `<a>` padre** | `app-vendedores img.fechaAtras` filtrando `/atras/i.test(src)` **y** `rect.x < 100` → `pg.mouse.click` en el centro del rect (**~32,31**) | `[gmp-20260730]` | **`h.clickBack` y todo `→ closest('a')` FALLAN**: `app-vendedores` renderiza el back como `<img class="fechaAtras" src="iconosatras.png">` suelto dentro del `ion-col`, **sin ancla** (`querySelectorAll('a')` dentro de `app-vendedores` = **0**) ⇒ `closest('a')` da `null` y se lee como "no existe botón atrás". Clickear la **`<img>` directamente**. ⚠ Además `img.fechaAtras` **NO es unívoco**: hay una 2ª instancia `vendedoresNuevoBlanco.svg` en **x=302** (icono decorativo del header, misma clase, mismo tamaño 43×43) → filtrar por rect visible **no alcanza**, hay que discriminar por `src` o por `x`. Es el **3er sabor** del quirk de back del build: (a) `.fechaAtras` con copias rect 0×0 (pedidos), (b) sin `.fechaAtras` (productos), (c) **con `.fechaAtras` pero sin `<a>`** (vendedores) |
| 🔴 **Oráculo de expansión: NUNCA el `innerText` del contenido** | `[slot="content"]`.`getBoundingClientRect().height` o `acc.className.includes('accordion-expanded')` | `[gmp-20260730]` | El `[slot="content"]` **ya contiene el `innerText` completo de los KPIs estando COLAPSADO** (con `height=0`) ⇒ usar presencia de texto como oráculo de expansión da **falso positivo** (parece expandido sin estarlo), y como oráculo de "hay KPIs" da falso positivo cuando el acordeón nunca abrió. Escala de altura observada: **0** colapsado · **~20 px** expandido-pero-vacío (latino_cosmetica) · **281 px** (globalmp) · **393 px** (ferrenuestro) — no usar umbrales altos |

### Flujo mínimo probado
```
1. Click Vendedores → app-vendedores (acordeones empresa)
2. Click acordeón → expande/contrae; KPIs si API devuelve
3. clickBack → HOME
```

### Notas por cliente
- globalmp: 2 empresas (COMERCIALIZADORA DE, HC TRADING MARKET 20); KPIs vacíos esta sesión → N/A estructural. `[gmp-2606][gmp-2611]`
- ⚠ **globalmp 2026-07-30: los KPIs AHORA POBLAN — anula la nota anterior de "KPIs vacíos"** → DM-VND-002 **PASS pleno, no N/A**. Los 2 acordeones traen métricas: COMERCIALIZADORA DE → Cartera 357 / Activados 119 / Nuevos 2 / Nuevos Activados 0; HC TRADING MARKET 20 → Cartera 14 / Activados 119 / Nuevos 2 / Nuevos Activados 0; Días Hábiles 23 / Transcurridos 22 / Restantes 1 en ambos. globalmp pasa al grupo "KPIs pueblan" (insumar/piercar/ferrenuestro/dm-electronica). **Actualizar `modules.vendedores.kpis_disponibles` a `true` en el YAML.** ⚠ **HALLAZGO H1 abierto (servicio de métricas):** solo `Cartera Clientes` se segmenta por empresa; `Activados`/`Nuevos`/`Nuevos Activados` vienen **idénticos** en ambos acordeones ⇒ en HC TRADING MARKET la app muestra **Activados 119 sobre Cartera 14**, aritméticamente imposible (activados ⊆ cartera). Parece filtro por `idEnterprise` presente en la consulta de cartera y ausente en las de activación/altas. No bloquea el módulo (solo lectura, el caso PASA por su criterio); revisar junto al caso inverso de latino_cosmetica (2 corridas con KPIs vacíos) — mismo servicio. `[gmp-20260730]`
- **insumar: 1 sola empresa ("INSUMAR DISTRIBUIDOR") y KPIs SÍ poblan** → DM-VND-002 **PASS pleno, no N/A** (2 corridas: 0610 Cartera 163/Activados 4; 0622 Cartera 164/Activados 6 — los KPIs evolucionan, lo estable es que poblan). `[ins-2610][ins-2622]`
- **don-theo: 1 empresa propia** (acordeón único "COMERCIALIZADORA HNOS TORRES 2017, C.A.", `enterpriseEnabled=true`), **KPIs vacíos** esta sesión (API sin métricas) → DM-VND-002 N/A estructural — coincide con globalmp. `[dth-2612]`
- `esVendedor=true` confirmado en globalmp, romher, insumar, don-theo, piercar, ferrenuestro, dm-electronica, jerez, **latino_cosmetica**. `[gmp-2606][rom-2606][ins-2606][ins-2610][dth-2612][prc-2606][latino_cosmetica-20260714]`
- **latino_cosmetica: 1 empresa "LATINOCOSMETICA C.A." con KPIs VACÍOS** esta sesión (`ion-grid` en `[slot="content"]` renderiza solo `ng-container` placeholders, API sin métricas) → DM-VND-002 N/A estructural (contenido vacío no es FAIL, RUNTIME §4). Alinea con globalmp/don-theo/jerez (vacíos); contrasta insumar/piercar/ferrenuestro/dm-electronica (poblados). Técnica de expansión `grp.value=acc.value`+ionChange vigente en La Tortuga v6.6.18 `window.ng=TRUE` (`contentHeight` 0↔20px). `[latino_cosmetica-20260714]`
- ⚠ **latino_cosmetica 2026-07-29 (2ª corrida): KPIs VACÍOS REPRODUCIDOS — deja de ser "esta sesión"** → DM-VND-002 **N/A por dato, no estructural**. Cuadro idéntico a `[latino_cosmetica-20260714]`: 1 acordeón "LATINOCOSMETICA C.A.", `[slot=content]` con `ion-grid` presente pero `innerText` vacío y 0 `ion-col` poblados (~503 chars de HTML = solo placeholders `ng-container`); altura 0↔20 px. La mecánica `grp.value=acc.value`+`ionChange` (expandir) y `grp.value=undefined` (contraer) funciona al 1er intento con `window.ng=true`. ⚠ **Corolario para el oráculo de expansión: con KPIs vacíos la altura expandida es de solo 20 px** — no usar umbrales altos (ferrenuestro llega a 393 px con KPIs poblados) para decidir si expandió. Sugerido al equipo revisar el servicio de métricas de vendedor para este cliente. `[latino_cosmetica-20260729]`
- **piercar: 1 empresa "PIERCAR REPUESTOS C." con KPIs poblados** (`infoVendedores=false` autogenerado) — Cartera Clientes 81, Activados 2, Nuevos 0, Días Hábiles 22/Transcurridos 12/Restantes 10. Alinea con insumar (KPIs sí poblan); contrasta con globalmp/don-theo (KPIs vacíos). `[prc-2606]`
- **ferrenuestro: 1 empresa "FERRENUESTRO MAYOR," con KPIs POBLADOS** (`infoVendedores=false` autogenerado por Denario) → DM-VND-002 **PASS pleno, no N/A**: Cartera Clientes 178 · Activados 13 · Nuevos 1 · Nuevos Activados 1 · Días Hábiles 23/Transcurridos 6/Restantes 17 · Venta Real Mes 7.610,09 $. Alinea con insumar/piercar (KPIs sí poblan); contrasta con globalmp/don-theo/jerez (vacíos). Técnica de expansión `grp.value=acc.value`+ionChange vigente en build El Yaque `window.ng=false`; `esVendedor=true` (heading `<h1>Vendedor</h1>`). `[ferrenuestro-2026-07-07]`
- ✅ **el_palmar (Isla Coche v1.0/db19, `window.ng=TRUE`, 2 empresas): KPIs poblados, 2 acordeones.** Patrones nuevos:
  - **`infoVendedores` es legible en runtime:** `window.ng.getComponent(document.querySelector('app-vendedores')).infoVendedores` — **evita deducir la VG desde la UI**. En builds con `window.ng=true` es el modo directo de confirmarla. `[el_palmar-20260805]`
  - 🔴 **`comp.userInfo` tiene 1 entrada por EMPRESA, no por vendedor** — con N empresas hay N entradas (`id` 49/50 acá), **todas con el mismo `idUser`/`coUser`**. Es el modelo que alimenta los acordeones 1:1 ⇒ **usarlo como oráculo en vez de parsear `innerText`**, y no leer "2 entradas" como "2 vendedores". `[el_palmar-20260805]`
  - 🔴 **Oráculo del bug de segmentación de KPIs: contrastar contra `client_template_user`, NO asumir.** KPIs idénticos entre empresas **NO** implican el H1 de globalmp: la query `count(DISTINCT id_client) GROUP BY co_enterprise` decide. En el_palmar dio **144/144 ⇒ coincidencia real, no defecto**. Segunda prueba disponible: `planesCuotaEmpresa` (si difiere por empresa, el servicio **sí** segmenta). `[el_palmar-20260805]`
  - **`[slot=content]` con `ng-container` vacíos ⇒ contar los `<!--container-->`:** el bloque Cuota/Venta Real Mes existe **siempre** en el template; si no aparece, quedan comentarios `<!--container-->` en el HTML. Contarlos **distingue "el template no lo tiene" de "un `*ngIf` lo suprime"**. `[el_palmar-20260805]`
  - **Altura expandida con KPIs poblados en v1.0/db19 = 281 px.** Escala acumulada: **0** colapsado · **~20 px** expandido-vacío (latino_cosmetica) · **281 px** (el_palmar, globalmp) · **393 px** (ferrenuestro) ⇒ confirma: **no usar umbrales altos**. `[el_palmar-20260805]`
  - **Reconfirmados en v1.0/db19 Isla Coche:** el back es `img.fechaAtras` **sin `<a>`** (`src=iconosatras.png`, ≈32,31; la 2.ª instancia en **x=302** es `vendedoresNuevoBlanco.svg` decorativa → discriminar por `src` o por `x<100`), y la expansión por `grp.value=acc.value`+`ionChange` sigue vigente (los `ion-accordion` no declaran `value` propio: Ionic asigna `ion-accordion-69/70` → **leerlo en runtime**; `mouse.click` en el header sigue **sin** expandir). `[el_palmar-20260805]`
  - ⚠ **Al entrar a `/vendedores` queda 1 `ion-loading` VISIBLE** ⇒ `dismiss()` de los loadings **visibles** antes de operar (**nunca en bucle sobre todos** — ver `_comunes.md`). `[el_palmar-20260805]`
- **ferrenuestro 2026-07-23 (La Tortuga v6.6.18, window.ng=TRUE): KPIs POBLADOS, evolucionan entre corridas (dato, no bug)** → DM-VND-002 **PASS pleno**: Cartera Clientes 186 · Activados 50 · Nuevos 4 · Nuevos Activados 2 · Días Hábiles 23/Transcurridos 18/Restantes 5 · Venta Real Mes 35.253,86 $ (Cuota Mes 0 $), vs. 2026-07-07 (Cartera 178/Activados 13/Venta 7.610,09 $). Lo estable es que POBLAN; los valores cambian (mismo patrón que insumar entre corridas) → NO requiere cambio de selector. Técnica `grp.value=acc.value`+ionChange (pura-DOM) vigente; altura `[slot=content]` 0→393,4px; `esVendedor=true`. `[ferrenuestro-20260723]`
- **dm-electronica: 1 empresa "BOTZ" con KPIs POBLADOS** → DM-VND-002 **PASS pleno, no N/A**: Cartera Clientes 80 · Activados 1 · Nuevos 0 · Nuevos Activados 0 · Días Hábiles 23/Transcurridos 9/Restantes 14 · Cuota Mes 0 UNI · Venta Real Mes 2 UNI. Alinea con insumar/piercar/ferrenuestro (KPIs sí poblan); contrasta con globalmp/don-theo/jerez (vacíos). Acordeón único, `enterpriseEnabled=true`, `esVendedor=true` (heading `<h1>Vendedor</h1>`). `[dm-electronica-20260713]`
- ✅ **difranca (El Yaque v1.0/db19, `window.ng=TRUE`, 3 empresas): KPIs poblados, 3 acordeones.** Patrones nuevos:
  - 🔴 **Oráculo DEFINITIVO de `VND-KPIS-SIN-SEGMENTAR`: la desigualdad `Clientes Activados > Cartera Clientes`.** No hace falta BD: si en **alguna** empresa los activados superan la cartera, el defecto está **probado** (activados ⊆ cartera por definición). En difranca `DISTRIBUIDORA DH VI` dio **7 > 1**. **Complementa** la receta de `[el_palmar-20260805]` (que exigía cruzar `client_template_user`, y allí dio 144/144 = coincidencia real, no defecto): **primero buscar la desigualdad; solo si no aparece, ir a la BD.** `[difranca-20260807]`
  - 🔴 **La segmentación es PARCIAL — comparar KPI por KPI, no el bloque entero.** El defecto **no** es "todos los KPIs iguales": `Cartera Clientes` **sí** se segmenta correctamente (178/33/1, exacto contra `client_template_user`) mientras `Activados`/`Nuevos` no. Un chequeo tipo "¿los 3 acordeones muestran lo mismo?" da **falso negativo**. Reconfirma el H1 de `[gmp-20260730]`. `[difranca-20260807]`
  - **Expandir los N acordeones A LA VEZ para diagnosticar segmentación:** `grp.multiple = true` + `grp.value = [todos los .value]` + `ionChange` permite leer y comparar las N empresas **en una sola llamada**. Contraer: `grp.value = undefined`. `mouse.click` en el header sigue **sin** expandir (reconfirma). `[difranca-20260807]`
  - ⚠ **CORRIGE `[el_palmar-20260805]`: `comp.userInfo` NO es fiable como oráculo** — en difranca llegó **vacío (`[]`)** con los 3 acordeones renderizados y los KPIs completos. Usar los `[slot=content]`, no `userInfo`. `[difranca-20260807]`
  - **Reconfirmados en v1.0/db19 El Yaque:** `infoVendedores` legible en runtime (`ng.getComponent(document.querySelector('app-vendedores')).infoVendedores` → `false`); back = `img.fechaAtras` **sin `<a>`** (`src=iconosatras.png`, filtrar `width>0 && x<100`; la 2.ª en x≈302 es `vendedoresNuevoBlanco.svg` decorativa) → `mouse.click` ≈(32,31); al entrar a `/vendedores` queda **1 `ion-loading` visible** ⇒ `dismiss()` de los **visibles**; altura expandida con KPIs poblados = **281 px** (misma escala que el_palmar/globalmp). `[difranca-20260807]`
  - **Los acordeones rotulan `lb_enterprise` TRUNCADO a 19 chars** (`*DISTRIBUIDORA DIAZ` / `DIFRANCA C.A` / `DISTRIBUIDORA DH VI`) ⇒ cotejar contra `lb_enterprise`, **nunca** contra `na_enterprise` (ver `_comunes.md`). `[difranca-20260807]`
- **jerez: 3 empresas (acordeones "INV JEREZ MOTORS VALERA/CARACAS/TURMEREMO", idEnterprise 1/2/3), KPIs VACÍOS** → DM-VND-002 N/A estructural (ion-grid presente sin métricas; API no puebla). Alinea con globalmp/don-theo. Antes `[jerez-2026-06-22]` los 3 rótulos eran idéntico "INVERSIONES JEREZ MO..."; ahora distintos por ciudad. La técnica de expansión `grp.value=acc.value`+ionChange **sigue vigente en el build refactorizado El Yaque** (`window.ng=false` no la afecta — skill pura-DOM); `mouse.click` en header NO expande. `esVendedor=true` reconfirmado (heading `<h1>Vendedor</h1>`). `[jerez-2026-07-06]`
